# 프로젝트 진행 상황

> 최종 수정: 2026-08-26
> 장르: 멀티플레이어 Extraction 게임 (알파 단계)
> 엔진: Unity 6000.4.0f1 / URP 17.4.0

---

## 완료된 것들

### 네트워크/UI
- [x] (2026-08-25 #2) 탄약 소모 복구 + 발사 경로 미구현 지점 표기 — 발사 경로의 `TEMP:` 2건(탄약 확인·탄약 차감)을 해제해 `Fire()`가 다시 탄창을 검사하고 `magazine.quantity--`를 수행한다. **함께 탄약 동기화 정책이 확정됐다(사용자 판단)** — 매 발사마다 인벤토리를 서버와 맞추는 것은 낭비이므로 하지 않고, 장전 등 확실한 동기화 시점까지 **클라와 서버 수치가 어긋나는 것을 정상 범위로 본다**. 서버는 발사 패킷 수신 시 가능한 경우 차감하되 패킷 유실과 클라 값 조작을 전제로 **최종 판정 권한을 갖는다**. 이 정책 덕에 착수 전 제기했던 "재동기화 때 탄약이 되살아나 사실상 무한 탄약" 우려가 해소됐다 — `D2CFullInventorySync`가 탄약을 덮는 것은 정상 동작이다. 정책은 코드 주석과 `Controller/CLAUDE.md`에 남겼다(대화에만 두면 다음 세션에서 결함으로 재해석된다). `EmptyAmmoFire()`에는 `TEMP:` 로그 1줄을 넣었고 기존 `TODO:`(사운드·재장전 UI)는 유지했다 — `_fireBlocked` 덕에 클릭 1회당 1줄이라 연사로 도배되지 않는다. `ProcessFire()`·`ProcessHit()` 상단에는 별도 작업 예정을 알리는 `TODO:`를 달되 **"발사/피격 처리 미구현"이 아니라 "연출·이펙트 미구현"으로 문구를 잡았다** — 두 함수 모두 이미 동작 중이고 특히 `ProcessHit()`은 발사 패킷 전송이라는 본 기능을 수행하므로, "미구현"이라 적으면 `Controller/CLAUDE.md`가 이 함수를 스텁으로 적어둔 것과 같은 오해가 반복된다(그 낡은 항목도 이번에 함께 고쳤다). 끝으로 `ProcessHit()`에 **유효 `hitObjectId`일 때만 찍는 `TEMP:` 로그**를 추가했다 — 현재 `ICombatTarget` 구현체가 플레이어 둘뿐이라 사실상 플레이어 명중 확인용이고, T14로 비플레이어 전투 오브젝트에 보급되면 그 검증에 그대로 쓴다. **착수 전 조사에서 재장전 부재가 드러났다 — 아래 우선순위 12번으로 분리**. **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-25 #3) 발사선을 몸 기준으로 교정 + 명중 누락 원인 계측 — 적 바로 앞 1~2m에서 30발을 쐈는데 데미지가 3발 내외만 들어가던 문제. **원인을 추정하지 않고 관문별 카운터부터 심어** 좁혔다(입력 → 차단 플래그 → 쿨다운 → 탄약 → 레이캐스트 → 대상 판정 → 송신). 실측은 `cooldownPass=31`(30발 + 탄창 소진 1회)·`ammoFail=1`·`ray=30`·`hit=30`·`miss=0`인데 **`targetNull=30`·`valid=0`, 비대상 명중 분포가 `Plane=30`** — 입력·쿨다운·탄약·송신은 전부 무결하고 **레이는 100% 명중하되 30발 전부 바닥에 박혔다.** `sent=30`이므로 네트워크·서버는 무관하다(클라가 유효 대상을 한 번도 싣지 않았다). `RaycastAll`로 경로를 통째로 뽑아 **원인이 둘로 갈렸다**: ① **발사 원점이 카메라였다** — 3인칭 카메라에서 레이가 출발해, 몸에서 나가야 한다는 설계 의도와 어긋났다 ② 적이 1.2m 앞 `angle=9.3°`에 있고 `collider=2/3`으로 콜라이더가 존재하는데도 `RaycastAll`이 `Plane` 하나만 반환했다 — 기하로 풀면 레이가 적 거리에서 흉부보다 18~24cm 아래(허벅지~무릎)를 지나므로 **적 프리팹이 그 높이에서 레이캐스트를 받아낼 오브젝트를 갖고 있지 않다**(에디터 작업이라 이번 범위 밖 — 우선순위 0번으로 분리). **이번에 고친 것은 ①이다.** `CalculateSpreadRay()` → `CalculateFireRay()`로 바꿔 원점은 `_shotPoint`(플레이어 루트 직속, **가슴팍**), 방향은 `_aimTarget`으로 **수렴**시킨다. 카메라 축과 평행하지 않으므로 **엄폐물 뒤에서는 총구 앞의 벽에 맞는다 — 의도된 동작으로 확정(사용자 판단)**. `_shotPoint`가 루트 자식이라 **피치를 따라가지 않는 것도 의도다**(피격 판정용 원점이고, 시각 이펙트는 추후 총구 기준으로 별도 처리). **서버가 발사선을 검증하지 않는 것도 확인**해서 원점 변경이 판정에 영향을 주지 않는다. **함께 고친 잠복 결함 셋** — ⓐ `Update()`에서 `ProcessFire()`가 `ProcessAim()`보다 **먼저** 돌아, 발사가 `_aimTarget`의 **직전 프레임 값**을 읽고 있었다(반동으로 시점이 초당 수십 도 움직이는 중이라 그대로 각도 오차이고, Setup 직후 첫 발은 프리팹 초기 위치를 읽었다. 순서를 뒤집었고 부수 효과로 Rig도 더 신선한 값을 받는다) ⓑ 스프레드 기저축이 `Vector3.Cross(dir, Vector3.up)`이라 **시점 피치가 ±90에 닿으면 영벡터가 되어 스프레드가 깨졌다** — 실측에서 피치 90에 도달한 기록이 있어 실제로 밟는 경로다. `dir.y` 크기를 보고 `up`/`right` 중 고르게 바꿨다 ⓒ 원뿔 각도를 `cos/sin` 분해로 정확화(기존 `(dir+offset).normalized`는 실제 편차가 `atan(sin θ)`라 미세하게 작았다). 죽은 필드 `_raycastDir` 제거. **자기 PlayerObject 레이캐스트 제외는 `TODO:`로 남겼다** — 전용 레이어가 필요해 에디터 작업이 선행된다. 계측용 임시 코드는 전부 회수했고(`git diff` 공백 확인) **발사선 검증용 `TEMP:` 시각화만 남겼다**(`DrawFireRayDebug`, 잔상 2분, 회수 조건은 '발사 원점·수렴 검증 후 제거'). **②가 남아 있어 이 변경만으로는 명중이 잡히지 않는다 — 검증 수단은 시각화뿐이다.** **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-25 #4) 적 히트박스 신설 + 무기 콜라이더 차단 — (#3)에서 분리했던 ②를 닫았다. 프리팹을 열어 **원인이 확정됐다**: `OppoPlayerObject.prefab`은 루트와 `Aim` 두 오브젝트뿐이고 `HB0/1/2OppoPlayer`·`HB0/1/2Player` 6종 전부 콜라이더가 **0개**다. 실측 `collider=2/3`은 **전부 손에 든 M4의 MeshCollider**였다(`Weapon_2_M4`가 3개 중 2개 활성으로 개수까지 정확히 일치. `AK`는 4개, `M16`은 3개). 즉 **적의 몸에는 콜라이더가 없었고, 지금까지 들어간 데미지는 전부 적의 총을 맞힌 것이다** — 무기가 `WeaponSocket` → 모델 → `OppoPlayerObject` 하위라 `GetComponentInParent<ICombatTarget>()`가 위로 올라가 정상 피격으로 판정한다. 로컬은 `PlayerObject.prefab`의 `CharacterController`(Height 0.81 / Radius 0.15 / Center y 0.42)가 몸 캡슐 역할을 하는데 오포에는 대응물이 없는 비대칭이었다. **해법으로 루트 캡슐(에디터 1분)이 아니라 본 히트박스(코드)를 골랐다** — 루트 캡슐은 애니메이션을 따라가지 못해 **뻗은 팔·손이 판정에서 빠진다**는 지적(사용자)이 결정적이었다. `OppoPlayerController.BuildHitboxes()`가 `Setup()`에서 본에 캡슐 11개를 붙인다(머리/상체/골반/상완·전완 ×2/허벅지·정강이 ×2). **치수를 하드코딩하지 않은 것이 이 설계의 핵심이다** — 길이는 본↔자식 본 실측 거리, 반지름은 **골반→머리 거리에 대한 비율**이라 `HB0/1/2`의 비율·스케일이 서로 달라도 모델이 교체돼도 따라간다(튜닝 대상은 비율뿐). `height = 본 길이`·`center = 길이/2`로 인접 캡슐이 관절에서 맞물려 부위 사이에 틈이 없고, 손·발은 전완·정강이 캡슐이 관절 너머를 덮어 생략했다. **에디터 작업이 필요 없어졌다** — 프리팹 3종에 각각 손대는 대신 코드 한 곳이 6개 모델을 전부 커버한다. **로컬 `PlayerController`에는 만들지 않는다(의도된 비대칭)** — 내 클라이언트가 나를 대상으로 레이캐스트할 일이 없고(내 피격 판정은 상대 클라이언트가 자기 쪽 `OppoPlayer`에 대고 하며 서버는 발사선을 검증하지 않는다), 발사 원점이 가슴팍이라 **내 팔 히트박스가 내 총알을 막는다**. **처리한 함정 둘** — ⓐ `LookRotation`의 up 힌트: 척추·다리 본은 축이 거의 수직이라 기본 `Vector3.up`과 평행해진다(**대부분의 히트박스가 해당**). (#3)의 스프레드 기저축과 같은 특이점이 다른 자리에서 또 나왔다 ⓑ 본 스케일: 콜라이더 치수는 로컬 단위인데 길이는 월드에서 재므로 `lossyScale`로 나눈다(균등 스케일 가정). 함께 **무기 콜라이더를 장착 시점에 끈다**(`GameObjectController.DisableWeaponColliders`, 양쪽 `EquipWeapon`이 호출) — 몸 히트박스가 생긴 이상 총이 피격 부위로 남을 이유가 없고 자기 무기 자탄 경로도 사라진다. **콜라이더를 둔 이유가 애초에 없었다는 확인(사용자)** 후에도 프리팹에서 지우지 않고 장착 시점에만 끈 것은, 바닥 무기 습득 용도가 생기면 되살리기 위함이다. `isTrigger = true`라 **Project Settings의 `Queries Hit Triggers`에 의존한다**(Unity 기본 켜짐, **미확인**) — 꺼져 있으면 히트박스가 통째로 죽는다. `Fire()`의 자기 제외 `TODO:`는 유지했으나, 무기 콜라이더가 꺼졌고 로컬에 히트박스가 없어 남는 자기 콜라이더가 `CharacterController` 하나뿐이며 **레이가 그 캡슐 안에서 출발하므로 Unity가 보고하지 않는다** — 검증 후 새 레이어 없이 닫힐 가능성이 높다. **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-25 #5) 플레이어 최대 HP 200.00으로 조정 — 서버 상수와의 수동 동기화 지점 명문화 — 서버 `PlayerObject::DEFAULT_MAX_HP`가 20000으로 내려가면서 클라 `IngameScene.MAX_HEALTH_POINT`도 `100000` → `20000`으로 맞췄다(전투 수치는 전부 1/100 고정소수점이라 1000.00 → 200.00 HP). **값 자체보다 이 상수의 성격이 기록할 거리다** — 최대 HP는 어떤 패킷으로도 오지 않아(`D2CNotifyHealthChange`에는 현재 HP 절대값만 실린다) 클라 쪽 유일한 출처이고, **서버 상수와 계약상 항상 같아야 하는데 어긋나도 컴파일·통신은 정상이다.** 증상이 체력바 비율만 조용히 틀어지는 것뿐이라 눈에 띄지 않으므로, 값만 바꾸지 않고 상수 선언부에 서버 심볼 이름을 주석으로 못박고 `Scenes/CLAUDE.md`에도 계약으로 남겼다(방어구 스펙 테이블 `ItemDBHelper._armorSpecs`도 서버 `ItemDataManager`에 복제된 같은 종류의 동기화 지점이라 함께 적었다). **파생 로직은 한 줄도 손대지 않았고 전수 확인 결과 그게 맞았다** — `Assets/Scripts` 전체에서 이 상수를 읽는 곳은 `_currentHealthPoint` 필드 초기값과 `SyncHealthBarMax()`의 `SetMaxHP()` 둘뿐이고 **HP 관련 숫자 리터럴 잔존은 0건**이라 상수 하나로 전부 따라온다(HP 로직을 추가할 때 리터럴을 쓰면 출처가 갈려 이 계약이 무너진다). `_currentHealthPoint`의 초기값을 상수로 두는 구조도 그대로다 — 0으로 바꾸면 스폰 직후부터 사망으로 오판돼 실드 재생이 멈춘다(T12에서 이미 겪은 함정). 서버는 헤더 상수라 `DedicateProcess` 재빌드가 선행 조건이다. **검증 완료** — 피격 수(맨몸 5발 / 경량 조끼 7발)와 스폰 직후 체력바 만충 표시를 확인했다. **부수 효과가 본 변경보다 크다: 아래 우선순위 12번(재장전)의 시급성 근거가 무너졌다** — 무방어 21발 / 방어구 착용 40발 이상이던 것이 5발 / 7발이 되어 탄창 30발로 킬이 충분해졌고, 3번의 "처치 검증은 무방어 상태에서만" 제약도 함께 풀렸다
- [x] (2026-08-26 #0) **2인 접속 전투 전 구간 검증 + 반동 '결함'이 오진이었음을 확정** — 2인 접속으로 **데미지 주고받기 → 사망 → 로비 복귀**와 **귀환 → 로비 복귀**가 전부 동작했고, Project Settings의 `Queries Hit Triggers`도 **켜져 있는 것으로 확인**됐다. 이 한 판으로 **(2026-08-25 #1·#2·#3·#4) 네 건의 "Unity 컴파일·런타임 모두 미검증"이 한꺼번에 해소됐다** — 특히 **데미지가 오갔다는 사실 자체가 (#4) 히트박스 11개 캡슐이 레이캐스트에 잡혔다는 증거**라, 최우선이던 0번에서 막고 있던 것(`Queries Hit Triggers` 확인 + 히트박스가 서는지)이 통째로 사라지고 **반지름 비율 튜닝만 남았다.** (#3)의 발사선도 유효 대상 판정까지 실증됐다. **가장 값진 것은 반동 오진을 걷어낸 것이다** — (2026-08-25 #3) 계측 중 "`_recoilTarget`이 감쇠하지 않는다(30발에 47도 누적, 멈춰도 안 돌아옴)"를 **결함으로 등록하고 우선순위 1번에 올려뒀는데, 시간 회복을 두지 않고 플레이어가 마우스로 직접 잡는 것이 확정된 설계였다.** 즉 코드가 이미 그 설계였고 고칠 것이 없었다. 되돌리면서 근거까지 확인해 남겼다: **누적이 무한히 커져도 `ProcessMouseLook()`의 `_aimPitch` 클램프가 `[-80 + _recoilPitch, 90 + _recoilPitch]`로 반동만큼 같이 밀려 가동 범위가 보존되고, 이 클램프에서 `_recoilPitch` 항을 빼는 순간 그때 진짜로 조작 범위가 잘린다.** `_recoilApplySpeed`의 Lerp는 회복이 아니라 킥을 몇 프레임에 걸쳐 얹는 용도이며, `_currentSpread`만 자동 회복하는 비대칭은 의도된 것이다. **`Controller/CLAUDE.md`에도 같은 내용을 적은 것이 이 작업의 실질이다** — 다음 세션은 progress.md보다 코드와 모듈 문서를 먼저 보므로, "감쇠 경로가 없다"를 코드에서 발견하고 다시 결함으로 집어들 자리를 거기서 막아야 한다. **오진의 교훈**: (#3)은 관문별 카운터로 실측해 원인을 좁힌 모범 사례였는데, 같은 계측에서 나온 부산물을 **설계 의도를 확인하지 않고 결함으로 단정**했다. 실측값이 정확한 것과 그것이 결함인 것은 별개다. 함께 우선순위 목록을 정리했다 — 신규 5건 등록(T19 크로스헤어 / T20 내 총알 궤적 / T21 상대 총알 궤적 / T22 빈 탄창 발사 애니메이션 / T23 인게임 옵션 UI), `MuzzlePoint`가 무기 프리팹 3종에 이미 추가되어 **T20·T21이 순수 코드 작업으로 내려왔고**, 1번은 마우스 감도 하나만 남긴 채 **T23 선행으로 보류**했다(인게임에서 옵션을 열 수단이 없어 체감으로 맞춰야 하는 값을 시험할 방법이 없다)
- [x] (2026-08-26 #1) 총알 궤적 표시 — 내 발사·상대 발사 (T20·T21) — **게임 화면에서 탄도를 볼 수단이 처음 생겼다.** 그동안 유일한 시각화가 `DrawFireRayDebug`(`Debug.DrawLine`)라 Scene 뷰에서만 보였고, 그래서 0번(히트박스 반지름 튜닝)에서 "히트박스가 좁은 건지 내가 빗나간 건지"가 갈리지 않았다. 무기 프리팹 3종에 `MuzzlePoint`가 이미 들어와 있어 코드만으로 끝났다. **신규 `BulletTracer`는 `DeathCameraController`와 같은 패턴이다** — `GameObjectController` 계층이 아니고 프리팹도 없으며 정적 `Play(from, to)`가 런타임에 `@BulletTracer` 리그를 만든다. `LineRenderer` 16개를 링버퍼로 도는 것은 **한 프레임에 나와 여러 오포가 동시에 쏠 수 있어서**다. **1프레임 지속은 `Time.frameCount` 비교로 구현했고 `LateUpdate`를 쓰면 안 된다** — 렌더링보다 먼저 돌아서 선이 한 번도 그려지지 않는다(끄는 조건을 `Update()` 한 곳에 모아, 지속을 시간 기반+페이드로 바꾸게 되면 거기만 고치면 된다). 씬 전환에 리그가 파괴되지만 파괴된 오브젝트에도 `== null`이 true라 다음 `Play()`에서 자연히 재생성되므로 정리 코드를 넣지 않았다. `MuzzlePoint` 탐색은 `GameObjectController.FindMuzzlePoint()` 공용 헬퍼(`DisableWeaponColliders` 옆)이며 **재귀 탐색이다** — 모델 하위에 중첩돼 있어 `transform.Find`로는 못 찾는다. **캐시 무효화 자리가 함정이었다** — 무기 GO가 사라지는 지점 한 곳에서 비우면 맨손 return과 프리팹 캐시 미스 return이 모두 그 뒤라 한 자리로 덮인다. **빗나갔을 때 처리가 내 궤적과 상대 궤적에서 갈린다(의도된 비대칭)** — 내 쪽은 레이를 직접 갖고 있어 `GetPoint(1000f)`까지 그리고, 상대 쪽은 `hit_point`가 안 오면 방향 자체를 모르므로 생략한다(proto 주석도 "없으면 미설정 — 이펙트 불필요"). `HandleWeaponFireBroadcast`의 미등록 발사자 early return은 **내 발사가 두 겹으로 그려지는 것을 막는 가드를 겸한다**(룸 브로드캐스트라 내 발사도 돌아오는데 나는 `_oppoPlayers`에 없다). **`DrawFireRayDebug`는 회수하지 않았다 — 착수 전 "T20이 대체한다"고 적었던 것을 계획 단계에서 정정했다**: 궤적은 `MuzzlePoint`에서 출발해 `_shotPoint` 원점·수렴을 아예 그리지 않으므로 대체가 성립하지 않는다. **2인 접속 런타임 검증 완료**
- [x] (2026-08-26 #2) 발사되지 않을 때 발사 애니메이션·행동 상태 끄기 + 맨손 발사 차단 (T22) — 빈 탄창으로 마우스를 누르고 있으면 `EmptyAmmoFire()`가 `_fireBlocked`를 세워 발사는 막히는데 **모션은 계속 돌았다.** `_fireBlocked`가 애니메이션 조건에 없었기 때문이다. **내 화면만의 문제가 아니었다** — 같은 프로퍼티가 `ActionState`를 겸하고 그 값이 상태 스트림으로 나가 상대 클라의 `OppoPlayerController`가 `_anim.SetBool("IsShooting", _actionState == 1)`로 쓰므로, **거짓 발사 모션이 남의 화면에도 그대로 보이고 있었다.** 3번(사망 경로 세부 관측)에서 상대 화면을 봐야 하므로 그 전에 닫았다. **핵심은 "한 프로퍼티가 요구 방향이 반대인 두 소비자를 섬기고 있었다"는 것이다** — 게이트는 **느슨해야** 하고(`Fire()`에 도달해야 `EmptyAmmoFire()`가 살아 있다) 표현은 **엄격해야** 한다(실제로 나갈 때만 모션). 한 값으로 둘 다 만족시킬 수 없어 `IsFireInput`(쏘려는 의사, 게이트용)과 `IsShooting`(실제로 나가는 중, 모션·`ActionState`용)으로 쪼갰다. **탄약 조건을 게이트에 얹으면 `EmptyAmmoFire()`가 영영 안 불려 12번(빈 탄창 사운드·재장전 유도)이 통째로 죽는다** — 이게 이 작업에서 가장 밟기 쉬운 함정이다. 이름은 새 의미 쪽이 `IsShooting`을 가져갔다(애니메이터 파라미터 문자열이 `"IsShooting"`이라 그래야 읽힌다). **`OppoPlayerController`는 손대지 않았다** — 소스가 정확해지면서 상대 화면이 따라온다. 함께 맨손 발사를 닫았다: `EquipWeapon(0)`이 `_fireInterval`을 리셋하지 않아 직전 무기 값이 남고 **총도 없는데 발사 모션·탄약 차감·반동·`weapon_dbid=0` 패킷이 전부 나가고 있었다.** `_muzzlePointTr = null`과 같은 자리에서 비워 스펙 조회 실패까지 덮었고, **`ProcessFire()`의 조기 return은 없앴다** — 그대로 두면 맨손 동안 `_fireBlocked` 갱신과 스프레드 회복까지 멈추므로 발사 조건에서만 막는다. `ActionState` 의미가 엄격해지는 것의 서버 영향은 없다(오포 애니메이션용 브로드캐스트이고 발사 판정은 `C2DRequestWeaponFire`가 따로 한다). **런타임 검증 완료** — 모션 정지와 빈 탄창 로그 유지가 동시에 성립하는 것까지 확인했다(`EmptyAmmoFire()`가 살아 있다는 증거)
- [x] (2026-08-26 #3) 크로스헤어 UI — 에디터 자산 없이 코드로 세운 잠정 구현 (T19) — 사격 가능한 상황에서 화면 중앙에 십자를 띄우고 `_currentSpread`에 따라 벌어진 정도를 바꾼다. **원래 "씬 오브젝트 제작이 선행되는 에디터 작업"으로 잡았던 것을, (2026-08-26 #1)의 `BulletTracer`가 만든 선례를 따라 런타임 생성으로 돌렸다(사용자 판단)** — 자기 `Canvas`를 만들고 **스프라이트 없는 `Image` 4개**를 배치한다(스프라이트를 주지 않은 `Image`는 단색 사각형으로 그려져 자산이 필요 없다). 덕분에 0번 히트박스 튜닝이 씬 오브젝트 제작을 기다리지 않아도 된다. **이건 최종 형태가 아니다 — `IngameSceneUI`로 다시 만들 예정이고, 그때 통째로 대체된다**(사용자 확정). 그래서 **손댈 곳이 셋뿐이도록 일부러 좁혔다**: 파일 삭제 / `IngameScene.Init()`의 `Create(this)` 한 줄 / `PlayerController.CurrentSpread` 접근자(새 UI도 스프레드를 읽으므로 대체 시에는 남긴다). **`IngameScene`이 필드로 들고 있지 않은 것도 같은 이유** — 이 클래스가 씬을 참조해 스스로 갱신하므로 지울 때 씬 쪽에 남는 상태가 없다. 파일 상단에 전용 주석 블록으로 이 절차를 남겼고 `UI/CLAUDE.md`에는 "이 예외를 선례로 삼지 말 것"으로 적었다. **구현 판단 셋** — ⓐ **`CanvasScaler`를 붙이지 않는다**: 1 유닛 = 1 픽셀이어야 각도 → 픽셀 환산이 스케일러 설정에 휘둘리지 않는다(정식 자산으로 옮길 때 스케일러를 쓰면 **환산식을 캔버스 기준으로 다시 잡아야 한다**) ⓑ 막대의 pivot을 **안쪽 끝**에 둬서, 간격만 넣으면 중심에서 바깥으로 자라고 길이를 바꿔도 간격이 흔들리지 않는다 ⓒ 스프레드는 원뿔 **반각**이라 같은 각도라도 화각·해상도에 따라 화면 크기가 달라진다 — 화면 절반 높이 기준으로 환산한다. 숨김은 `IsAnyUIOpen`과 `IsInputLocked`(사망 연출이 카메라를 가져간다) 둘뿐이고 **달리는 중에는 숨기지 않는다**(발사는 막히지만 이동할 때마다 깜빡여 거슬린다). 씬 파괴 순서가 보장되지 않아 `Update()`에 씬 null 가드를 넣었다 — 같은 유형이 (2026-08-24 #2)에서 이미 한 번 나왔다. **런타임 검증 완료.** **그리고 이 작업이 곧바로 결함을 하나 드러냈다 — 스프레드가 시간 경과로 사실상 회복되지 않는다((2026-08-26 #4)에서 DB 수치 갱신으로 해소). 표시 수단이 없어 여태 보이지 않던 것이며, "관측 수단을 먼저 세우면 결함이 따라 나온다"가 (2026-08-25 #3) 이후 두 번째다**
- [x] (2026-08-26 #4) DB 마이그레이션 반영 확인 + `ItemDBHelper` 수정 금지 규칙 확립 — **이 세션이 코드를 쓴 작업이 아니다.** DB 쪽에서 무기 스펙 3종이 갱신되어(`SpreadBase` 50→40, `SpreadMax` 1000→800, **`SpreadRecoveryRate` 40→400**) 들어온 것을 추적하고 문서만 맞췄다. **직전에 우선순위 1번으로 올려뒀던 "스프레드 회복이 사실상 없다"가 코드 한 줄 없이 해소됐다** — 초당 0.4도(최대→기본 23.75초)에서 초당 4.0도(**1.9초**)가 됐고, 최대치도 10도→8도로 낮아져 도달점 자체가 줄었다. `ItemDBHelper` 외에 스프레드 값을 하드코딩한 곳이 `Assets/Scripts` 전체에 없어 값만으로 전부 따라온다. **가장 값진 것은 `ItemDBHelper.cs`의 성격이 확정된 것이다(사용자 확인)** — 이 파일은 **DB 자료를 긁어 생성되는 스크립트**이고 수치는 DB 쪽이 주도한다. 그래서 루트 `CLAUDE.md`의 '절대 규칙'에 **수정 금지**를 추가했다. `Gitignores.cs`와 달리 **읽기는 허용**으로 뒀는데, 스펙 조회가 상시 필요한 파일이라 읽기까지 막으면 작업이 서지 않기 때문이다. 금지 사유를 "덮여 사라진다"는 결과로 적은 것도 의도적이다 — 규칙만 적으면 다음 세션이 급할 때 넘어간다. **함께 서버 동기화 서술을 걷어냈다** — "`ItemDBHelper` 수치를 건드리면 서버 `ItemDataManager`와 함께 고쳐야 한다"는 `Scenes/CLAUDE.md`의 문장과 progress.md의 관련 메모를 지웠다. DB 주도인 이상 **클라 세션이 알 필요도 없고 행동할 수도 없는 정보**라 남겨두면 잘못된 행동(직접 수정)을 유도한다. **`MAX_HEALTH_POINT`의 동기화 서술은 남겼다 — 성격이 다르다**: 그건 DB가 아니라 클라가 직접 들고 있는 상수이고 서버가 최대치를 어떤 패킷으로도 보내지 않아 손으로 맞춰야 하는, **이 세션이 실제로 고칠 수 있는 값**이다. 둘을 한 덩어리로 묶어 지우지 말 것. **검증 완료** — 스프레드 재조정을 포함해 지금까지 작업한 내용이 전부 인게임 테스트에서 정상 동작하는 것이 확인됐다(사용자 확인). 연사 후 크로스헤어가 곧 기본 크기로 돌아오는 것까지 포함이며, 이걸로 직전까지 우선순위 1번이던 "스프레드 회복 재조정 결과 확인"이 닫혔다
- [x] (2026-08-26 #5) 인게임 설정 UI 신설 + 마우스 감도 실제 연결 + Tab/I 인벤토리 토글 (T23 · 구 17번 마우스 감도 · 구 13번 인벤토리 키바인딩 — 셋 다 목록에서 제거됐다) — **T23의 존재 이유가 구 17번을 여는 것이었으므로 둘을 한 번에 닫았다.** 감도가 `PlayerController`에 `1f`로 하드코딩돼 raw 픽셀 델타에 그대로 곱해지고 있었다 — **1픽셀 = 1도**라 세로 가동 범위 170도 전체가 마우스 170픽셀이었고, 조준이 성립하는 값이 아니었다. `MOUSE_SENSITIVITY_DEG_PER_PIXEL`(0.1)을 도입해 **실제 계수 = 슬라이더 값 × 0.1 도/픽셀**로 바꿨다. **설정에서 매 프레임 읽는다** — 캐시하면 창에서 바꾼 값이 다음 매치까지 반영되지 않는다. **프리팹이 없어도 이 변경은 지금 바로 체감된다** — `SettingManager` 기본값이 1.0이라 0.1 deg/px, 즉 **예전보다 10배 느려진 상태로 다음 플레이가 시작된다**(아래 우선순위 1번). **`LobbySettingUI` 재사용은 기각됐다(사용자 확정)** — 창모드·해상도·프레임·FOV처럼 **매치 중에 바꾸면 안 되는 값까지 담고 있어서**다(로비 씬을 필드로 직접 참조하는 것도 걸림돌이었다). 인게임에 담은 것은 **마우스 감도 + 볼륨 3종**뿐이고, 프리팹은 `LobbySettingWindow`를 복제해 그래픽 탭만 들어내는 형태라 경로를 로비와 동일하게 잡았다. **크로스헤어의 런타임 생성 예외를 두 번째로 쓰지 않았다** — `UI/CLAUDE.md`에 "선례로 삼지 말 것"이라 적어둔 것을 지켰고, 대신 `GameObject.Find` 기반 씬 내장 UI 규칙을 따른다(**씬에는 활성 상태로 저장해야 한다 — `Find`는 비활성 오브젝트를 못 찾는다**). **반영 정책을 로비와 일부러 갈랐다** — 여기는 슬라이더를 움직이는 즉시 `Managers.Setting`에 반영하고 취소하면 `Show()` 시점 스냅샷으로 되돌린다(볼륨은 귀로 들어야 맞출 수 있다). 그래서 **'적용'은 확정이 아니라 '이대로 닫기'**이고 재확인 팝업이 없다(인게임에 `LobbyReconfirmUI` 대응물이 없기도 하다). 되돌릴 때 **마스터 볼륨을 먼저 넣는 것**은 `SetMasterVolume()`이 효과·BGM을 다시 밀어내기 때문이고, 슬라이더 갱신을 전부 `SetValueWithoutNotify`로 하는 것은 그냥 넣으면 `onValueChanged`를 타고 **되돌리는 값이 다시 '변경'으로 재적용되기** 때문이다. **Tab·I는 둘 다 인벤토리 토글이고, 컨테이너 상호작용 중이면 그 종료가 우선이다(사용자 확정 규칙).** 호출자가 0개로 놀고 있던 `IngameInventoryUI.ActiveMyInventory()`가 그대로 쓰였다. ESC는 **가장 위에 있는 것부터 닫고**(설정 → 컨테이너 → 인벤토리) 닫을 것이 없을 때만 설정을 연다. **ESC는 `IsInputLocked`에서도 막지 않는다**(설정은 서버로 나가는 요청이 없다) 반면 **Tab·I는 막는다**(인벤토리 조작은 서버 요청으로 이어진다). **밟기 쉬운 함정 둘을 처리했다** — ⓐ **컨테이너와 내 인벤토리가 같은 오브젝트(`IngameInventoryUI`)를 레이아웃만 바꿔 쓴다.** 인벤토리를 연 채 컨테이너를 열면 `OnUIOpened`가 두 번인데 닫는 건 한 번이라 **`_uiOpenCount`가 1로 남아 그 판 내내 커서가 풀리고 총이 안 나간다.** `ShowOpenedContainer()`에서 인벤토리 열림을 반납하되 **`OnUIOpened()` 뒤에 둬서** 카운트가 0을 찍어 커서가 잠겼다 풀리는 왕복을 피했다 ⓑ **E키를 막아야 했다** — UI가 뜨면 커서가 풀려 시점이 멈추는데 직전 조준 대상은 그대로 남아, **창을 띄운 채 컨테이너가 열리거나 귀환이 요청된다.** `TryInteract()`에 `IsAnyUIOpen` 가드를 넣되 컨테이너 닫기 분기보다 **뒤에** 뒀다(E로 컨테이너 닫기는 유지). 함께 **크로스헤어를 `TEMP:` → `OPTION:`으로 내렸다** — 런타임 검증에서 사용감이 나쁘지 않아 이대로 유지하기로 확정됐고(사용자 판단), 정식 `IngameSceneUI` 자산으로 다시 만드는 것은 상시 보류 가능한 개선 항목이 됐다. **프리팹 대기라 설정 창 자체는 런타임 미검증**(아래 우선순위 2번) — 감도 변경과 Tab/I 토글은 프리팹 없이도 동작한다

---

## 다음 작업 우선순위 (제안)

> **서버 변경분(2026-08-12 이후) 반영 작업은 별도 문서로 분리** — `server-sync-todo.md` (T1~T17) 참조.
> proto의 `[작업사항]` 주석을 추적한 임시 문서이며, 전 항목 반영 후 이 파일로 이관하고 삭제할 것.
> 아래 목록과의 대응: 6번(결과 표시 UI) = T15의 남은 표시부, 9번(워치독 제거 검토) = 6번에 종속
>
> **T번호 공간은 `server-sync-todo.md`와 공유한다** — 그쪽이 T1~T18을 쓰고 있어 신규는 T19부터다.
> **T19~T23은 전부 완료됐다**(T19 크로스헤어 / T20·T21 총알 궤적 / T22 빈 탄창 발사 모션 / T23 인게임 옵션 UI). 신규 클라 작업은 T24부터 쓸 것.

0. **히트박스 반지름 비율 튜닝 (2인 접속) — 막고 있던 것은 전부 해소됐다** — Project Settings > Physics > **`Queries Hit Triggers`는 켜져 있는 것으로 확인**됐고(새 레이어 + 충돌 매트릭스 대안은 필요 없다), 2인 접속에서 **데미지가 실제로 오가 (2026-08-25 #4)의 코드 히트박스가 동작하는 것이 실증**됐다. **남은 것은 반지름 비율 튜닝뿐이다** — 적을 선택하면 Scene 뷰에 캡슐 기즈모가 보이므로 눈으로 맞춘다(`HITBOX_DEFS.RadiusFactor`, 절대값이 아니라 골반→머리 대비 비율). 함께 볼 것: 자기 무기·자기 몸에 자탄이 안 걸리는지 — 안 걸리면 `Fire()`의 자기 제외 `TODO:`를 새 레이어 없이 닫을 수 있다. **3번과 같은 판에서 처리할 것.** 도구는 갖춰졌다 — (2026-08-26 #1)의 총알 궤적으로 탄도가 게임 화면에 보이고, (2026-08-26 #3)의 크로스헤어로 조준 기준선과 현재 스프레드가 함께 보인다. **막던 것이 이제 없다** — 스프레드 회복이 24초에서 1.9초로 바뀌어 연사 후에도 탄이 곧 조준점으로 돌아오고((2026-08-26 #4)에서 검증됐다), **감도가 1픽셀=1도에서 벗어나 조준 자체가 통제 가능해졌다**((2026-08-26 #5), 아래 1번). 이전에는 히트박스가 좁은 건지 손이 튄 건지가 갈리지 않았다
1. **마우스 감도 기본값 확정 (혼자 가능, 0번의 선행)** — (2026-08-26 #5)에서 감도가 **슬라이더 값 × 0.1 도/픽셀**로 바뀌었다. `SettingManager` 기본값이 1.0이므로 **다음 플레이는 예전보다 10배 느린 0.1 deg/px로 시작한다** — 세로 가동 범위 170도가 마우스 170픽셀에서 1700픽셀이 된다. **프리팹 없이도 이미 적용된 상태다.** 볼 것: ① 0.1 deg/px가 과하게 느리지 않은지 ② 반동을 손으로 잡는 설계인 만큼 **연사 중 총구를 끌어내리는 조작이 되는 감도인지**(이게 이 값의 진짜 기준이다) ③ 슬라이더로 조절하려면 아래 2번이 선행. 안 맞으면 고칠 곳은 `PlayerController.MOUSE_SENSITIVITY_DEG_PER_PIXEL` 하나이고, 슬라이더 범위(`Min 0.1 / Max 3.0` 권장)와 함께 본다. **체감으로 수렴시켜야 하는 값이라 상수만 바꿔서는 안 맞는다** — 2번이 붙으면 그때 확정
2. **인게임 설정 창 런타임 검증 (혼자 가능) — 코드·프리팹 모두 준비됐다** — **프리팹은 이미 만들어졌다** — `Resources/Prefabs/UI/IngameSceneUI/IngameSettingUI.prefab`이며 `TestIngameScene.unity`도 함께 수정됐다(배치까지 된 것으로 보인다). **남은 것은 런타임 검증뿐이다**(메모의 '인게임 설정 창 검증'). 아래 규격은 검증에서 어긋났을 때 대조용으로 남긴다. (2026-08-26 #5)에서 클래스·키 배선·감도 연결이 전부 끝났고 **프리팹만 없어 설정 창이 열리지 않는다.** `LobbySettingWindow`를 복제해 **그래픽 탭(`Tab_Graphic` + `GraphicContent`)을 들어내고** 루트를 `IngameSettingUI` / 창을 `IngameSettingWindow`로 renaming하면 나머지 경로는 로비와 동일하다. 정확한 계층 규격은 `UI/CLAUDE.md`의 '`IngameSettingUI`' 절에 있다. **반드시 지킬 것** — ⓐ 루트를 **씬에 활성 상태로 저장**할 것(`GameObject.Find`는 비활성 오브젝트를 못 찾는다. `Init()`이 바인딩 직후 끈다) ⓑ 다른 인게임 UI와 같은 Canvas 아래, **형제 순서 마지막**(인벤토리 위에 떠야 한다) ⓒ **감도 슬라이더 Min 0.1 / Max 3.0 / Value 1.0** — 실제 도/픽셀은 이 값 × 0.1이다. 경로가 어긋나도 터지지는 않는다(`Util.BindComponent`가 `LogError` 후 그 항목만 죽는다). **맵 씬이 늘어나면 씬마다 배치해야 한다**(기존 인게임 내장 UI와 같은 제약, 아래 14번)
3. **사망 경로 세부 관측 (2인 접속) — 골격은 통과했다** — 2인 접속으로 **데미지 주고받기 · 사망 후 로비 복귀 · 귀환 후 로비 복귀가 전부 동작**하는 것이 확인됐다. T18의 사망 경로가 실제로 돌았다는 뜻이라 "사망 경로 통째로 미검증"은 해소됐다. **남은 것은 개별 관측 항목이고, 2인을 붙이는 비용이 가장 비싸므로 한 판에 몰아서 볼 것**: ① 진입 직후 HP·실드 만피 → 방어구 해제 시 0 (T11·T12, (2026-08-25 #1) 반영분) ② 1/2 키 전환이 한 박자 뒤에 반영 / 전환 대기 중 클릭이 안 나감 / 주무기 해제 시 보조무기가 손에 (T9·T9-a) ③ 상대 화면에서 내 손의 무기가 바뀌는지 (T8) ④ 피격 시 게이지가 서버값으로 점프 ⑤ 처치 시 킬러 화면 `[KillFeed]` (T10) ⑥ 피해자 화면 탑뷰 연출 + 4초 조작 차단 + **상대는 계속 움직이는지**(관전 유지) ⑦ 킬러 쪽 `킬수 — 플레이어: 1`. **⑤가 안 나오면 ③을 볼 것** — `weapon_dbid`가 어긋나면 사격이 통째로 조용히 무시된다(히트박스는 위 0번에서 실증됐으므로 후보에서 빠졌다). 정상인데 오해하기 쉬운 것 둘 — `오브젝트: 0`(T14 대기)과 시신 컨테이너의 `매핑되지 않은 objectType=3` 에러(T13 대기). **(2026-08-25 #5)로 탄약 제약이 풀렸다** — 최대 HP 200.00에서 맨몸 5발 / 경량 조끼 7발이라 탄창 30발 안에서 여유 있게 처치되고, 처치·킬 피드·킬 카운트를 무방어 상태로 몰아둘 필요가 없다. 탄약 자체 검증(차감·빈 탄창 로그)은 2인이 필요 없으므로 혼자 먼저 끝내두면 이 판이 깔끔해진다
4. **서버 변경분 반영 (`server-sync-todo.md`)** — T1~T11 완료(핸들러는 41번까지 전부 등록 완료 — proto의 `[작업사항]` 중 패킷 배선이 필요한 항목은 남은 것이 없다), T10은 데이터 경로가 끝나고 **UI 프리팹 대기**, T12는 **런타임 실측만 남음**, **T15·T18은 귀환 경로 한정으로 런타임까지 검증됐고**((2026-08-25 귀환 경로 첫 런타임 검증)) 표시 UI와 **사망 경로 검증**(위 3번)이 남았다. 남은 것은 T16·T17. **T13은 시신 프리팹 제작 대기(아래 5번), T14는 서버 목록 대기**. 전 항목 반영이 끝나면 proto의 `[작업사항]` 블록을 일괄 삭제할 것(proto 11~12행 지시) — 그 전에 지우면 잔여 작업 추적 근거가 사라진다
5. **시신(Corpse) 오브젝트 매핑 — 지금 새고 있는 결함** — `object_type=3`(Corpse)이 `Define.ObjectPaths`에 없어, 플레이어가 죽어 시신 컨테이너가 `D2CNotifySpawnObject`로 와도 `SpawnObject()`가 "매핑되지 않은 objectType=3" 에러만 남기고 반환한다. **시신이 보이지도 털리지도 않는다.** `Define.ObjectType`에 `Corpse`를 **`MaxCount` 앞에** 넣으면 값이 자동으로 3이 되며(현재 `MaxCount`가 3을 차지), `ObjectType.MaxCount`는 코드 어디서도 쓰이지 않는 것을 확인했으므로 4로 밀려도 안전하다. 컨테이너 조작은 일반 컨테이너와 동일하니 **프리팹만 생기면 코드는 두 줄**이다
6. **게임 결과 표시 UI** — 흐름(전환·엔터 복귀·세션 유지)은 끝났고 `GameResultScene.LogGameResult()`가 `Util.Log`로만 뿌린다. 데이터는 `Managers.Scene.LastGameResult`(이탈 사유 + 인벤토리 25슬롯 + 무기 2·방어구 + 킬수 2종)이며, **사유별 의미 차이를 표시할 것** — Recalled=반출 확정, Dead·ConnectionLost=잃은 것. 프리팹은 `Assets/Resources/Prefabs/UI/GameResultSceneUI/`에 자리가 잡혀 있고 `UI/CLAUDE.md` 규칙을 따른다. **전환은 반드시 `CompleteMatchExit()`을 거칠 것** — 우회하는 강제 전환은 결과가 저장되지 않는다. 탈출(귀환) 연출은 여전히 `BeginMatchExit()`에 자리만 비어 있다
7. **귀환 스팟 씬 배치** — 맵 씬에 귀환 단말기 오브젝트 배치, `RecallSpotController` 부착 후 인스펙터에서 `_recallSpotIndex`를 서버 테이블 값에 맞춤. 조준 레이가 맞아야 하므로 트리거가 아닌 일반 콜라이더 사용
8. **귀환 진행 중 UI 피드백** — 승인~결과 사이 5초 구간 표시(카운트다운 등). `InteractText`는 매 프레임 재조회되므로 `virtual` 프로퍼티화하면 동적 텍스트 전환 가능
9. **TEMP 워치독 2건 제거 검토** — 귀환(`RECALL_TIMEOUT`/`_recallTimer`)과 무기 교체(`SWITCH_WEAPON_TIMEOUT`/`_switchWeaponTimer`). 서버 통지 신뢰성이 실측으로 확인되면 제거. **귀환 쪽은 실처리가 붙었으므로 이제 검토 가능**하고, 무기 교체 쪽은 통보 유실 시 발사가 영구 차단되는 것을 막는 안전장치다
10. **킬 피드 UI 프리팹 준비 → 표시부 연결** — 데이터 경로는 완료(T10). `HandlePlayerKilled()`의 `TODO:` 자리에서 `Util.Log`를 표시부로 교체하면 되며, **내 사망 줄은 `HandleHealthChange` 쪽 경로라 함께 봐야 한다**
11. **발사 이펙트 프리팹 준비 → 이펙트 구현** — 로컬 탄착 이펙트(`ProcessHit`), 수신 측 머즐 플래시/총성/탄착 이펙트(`HandleWeaponFireBroadcast`). 파티클·사운드 에셋이 `Resources/Prefabs/` 아래에 필요. **이펙트 기준점은 `_shotPoint`가 아니라 총구다** — (2026-08-25 #3)에서 `_shotPoint`(가슴팍)는 피격 판정 전용으로 확정했고 시각 표현은 별도 기준을 쓰기로 했다. 판정선과 이펙트선이 어긋나는 것은 의도된 것이므로 맞추려 들지 말 것. **총구 기준점(`MuzzlePoint`) 추적은 (2026-08-26 #1)에서 이미 끝났다** — `GameObjectController.FindMuzzlePoint()`와 `PlayerController._muzzlePointTr`을 그대로 쓸 것. 여기서 따로 만들지 말 것
12. **재장전(탄창 교체) 구현 — (2026-08-25 #2)에서 드러난 부재** — 탄약 소모가 살아나면서 **탄창이 비면 그 판 내내 못 쏘는 상태**가 됐다. 조사 결과 재장전 수단이 클라·서버 어디에도 없다: ① `Assets/Scripts` 전체에 총기 재장전 코드 0건(`Reload()`는 전부 로그인·시작 UI) ② proto에 탄약·재장전 메시지 0건 ③ **탄창을 갈아끼울 클라 경로가 없다** — 장비 슬롯은 `0=주무기/1=보조무기/2=방어구`뿐이고(`IngameInventory.GetEquipmentSlot`), `SetPrimaryWeaponMagazine`/`SetSecondaryWeaponMagazine`은 **호출자 0개**라 탄창은 `D2CFullInventorySync`로만 채워진다. **선행 결정 2개** — 장전을 서버 요청으로 할지(새 패킷 필요) 인벤토리 장착 조작에 탄창 슬롯을 추가하는 형태로 할지, 그리고 장전 시점을 탄약 동기화의 확정 지점으로 삼을지(정책상 그렇게 읽힌다). **시급성은 (2026-08-25 #5)로 크게 낮아졌다** — HP가 100,000이던 시절엔 `BaseDamage` 4,800·탄창 30발 기준 무방어 21발 / 방어구(실드 10,000 + 감쇠 50%) 착용 40발 이상이라 **한 탄창으로 킬이 불가능했다.** HP 20,000(200.00)에서는 맨몸 5발 / 경량 조끼 7발이라 한 탄창에 여러 명을 잡는다. **지금 남은 문제는 "못 죽인다"가 아니라 "탄창이 비면 그 판 내내 못 쏜다"뿐이다** — 전투 검증을 막지는 않으므로 우선순위를 끌어올릴 이유는 없다
13. **EmptyAmmoFire() 구현** — 빈 탄창 사운드, 재장전 유도 UI. 현재는 `TEMP:` 로그 1줄뿐이며 **12번이 선행되어야 '재장전 유도'가 의미를 갖는다**
14. **실제 맵 씬에서 IngameScene 상속 완성** — `IngameScene`을 상속하는 맵별 씬 컴포넌트 구현. **씬 내장 UI 5종(`IngameInventoryUI`·`IngameDragGhost`·`InteractUI`·`IngameHealthBarUI`·`IngameSettingUI`)을 맵 씬마다 배치해야 한다** — `GameObject.Find()` 기반이라 없으면 그 기능만 조용히 죽는다(크로스헤어만 코드가 세우므로 예외)
15. **설정값 실제 적용 + 저장** — ① 해상도/창모드/FOV 변경이 `Screen.SetResolution()`, `Camera.fieldOfView` 등에 반영되도록 구현(현재 `SettingManager`가 실제로 적용하는 것은 `ApplyFrameRate()`와 볼륨뿐이다) ② **영속화가 아예 없다** — `SettingManager` 필드가 메모리에만 있어 게임을 끄면 전부 기본값으로 돌아간다. (2026-08-26 #5)로 감도가 인게임에 실제로 먹기 시작하면서 **매 실행마다 감도를 다시 맞춰야 하는 상태**가 됐다. `PlayerPrefs` 저장·로드를 `SettingManager.Init()`/setter에 붙이면 되고, 로비·인게임 양쪽 설정 UI가 같은 매니저를 쓰므로 **한 곳만 고치면 둘 다 따라온다**

---

## 메모

### 확인 필요
- **(2026-08-26) UDP 패킷 크기 상한 — 결함으로 재해석하지 말 것** — `UDPManager._recvBuf`가 **1500바이트 고정**이고, Windows에서 데이터그램이 버퍼보다 크면 `Socket.Receive`가 `SocketException`(WSAEMSGSIZE)을 던지는데 `NetworkLoopSync`의 catch가 `break`라 **워커 스레드가 영구히 죽는다.** 수신·송신 겸용 스레드라 10초 뒤 워치독이 `ConnectionLost`를 태우고 인벤토리까지 잃는다. **그런데 이 경로는 도달하지 않는다** — 서버 개별 패킷은 **1100바이트 이하**, 클라 송신은 **1KB 이하**인 것이 확인됐다(2026-08-26, 사용자 확인). 헤더 35바이트를 빼면 payload 상한이 1465라 여유가 충분하다. **사용자 지시: 이 건으로 코드를 고칠 필요 없다.** 같은 이유로 `PacketHandler.BuildPacketInto`에 송신 길이 검사가 없는 것(`MAX_PACKET_SIZE` 1400, payload 상한 1365)도 지금은 밟히지 않는다. **전제가 깨지는 조건은 하나다** — `D2CSpawnPlayerObjects`·`D2CUpdatePlayerStates`는 룸 전원을 한 패킷에 싣는데 `index`/`is_last` 분할 필드가 없다(blueprint 계열은 있다). 플레이어당 40~60바이트라 **정원이 25~30명대가 되면 1100 상한이 유지되지 않는다.** 정원을 늘릴 때 이 항목을 다시 볼 것
- **(2026-08-26) 인게임 설정 창 검증 (혼자 가능, 위 우선순위 2번의 프리팹이 선행)** — 볼 것: ① ESC로 열리고 커서가 풀리는지, 닫으면 다시 잠기는지 ② 창이 열린 동안 **시점이 안 돌고 총이 안 나가는지**(`_uiOpenCount` 배선 확인) ③ 볼륨 슬라이더가 **드래그하는 즉시 소리에 반영되는지**, 취소하면 원래대로 돌아오는지 ④ 감도를 바꾸고 닫으면 시점 속도가 바뀌는지 ⑤ **인벤토리를 연 채 컨테이너를 열었다 닫았을 때 커서가 정상 복귀하는지**(같은 오브젝트를 공유해 `_uiOpenCount`가 새기 쉬운 자리다) ⑥ 창이 떠 있을 때 E키로 컨테이너·귀환이 눌리지 않는지 ⑦ Tab·I가 둘 다 토글이고, 컨테이너를 뒤지는 중에는 컨테이너 닫기로 동작하는지. **에디터 한정 함정** — 에디터에서 ESC는 Unity 자체의 커서 잠금 해제를 겸하므로 ①이 어긋나 보일 수 있다. 빌드에서는 정상이며, 에디터에서는 Apply 버튼으로 닫아 비교할 것
- **(2026-08-25) 발사선 변경 검증 (혼자 가능, 시각화로만)** — (2026-08-25 #3)의 `TEMP:` `DrawFireRayDebug`가 붙어 있다. **에디터 플레이 모드에서 Scene 탭으로 전환**하면 보이고(Game 탭은 상단 Gizmos 토글 필요, **빌드에서는 안 보인다**) 잔상은 2분이다. 표시는 흰 십자=발사 원점(`_shotPoint`)·청록 십자=조준점(`_aimTarget`)·파랑=카메라 중심축 20m·발사선(초록=유효 대상/노랑=대상 아님/빨강=미명중). 볼 것: ① **흰 십자가 가슴팍에 찍히는지** — 카메라 위치에 찍히면 `_shotPoint`가 null이라 폴백된 것이고 `Setup()`에서 에러 로그가 함께 뜬다 ② 흰 십자에서 나간 선이 **청록 십자를 지나는지**(수렴 성립) ③ 파랑 선과 발사선이 근거리에서 벌어졌다가 조준점에서 만나는지 — **계속 평행하면 `MIN_CONVERGE_DIST`(0.5m) 폴백에 걸린 것**. **2인 접속에서 데미지가 실제로 오갔으므로 유효 대상 판정(초록)은 이미 실증됐다** — 이 시각화로 남은 확인은 ①~③(원점·수렴)뿐이다. 회수 조건은 코드 주석에 '발사 원점·수렴 검증 후 제거'다. **"총알 궤적(T20)이 들어가면 회수한다"고 적었던 것을 정정한다 — 대체되지 않는다.** 궤적은 `MuzzlePoint`에서 출발하므로 `_shotPoint` 원점과 조준점 수렴을 **아예 그리지 않는다**. 두 선을 나란히 보면 총구선과 판정선의 차이를 눈으로 확인할 수 있어 지금은 오히려 유용하며, 회수는 위 0번 튜닝이 끝난 뒤로 미룬다
- **(2026-08-25) 세션 유지 — `Success` 경로만 검증됐다** — (2026-08-25 귀환 경로 첫 런타임 검증)에서 정상 경로는 확인됐고 **실패 분기 둘이 남았다. 이 분기가 (2026-08-24 #0) 판단의 핵심이었는데 정작 미검증이다.** ① 서버 중지 상태로 복귀 → `Unreachable`, 재시도/취소 팝업이 뜨고 취소 시 추가 팝업 없이 TestStart 플로우로 합류하는지 ② Redis 세션 삭제 후 복귀 → `Expired`, **폴백이 먼저 수행되고 안내가 나중에** 뜨는지(`LobbyReconfirmUI.isActive` 때문에 순서가 뒤집히면 안내가 조용히 삼켜진다) ③ 게스트 로그인으로 같은 루프 → Header가 Guest로 복귀하는지 ④ **일반 최초 실행 경로(게임 시작 → 로비) 회귀 확인**
- **(2026-08-25) 로비 복귀 구간은 정리됐다 — 종료 경로만 남았다** — 잠복 결함이 세 번 연속 이 경로에서 나왔었고(씬 이름 불일치, 입력 디스패치 중 컬렉션 변경, 종료 시 `Managers` null) 셋 다 "로비로 돌아가는 경로가 없어 여태 실행되지 않던 코드"였다. (2026-08-25 귀환 경로 첫 런타임 검증)의 **재매칭까지 통과해 이 구간의 회귀 위험은 걷혔다.** 남은 것은 **로그아웃 → ESC 종료** 하나 — (2026-08-24 #2)의 `OnDestroy` null 가드가 아직 실행된 적이 없다(2인 없이 혼자 가능)
- **(2026-08-24) `The referenced script (Unknown) on this Behaviour is missing!` 경고** — 도메인 리로드 직후 뜬다. 현재 열려 있는 씬의 오브젝트에 스크립트가 빠진 컴포넌트가 있다는 뜻이며, 아래 `Poolable` 항목과 증상이 일치한다. 새로 만든 `GameResultScene` 프리팹 쪽일 가능성도 있어 단정은 못 한 상태다(씬·프리팹 파일 확인 필요)
- **(2026-08-24) resume 경로의 로비는 재화·인벤토리가 비어 보인다 (버그 아님)** — 서버 `/api/session/resume` 핸들러가 아직 뼈대라 `money: 0, inventory: []`를 반환한다. 서버 세부 구현(`LinuxServerTest/` 세션 담당)이 끝나기 전까지는 정상이므로 클라 파싱 버그로 오해하지 말 것. **스펙 파일(`http-api-spec.yaml`)은 서버 사본과 동기화되어 있으므로 한쪽만 수정하지 말 것**
- **(2026-08-21) 탑뷰 연출에서 시신이 보이는지** — 컬링 마스크를 원본 카메라에서 복사하므로, **1인칭에서 자기 몸을 숨기려고 레이어를 빼뒀다면 탑뷰에서도 시신이 안 보인다.** 그 경우 마스크에 해당 레이어를 더하는 한 줄이면 된다. URP 후처리·전용 렌더러를 원본 카메라에 지정해뒀다면 그것도 연출 카메라에는 적용되지 않는다(`UniversalAdditionalCameraData`는 복사하지 않음)
- **(2026-08-26 갱신) T18 검증 — 사망 경로도 한 바퀴 돌았다** — 4초 유예·결과 씬 전환·`Clear()` 정리는 (2026-08-25 귀환 경로 첫 런타임 검증)으로, **사망 → 결과 씬 → 로비 복귀는 2인 접속 전투로** 확인됐다. 사망·귀환 두 출구가 모두 실증됐으므로 `BeginMatchExit`의 출구 공유 구조는 닫힌 것으로 본다. 남은 것은 **연출·표시 쪽 세부 관측**(탑뷰 연출이 실제로 보이는지, 유예 중 상대가 계속 움직이는지)이며 위 우선순위 3번에 통합돼 있다
- **(2026-08-26 갱신) 탄약 소모 검증 — ②는 닫혔다** — (2026-08-26 #2) 검증에서 빈 탄창 로그가 클릭 1회당 1줄 나오는 것을 확인했다. 남은 항목만 볼 것 — ① 탄약이 있을 때 발사하면 **인벤토리를 열어** 탄창 수량이 쏜 발수만큼 줄었는지(상시 탄약 HUD가 없어 `IngameISlot`의 수량 텍스트로만 보이고, **UI를 열면 발사가 막혀 쏘면서 동시에 볼 수 없다**) ② 탄창을 다 비운 뒤 클릭하면 `[Fire] 탄약이 없어 발사하지 않는다`가 **클릭 1회당 1줄** 나오는지 ③ 그 상태에서 발사 패킷이 안 나가는지는 직접 관측 수단이 없으나, `EmptyAmmoFire()` 직후 `return`이라 `ProcessHit()`에 도달할 수 없으므로 **로그 출력이 곧 미전송의 증거**다 ④ 명중 시 `[Fire] 피격 대상 objectId=N`(`TEMP:`)이 상대 objectId로 찍히는지
- **(2026-08-25) 씬 이름 컨벤션 잔여** — `TestIngame2.unity`도 컨벤션대로면 `TestIngame2Scene.unity`이지만 `Define.Scene` enum에 없어 코드 영향이 없고, `WinchesterAlpha.unity`는 맵 에셋 성격이라 **둘 다 판단 보류**. enum에 올리는 순간 파일 이름을 맞춰야 한다(`Scenes/CLAUDE.md`의 씬 이름 규칙)
- **(2026-08-21) T10 검증** — 2인 접속. A가 B를 죽였을 때 **A 화면**에 `[KillFeed] 나 objectId=A(weaponId=N) → objectId=B(...)`, **B 화면**에는 이 패킷이 오지 않으므로 `[HealthChange]` 뒤에 내 사망 줄이 찍혀야 한다. 제3자 화면에는 양쪽 objectId와 킬러 무기. `weaponId`가 0이나 `미스폰`으로 나오면 T8의 무기 추적부터 볼 것
- **(2026-08-21) T9 컴파일 + 검증** — ① 1/2 전환이 **한 박자 뒤에** 반영되는지(예측 없음이라 정상) ② **주무기 해제 시 보조무기가 손에 들리는지**(T9-a. 서버와 어긋나면 사격이 통째로 무시된다) ③ 전환 대기 중 클릭이 발사되지 않는지 ④ 빈 슬롯 키에 패킷이 안 나가는지 ⑤ 버전 불일치 거부는 자연 재현이 어려우니 낡은 버전을 싣는 `TEMP:` 코드로 재동기화 경로를 한 번
- **(2026-08-21) T8 컴파일 + 2인 접속 검증** — ① A가 무기를 장착하면 B 화면에서 A 손에 무기가 생기는지(경로 1, 이번 작업의 실질 검증) ② A가 해제하면 B 화면 맨손 + **A 본인 화면도 맨손인지**(해제 버그 수정 확인) ③ 미스폰 플레이어가 있을 때 `C2DRequestSpawnByObjectId`가 **한 번만** 나가는지. 본인 롤백(경로 3)은 `C2DRequestSwitchWeapon`을 보내야 발생하므로 T9 전까지 검증 불가
- **(2026-08-25 갱신) 체력/실드 게이지 런타임 실측 — HP 쪽은 닫혔고 실드가 남았다** — **(2026-08-25 #5)에서 스폰 직후 HP 만충 표시와 피격 수(맨몸 5발 / 경량 조끼 7발)를 확인**했으므로 HP 최대치 반영과 `SyncHealthBarMax()`의 최대치 → 현재값 순서는 실증된 것으로 본다. 남은 것은 **실드 쪽 기대값**이다. **(2026-08-25 #1)로 기대값이 바뀌었다** — 순서: 매치 진입 직후 HP 만피 + **실드도 만피**(`[ShieldInit] 스폰 실드 최대치 적용 — N` 로그와 대조. 방어구 없이 스폰하면 `N=0`이 정상) → 방어구 **해제** 시 0 → 재착용 후 초당 100(=1%) 상승 → 피격 시 서버값으로 점프(`[HealthChange]` 로그와 대조). 통과하면 T11·T12를 함께 종료 처리한다. **어긋나면 원인은 이제 스크립트 쪽이다** — Editor 설정이 유일한 잔여 변수였으므로 남는 후보는 `SyncHealthBarMax()` 호출 시점과 실드 예측 누적 로직
- **(2026-08-23) `ObjectKillCount`는 `ICombatTarget` 보급 전까지 항상 0이다** — 패킷 배선(41)은 끝났지만, 전투 오브젝트가 `ICombatTarget`을 구현하지 않으면 `PlayerController`가 `hit_object_id`에 `0xFFFFFFFF`를 실어 보내 서버가 데미지를 넣지 않는다. **41번 검증은 아래 T14가 풀린 뒤에야 가능하다** — 그 전에 킬수가 0인 것은 정상이므로 핸들러 버그로 오해하지 말 것
- **(2026-08-23) 오브젝트 처치 검증** — T14 해소 후 2인 접속. 전투 오브젝트를 파괴했을 때 ① `[ObjectKill] 나 objectId=A(...) → objectId=N` 로그 ② **그 오브젝트가 화면에서 사라지는지**(41이 제거 신호를 겸하므로, 남아 있으면 `HandleObjectKilled`의 `DespawnObject()` 경로부터 볼 것) ③ 결과에 킬수가 잡히는지. **39번(`D2CNotifyDespawnObject`)은 현재 서버가 보내지 않으므로 그쪽 로그를 기다리지 말 것**
- **(2026-08-20) T14용 HP 보유 비플레이어 오브젝트 목록** — 서버 답변 대기 중. 이게 와야 `ICombatTarget` 구현 대상이 정해진다. **위 두 항목의 선행 조건이다**
- **(2026-08-20) `Poolable` 삭제 후 Unity 콘솔 확인** — MonoBehaviour라 프리팹·씬 오브젝트에 수동으로 붙어 있었다면 "Missing (Mono Script)" 경고가 뜬다. 코드가 붙이는 경로는 없었으므로 가능성은 낮다

### 발사-데미지 경로 진단 (2026-08-25 #3 실측 — 재현 비용이 크므로 수치를 남긴다)

- **관문별 카운터 실측(30발 연사 1회)**: `intent=265`·`blocked=88`·`notShooting=0/0/0/0` / `cooldownPass=31`·`ammoFail=1` / `ray=30`·`hit=30`·`miss=0` / `targetNull=30`·`self=0`·`valid=0`·`sent=30` / 비대상 명중 분포 `Plane=30`. **입력·차단·쿨다운·탄약·송신 전 구간이 무결했다** — 같은 증상이 재발하면 이 표와 대조할 것
- **`ammoFail=1`은 정상이다** — 탄창 30발(`MaxAmmo=30`)을 전탄 소모하고 31번째 시도에서 빈 것. `cooldownPass=31`도 같은 이유다
- **프레임 기아는 없었다** — 실 발사창 177프레임(265−88)에 31발이면 약 69fps, RPM 700(무기 id=2)과 맞는다. 저프레임에서 `_fireTimer`가 `Mathf.Min`으로 캡되어 발사가 누락되는 경로는 이번 건과 무관
- **적 콜라이더 실측**: `dist=1.20m`·`angle=9.3deg`·`layer=Default`·`collider=2/3(활성/전체)`인데 **`RaycastAll`이 경로에서 `Plane` 하나만 반환**했다. 두 번째 표본도 동일(`1.17m`·`12.9deg`·`Plane` 1건)
- **레이 높이 역산**: 눈높이 0.89m, 바닥 `y=0`, 적 흉부 `+0.58m`. 적 거리에서 레이 높이가 0.398m / 0.337m로 **흉부보다 18~24cm 아래**였다. `angle`을 순수 수직 편차로 역산하면 8.6°/11.7°로 실측 9.3°/12.9°와 거의 일치 — **편차는 수평이 아니라 거의 전부 수직이다**
- **초근접에서는 조준점이 몸통 하부에 걸리는 것이 정상이다** — 1.2m·FOV 80°에서 적은 머리 5°아래 ~ 발 36.6°아래를 차지한다. "화면 중앙에 적을 뒀는데 흉부 기준 9° 벗어남"이 동시에 참일 수 있으므로 **`angle` 값만 보고 조준 실수로 판단하지 말 것**
- **카메라와 레이는 어긋나 있지 않았다** — `dir ≈ camFwd`, `camEuler.x == viewPointLocal.x`. 렌더링 카메라(`Pcam`)는 `Camera.main`이 아니지만(`isMain=False`) 발사선 산출과는 무관

### 콜라이더 분포 실측 (2026-08-25 #4 — 프리팹 전수 확인)

| 프리팹 | 콜라이더 |
|--------|----------|
| `OppoPlayerObject` | **0개** (루트 + `Aim`뿐) |
| `HB0/1/2OppoPlayer`, `HB0/1/2Player` | **0개** (6종 전부) |
| `PlayerObject` | `CharacterController` — Height 0.81 / Radius 0.15 / Center (0, 0.42, 0) |
| `Weapon_1_AK` | MeshCollider 4개 (활성 3) |
| `Weapon_2_M4`, `Weapon_3_M16` | MeshCollider 3개 (활성 2) |

- **캐릭터가 작다** — `CharacterController`가 y 0.015~0.825를 덮고 실측 눈높이가 0.89m다. 사람 크기(1.8m)를 가정한 수치를 쓰면 전부 어긋난다. 히트박스 반지름을 비율로 잡은 이유이기도 하다
- **레이어는 `Default`/`TransparentFX`/`Ignore Raycast`/`Water`/`UI`/`ClickableUI`뿐이다.** 커스텀은 `ClickableUI` 하나이며 **`Assets/Scripts` 전체에 `LayerMask`·`gameObject.layer`·`isTrigger` 코드가 0건**이다(히트박스의 `isTrigger` 제외). 레이어 기반 해법을 쓰려면 신설부터 해야 한다
- 무기 MeshCollider는 **비볼록(`m_Convex: 0`)이고 트리거가 아니다.** 애니메이션으로 움직이는 본에 붙은 채 켜져 있으면 물리·성능 양쪽으로 나쁘다 — 장착 시 끄는 이유 중 하나

### 반동 설계 확정 사항 (2026-08-26 확정 — 이전 기록의 오진을 정정한다)

- **반동은 시간 경과로 회복시키지 않는다. 플레이어가 마우스를 직접 내려 잡는다.** 자동 복귀를 넣을 계획이 없다
- **따라서 `_recoilTarget`에 감쇠 경로가 없는 것은 결함이 아니라 설계다.** (2026-08-25 #3) 계측 당시 "30발에 47도가 누적되고 사격을 멈춰도 돌아오지 않는다"를 결함으로 등록했었으나 **오진이었다.** 감쇠를 추가하는 방향으로 "고치지" 말 것
- **누적이 무한히 커져도 가동 범위는 보존된다** — `ProcessMouseLook()`의 `_aimPitch` 클램프가 `[-80 + _recoilPitch, 90 + _recoilPitch]`로 반동만큼 같이 밀리므로, `ApplyViewRotation()`의 `finalPitch = _aimPitch - _recoilPitch`가 여전히 −80~90 전체를 덮는다. 한 매치(최대 15분) 동안 수천 도가 쌓여도 float32 유효자리 안이라 실무상 정밀도 문제도 없다. **이 클램프에서 `_recoilPitch` 항을 빼면 그때 실제로 조작 범위가 잘린다**
- 수평 반동(`_recoilYaw`)도 같다. 좌우 랜덤이라 랜덤워크로 흘러가며 요에는 클램프가 없고, 플레이어가 보정한다
- `_recoilApplySpeed`(기본 15)의 Lerp는 **회복이 아니라 킥을 몇 프레임에 걸쳐 부드럽게 얹는 용도**다. 이걸 회복 속도로 읽지 말 것
- **`_currentSpread`는 반대로 `_spreadRecoveryRate`로 자동 회복한다** — 스프레드와 반동은 회복 정책이 서로 다르다. 비대칭을 맞추려 들지 말 것

### 탄약 동기화 정책 (2026-08-25 확정)
- **매 발사마다 인벤토리를 서버와 동기화하지 않는다.** 발사 1회당 왕복은 낭비라는 판단
- **장전 등 확실한 동기화 시점 전까지 클라와 서버의 탄약 수치가 어긋나는 것을 정상 범위로 본다**(느슨한 동기화). 클라의 차감은 예측이고, 클라의 발사 차단은 UX·트래픽 절약용이다
- **최종 판정 권한은 서버에 있다** — 서버는 발사 패킷 수신 시 가능한 경우 차감하되 패킷 유실과 클라 값 조작을 전제로 판정한다
- 따라서 `D2CFullInventorySync`가 탄약을 서버 값으로 덮는 것은 **정상 동작이다. 버그로 보지 말 것**
- **재장전 수단이 아직 없다** — 위 우선순위 12번. 이 정책의 '확실한 동기화 시점'이 아직 존재하지 않는다는 뜻이기도 하다

### 서버 계약 (2026-08-25 — 스폰 실드)
- **플레이어가 처음 스폰될 때 실드는 최대치다.** 0에서 재생으로 차오르는 게 아니다
- **어떤 패킷으로도 오지 않는다** — 최대치의 출처가 방어구 스펙(`ArmorSpec.MaxShieldPoint`)뿐이라, 클라는 `D2CFullInventorySync`로 방어구를 알게 된 뒤에야 값을 채울 수 있다. HP 최대치(`MAX_HEALTH_POINT` 상수)와 사정이 다르다
- **방어구 착용·해제·교체는 여전히 0에서 다시 찬다.** 스폰과 규칙이 갈리므로 `TryInitShield()`의 일회성 플래그로 구분한다 — 한쪽 규칙으로 통일하지 말 것

### 서버 계약 (2026-08-23 proto 변경 3차 — 오브젝트 처치)
- **비플레이어 오브젝트의 제거 통보가 둘로 갈렸다.** 처치는 `D2CNotifyObjectKilled`(41), 그 외 사유는 `D2CNotifyDespawnObject`(39). **41이 제거 신호를 겸하므로** 이걸 받고 치우지 않으면 파괴된 오브젝트가 맵에 영원히 남는다
- **39는 현재 서버가 보내는 경로가 없다** — 컨테이너가 게임 종료까지 유지되어 처치 외 제거 사유가 없기 때문이다. 사유가 생길 때를 위한 예약이므로 핸들러는 지우지 말 것
- 41의 필드·규약은 `D2CNotifyPlayerKilled`와 동일하다(`0xFFFFFFFF`=가해자 없음, 무기 미탑재, 룸 전체 브로드캐스트). 피해자가 플레이어인 죽음은 여전히 40번이다
- **`enum PktId` 누락이 두 번째다** — 36~40에 이어 41도 메시지 주석에는 번호가 적혀 있는데 enum에는 빠져 있었다(서버측에서 확정). 새 패킷을 받을 때 **enum 등재 여부를 먼저 확인할 것**. 생성 스텁이 메시지 클래스만 있고 `PktId` 멤버가 없는 반쪽 상태도 실제로 발생했다

### 서버 계약 (2026-08-21 proto 변경 2차 — 사망 유예)
- **사망해도 세션이 즉시 끊기지 않고 5초의 유예가 있다.** 기점은 `D2CNotifyPlayerKilled.victim_object_id == 내 objectId`이며, 이 패킷은 **피해자에게도 온다**(구 계약에서 바뀜). 사망 판정을 HP 0으로 하면 기점이 이중화된다
- **유예 중 하향은 전부 계속 온다**(관전 유지 가능), **상향은 하트비트를 뺀 전부가 버려진다.** 유예 길이는 어떤 패킷으로도 오지 않아 클라 상수로 둘 수밖에 없다
- **자기 캐릭터의 디스폰 통보는 오지 않는다** — 유예 동안 남겨두고 스스로 치우라는 뜻
- 게임 결과·인벤토리 반영·매칭 락 해제는 **사망하는 순간 이미 시작**된다. 유예 중 연결을 끊어도 결과가 달라지지 않는다
- 귀환 성공 통보의 ACK는 **세션이 언제 끊기느냐만** 좌우한다. 인벤토리 반출은 성공 시점에 확정된다

### 서버 계약 (2026-08-21 proto 변경 1차 — 무기)
- **'장착한 무기'와 '손에 든 무기'는 다른 개념이다.** 무기 슬롯 2개는 장착이고 손에 든 것은 하나뿐이며, `C2DRequestWeaponFire.weapon_dbid`는 손에 든 쪽이어야 한다. 어긋나면 서버가 발사를 **조용히 버린다**(에러 응답 없음). 추적 출처가 넷이라 한 묶음으로 볼 것 — 상세는 `Scenes/CLAUDE.md`의 '손에 든 무기'
- **내 장착·해제로 손에 든 슬롯이 바뀌는 경우는 통보가 없다.** 클라가 서버 규칙을 직접 들고 있어야 하는 유일한 상태다
- `D2CNotifyWeaponChanged`의 `inventory_version`은 남의 통보에서 `0xFFFFFFFF`다. `0`은 실재하는 버전(세션 시작값)이라 미설정으로 읽으면 안 된다 — `attacker_object_id`와 같은 함정

### 서버 계약 (2026-08-20 확인)
- **objectId는 한 게임 안에서 단조 증가하며 재사용되지 않는다. 죽은 오브젝트가 살아나지도 않는다.** proto에 문서화된 보장은 아니다. `_despawnedObjectIds`를 만료 없이 씬 수명 내내 들고 가는 설계의 근거이며, 서버 계약이 바뀌면 이 설계를 다시 봐야 한다(증상은 "특정 오브젝트가 끝까지 안 보임")

### 해소된 버그
- **발사가 `_aimTarget`의 직전 프레임 값을 읽던 문제** — (2026-08-25 #3)에서 해소. `Update()`가 `ProcessFire()`를 `ProcessAim()`보다 먼저 부르는데 후자가 `_aimTarget`을 갱신하고 전자가 읽었다. **같은 프레임 안에서 쓰고 읽는 상태는 `Update()` 호출 순서가 곧 계약이다** — `PlayerController.Update()`에 새 단계를 끼워 넣을 때 이 의존을 먼저 볼 것(순서 근거는 코드 주석과 `Controller/CLAUDE.md`에 남겼다)
- **키 리스너 안에서 씬을 전환하면 입력 디스패치가 예외로 터지던 문제** — (2026-08-24 #2·#3)에서 해소. `LoadScene()`의 `Managers.Clear()`가 `InputManager`가 순회 중인 `_keyActions`를 비웠다. **씬 전환은 리스너 안에서 직접 하지 말고 잡큐로 미룰 것**(`Managers.ExecuteAtMainThread`). `InputManager`가 스냅샷 순회로 바뀌어 이제 예외는 나지 않지만, 잡큐 예약이 여전히 올바른 형태다
- **종료 시 `OnDestroy`에서 `Managers.Instance`가 null이던 문제** — (2026-08-24 #2)에서 해소. `OnApplicationQuit`이 먼저 돌아 `s_isQuitting`이 서면 `Instance` 게터가 null을 반환한다. **`OnDestroy`에서 `Managers.*`를 건드리는 모든 컴포넌트는 null 가드가 필요하다** — `IngameScene`·`PlayerController`에만 있고 로비 쪽에 없었던 것은, 테스트 종료가 늘 인게임에서 이뤄져 드러날 기회가 없었기 때문이다
- **`RequestSpawnIfUnknown()`의 자기 objectId 가드 누락** — (2026-08-21 #3)에서 해소. 나는 `_oppoPlayers`에 들어가지 않아, 킬러가 나인 킬 피드마다 **내 스폰을 서버에 요청**하게 된다. 기존 호출부 2곳이 호출 전에 자기 id를 걸러내고 있어 드러나지 않다가 세 번째 호출자에서 노출됐다 — 가드는 호출부가 아니라 헬퍼 안쪽에 있어야 한다
- **장착·해제 시 손에 든 무기가 어긋나던 문제** — (2026-08-21 #2)에서 해소. 두 번 틀렸다: 원래는 `currentWeapon != null`일 때만 `EquipWeapon`을 호출해 **해제해도 이전 무기가 손에 남았고**, (#1)에서 맨손으로 고친 것은 **서버 규칙(반대쪽 슬롯으로 옮김)과 어긋났다**. 서버가 보조무기를 들려준 상태에서 클라만 맨손이면 `weapon_dbid` 불일치로 사격이 통째로 무시된다. 이 경로는 **본인에게 통보가 오지 않아** 클라가 규칙을 직접 들고 있어야 한다
- **미스폰 objectId에 대한 스폰 요청 폭주** — (2026-08-21 #1)에서 해소. `UpdatePlayerStates()`가 10Hz로 새 reliable을 만들어 in-flight 32슬롯을 채우면 **무관한 다른 패킷이 덮어써져 조용히 유실된다**. 재전송 한도를 없앤 뒤(T2) 슬롯 상한을 지키던 장치가 없어져 생긴 노출이다. 새 통보 패킷을 붙일 때 `RequestSpawnIfUnknown()`을 쓰지 않고 직접 전송하면 같은 문제가 재발한다
- **디스폰된 플레이어의 유령 재스폰** — (2026-08-20 #1)에서 해소. 최초 조사 때는 `UpdatePlayerStates()`의 요청 억제 한 곳만 지목했으나, 실제로는 이미 보낸 요청의 늦은 응답이 `SpawnPlayerObject()`로 들어가는 경로가 더 위험해 **두 곳 모두** 막아야 했다

### 서버 변경분 반영 관련 (2026-08-19 조사)
- **`ExternalProtocol.cs`는 이미 새 proto 기준으로 재생성되어 있다** — `.gitignore` 대상이라 git diff에 안 잡히지만 `PktId` 35~40, `D2CNotifyWeaponChanged`, `C2DRequestSwitchWeapon`, `D2CNotifyHealthChange.AttackerObjectId` 스텁이 전부 존재. protoc 재실행 불필요
- **`0xFFFFFFFF`가 '없음', `0`은 실재 objectId** — `D2CNotifyHealthChange.attacker_object_id`, `D2CNotifyPlayerKilled.killer_object_id` 모두 proto3 기본값 0을 미설정으로 해석하면 오귀속이 된다
- **UDP 송신 경로는 전부 메인 스레드다** — 하트비트·재전송 모두 `Managers.Update()` → `UDPManager.OnUpdate()`에서 나가고, 워커 스레드는 미리 만들어진 바이트만 큐에서 꺼내 보낸다. 따라서 메인 스레드가 멈추면 수신 측 상태를 아무리 최신으로 유지해도 내보낼 주체가 없다. 수신 상태 갱신을 워커 스레드로 옮기는 최적화는 이 이유로 이득이 없다(T1에서 검토 후 기각)
- **남의 방어구·실드·HP는 어떤 패킷으로도 오지 않는다 (의도된 설계)** — `blueprint_id` 하나가 `ArmorSpec` 전체와 동치라 수치 노출이 되기 때문. 구 `D2CNotifyEquipmentChanged`의 `armor_id`가 삭제된 이유
- **HP 최대치도 서버가 보내지 않는다** — `MAX_HEALTH_POINT`(20000 = 200.00 HP) 상수가 유일한 출처다. **서버 `PlayerObject::DEFAULT_MAX_HP`와 손으로 맞춰야 하는 값이다.** 이 때문에 `_currentHealthPoint`를 0으로 초기화하면 스폰 직후부터 사망으로 오판되어 실드 재생이 멈춘다(T12에서 발견). HP를 다루는 로직을 추가할 때 같은 함정을 주의할 것
- **실드 재생은 정수 누적으로 구현할 것** — 서버가 `(재생량 × 경과ms)`를 누적해 1000마다 1 회복하는 방식이라, 초당 실수 보간으로 바꾸면 값이 미세하게 어긋난다. 검증 지점이 피격 시점뿐이라 어긋남이 눈에 잘 띄지 않는다

### 귀환(Recall) 설계 확정 사항
- **2단계 프로토콜** — ① `C2DRequestRecall` → `D2CResponseRecall`(승인/거부) ② 승인 시 서버가 1초 간격 5회 검사 후 `D2CNotifyRecallResult`(성공/취소 + 사유)
- **사유 전달 범위가 단계별로 다르다 (의도된 설계)** — **요청 거부**(`D2CResponseRecall`)는 사유를 알리지 않고 bool만. **진행 중 취소**(`D2CNotifyRecallResult`)는 `RecallResultReason`으로 사유를 알린다
- **복수 요청 허용 안 함** — 서버가 멱등성으로 처리, 클라도 `IngameScene._recallRequested`로 막는다. 스팟별이 아닌 씬 단위 플래그여야 다른 스팟 재요청 경로가 막힌다. 요청/응답 대조는 `recall_spot_index` 에코로 충분
- **응답 도달 시점에 씬은 항상 `IngameScene`** — 로딩 중 도달은 논리적으로 불가하므로 핸들러의 씬 가드는 방어 코드일 뿐, 별도 pending 처리 불필요
- **귀환 스팟은 작은 프롭** — 상호작용 판정이 `hit.collider.transform.position` 기준 2m 고정(`PlayerController.CheckInteractable`)이라 넓은 트리거 존은 성립하지 않는다. 영역형으로 바꾸려면 `InteractDistance` virtual화 + `hit.point` 기준 거리로 판정부 리팩터링 필요
- **귀환 스팟은 서버 스폰 대상이 아님** — `ObjectData`에 인덱스 필드가 없으므로 맵 씬에 직접 배치하고 `[SerializeField] _recallSpotIndex`로 부여
- **"실패 시 서버가 반드시 알린다"는 전제 + 워치독** — 전제 자체는 유지하되, 전제가 깨졌을 때 복구 불가가 되지 않도록 클라에 타임아웃을 둔다. `SESSION_LOST`·`SERVER_INTERNAL`은 사유 자체가 통지 경로의 불안정을 가리키고, 서버가 이미 멱등 처리하므로 안전장치 비용은 사실상 0. 반대로 없을 때의 손실은 "그 판 탈출 영구 불가"로 최대치라 비용이 비대칭
- **알려진 한계** — 타임아웃 후 재요청 시 1차 시도의 늦은 응답을 2차 결과로 오인할 수 있다(`recall_spot_index` 에코만으로는 시도 구분 불가). 타임아웃을 10초로 넉넉히 잡아 실무상 회피 중. 엄밀히 하려면 `C2DRequestWeaponFire`의 `FireSequence`처럼 요청 시퀀스를 proto에 추가해야 함
