# 프로젝트 진행 상황

> 최종 수정: 2026-08-31
> 장르: 멀티플레이어 Extraction 게임 (알파 단계)
> 엔진: Unity 6000.4.0f1 / URP 17.4.0

---

## 완료된 것들

### 네트워크/UI

- [x] (2026-08-29 #4) **설정값 실제 적용 완성 + VSync 신설** — 저장만 되고 화면에 반영되지 않던 해상도·창모드·FOV를 붙이고 프레임 상한에 120·144를 더해, `PlayerPrefs`에 저장되는 9개가 전부 실제로 적용된다. 값 매핑이 삼항 두 곳에 하드코딩돼 있어 **enum만 늘리면 새 항목이 조용히 60으로 도는** 구조였으므로 `Define.FrameRateValues`로 일원화했고, VSync는 `vSyncCount > 0`이면 `targetFrameRate`가 통째로 무시되므로 `ApplyFrameRateAndVSync()` 한 함수로 묶었다(나누면 "상한을 바꿨는데 안 먹는다"가 된다). 마우스 감도 상한은 프리팹 슬라이더 둘에 맞춰 3.0 → 5.0으로 올렸다.

- [x] (2026-08-29 #5) **네 건 실측 통과 + 달리기 발소리 간격 조정** — 재장전 연출 3단계·월드 3D 사운드·점프 스태미나·재장전 진입 문턱이 한 판에서 모두 정상 동작을 확인했다. 그 결과로 달리기 발소리 간격을 `1f/3f`에서 **0.25초**로 당겼고(애니 접지 기준값 0.3보다 빠른 쪽을 체감으로 택했다), 발소리·발사음의 `volumeScale` 밸런스는 거슬리지 않아 `OPTION:`으로 보류했다.

- [x] (2026-08-29 #6) **컨테이너 거부 비트 `0x0400`·`0x0800` 분기** — 두 Deny 패킷이 비트를 보지 않고 전부 재동기화하던 것을 갈랐다. `0x0400`은 그 컨테이너가 내 것이 아니라는 뜻이라 **컨테이너 재동기화 요청 자체가 성립하지 않아** 로컬 UI를 먼저 닫고(닫힌 상태에서 부르면 `_uiOpenCount`가 잘못 깎이므로 가드가 붙었다), `0x0800`은 가까이 가면 풀리는 일시적 조건이라 아무것도 하지 않는다. 통보에 `container_object_id`가 없어 **앞 컨테이너의 늦은 거부가 방금 연 컨테이너를 닫는 오작동은 감수했다.**

- [x] (2026-08-29 #7) **proto `[작업사항]` 체계 소진 — 서버 변경분 반영이 전부 끝났다** — 마지막 잔여 항목이던 `D2CUpdatePlayerStates`(윈체스터 맵 룸이 이동 상태를 브로드캐스트하지 않던 서버 버그, 클라 수정 없이 확인만 필요했다) 확인이 끝나 그 블록과 안내 헤더를 함께 회수했고, **파일에 `[작업사항]`이 하나도 남지 않았다.** 블록이 들고 있던 유일한 계약성 문장("실드 값은 이 패킷에 실리지 않는다")은 `Scenes/CLAUDE.md`에 이미 두 곳으로 있어 옮길 것이 없었다. 태그 규약 자체는 서버 팀의 다음 인계를 위해 `Network/CLAUDE.md`에 남겼다.

- [x] (2026-08-29 #8) **씬 배치 오브젝트 바인딩을 `BaseScene.BindSceneComponent<T>`로 통합** — `Find` 결과만 보고 `GetComponent`는 검사하지 않던 바인딩이 11곳(인게임 8 · 로비 3) 있었고, 스크립트 미부착 하나가 `Init()`에서 NRE를 내며 **그 아래 크로스헤어 생성·키 리스너 등록까지 통째로 멈추는** 구조였다. 오브젝트 없음·스크립트 없음을 각각 `LogError`로 드러내는 헬퍼로 묶어 68줄을 39줄로 줄이면서 구멍을 닫았다. **UI 전용으로 두지 않은 것은 의도다** — 좁은 이름은 "저건 UI용"이라는 판단을 만들어 `Find` 직접 사용을 되살리므로, 규칙의 집도 `UI/CLAUDE.md`에서 `Scenes/CLAUDE.md`로 옮겼다.

- [x] (2026-08-29 #9) **`IngameWeaponUI.WeaponImage` 배선 — 자산 규격이 정해져 막혀 있던 `TODO:`가 풀렸다** — 스프라이트가 √2:1로 확정돼 `weapon_sprite_{item_id}` 규약(아이템·맵과 같은 꼴)으로 붙였고, 지금까지 흰 사각형으로 그려지던 자리가 채워졌다. **`SetWeapon()`이 발사마다 불리므로 `_shownWeaponId`가 바뀔 때만 로드한다** — 이 id의 출처가 서버 확정값이라 가드 통과 시점이 곧 교체 확정 시점이 되고, 그래서 이미지 전용 갱신 경로를 따로 만들지 않았다(경로를 나누면 '갱신 지점 다섯' 함정이 되살아난다). `Preserve Aspect`는 슬롯 비율이 √2:1에 가까워 **의도적으로 꺼둔 것**이라 근거를 문서 양쪽에 남겼다.

- [x] (2026-08-30 #0) **무기 슬롯 조작의 탄창 언로드 배선 — 인벤토리가 서버와 갈리던 원인이 닫혔다** — 무기를 내려도 클라는 탄창을 그대로 두는데 서버는 인벤토리로 쏟고 있었고, `SetPrimaryWeaponMagazine` 계열이 **선언만 있고 호출부가 하나도 없던 것**이 그 증거였다. 서버가 명문화한 배치 규칙(①같은 탄종 스택에 합산 ②최소 인덱스 빈 칸 ③폐기)을 `UnloadMagazineToInventory()`로 재현해 **스왑을 마친 뒤** 부르고(앞으로 옮기면 빈 칸에 내린 무기가 방금 쏟은 탄약과 자리를 다툰다), 응답의 `unloaded_ammo_slot`으로 검산하되 **`quantity`는 비교하지 않는다** — 발사 탄약이 느슨한 동기화라 넣으면 쏘고 나서 무기를 바꿀 때마다 헛 재동기화가 나간다. 곁들여 장비 경로의 `0x0040` 거부를 경고로 내렸다(탄약 칸에 무기를 내리는 흔한 오조작이라 `LogError`로 두면 진짜 내부 오류가 묻힌다).

- [x] (2026-08-31 #0) **UI 사운드 3종 배선 — 자산만 있고 호출부가 없던 마지막 사운드가 붙었다** — `SoundManager`에 `PlayUISubmit`/`PlayUIReturn`/`PlayInventoryChange` 진입점을 두고 47곳을 연결해, `Sounds/` 12종 전부에 재생 경로가 생겼다. 호출부가 40곳이 넘는데 **클립을 못 찾으면 무음이고 `GetOrAddAudioClip`이 그 null을 캐시까지 해 로그도 안 남으므로**, 문자열을 흩지 않고 이름을 진입점 세 개에 가뒀다. 조기 return 가드 뒤에서 내고(아무 일도 안 하는 클릭이 성공처럼 들리지 않게) 팝업을 여는 버튼은 무음이며(두 번 울린다), **인벤토리음은 판정 주체가 달라 로비는 조작 지점·인게임은 응답 핸들러로 자리가 갈린다.**

- [x] (2026-08-31 #1) **로비 ESC 우선순위 신설 — `UserState == Main`에 로그아웃 팝업을 붙이면서 가려져 있던 층 문제가 함께 드러났다** — 한 번의 입력이 가장 위에 있는 것 하나만 소비하도록 **팝업 → 설정 창 → `_lobbyState` 분기** 순으로 갈랐고, 앞의 둘은 상태와 무관한 오버레이라 `switch` **바깥**에 뒀다(안에 넣으면 상태마다 가드가 복제돼 새 상태에서 하나가 빠진다 — 실제로 "팝업이 뜬 채 뒤 화면만 바뀌는" 기존 버그가 그 형태였다). **ESC는 모든 층에서 '취소'로 통일했고**(설정 창은 취소 버튼과 같은 경로라 변경이 있으면 재확인 팝업이 뜬다) 결말이 하나뿐인 `LobbyOnlyConfirm`만 확인으로 가는데, 무동작으로 두면 그 자리에서 ESC가 죽은 키가 되고 확인 콜백이 실제 흐름(로그아웃 완료)을 들고 있어 태우는 쪽이 맞다. **팝업 해제는 `Button.onClick.Invoke()`를 거친다** — 콜백만 부르면 `isActive`가 true로 굳어 그 뒤 모든 팝업이 조용히 뜨지 않는다(무증상 실패 모드라 이 한 건만 먼저 실측했다).

- [x] (2026-08-31 #2) **게임 결과 씬 표시 UI — 로비→매치→결과→로비 루프의 마지막 구멍이 닫혔다** — `Util.Log`로만 뿌리던 자리에 `GameResultSceneUI`·`LootContainerSlot`을 붙여 결과 문구(0.3초) → 전리품 0.2초 간격 생성 / 사망 시 분실 문구(0.3초) → 확인 버튼(0.3초) 연출로 세웠고, **결과는 참조가 아니라 값으로 넘긴다** — `MoveToLobby()`가 `ClearGameResult()`를 즉시 부르고 `LoadScene`만 미뤄, 연출이 도는 중에 원본이 null이 되는 창이 실재한다. **중단 지점을 `CompleteImmediately()` 하나로 모았고 '결과 없이 진입'이 그 첫 소비자다** — 연출을 안 돌리면 확인 버튼이 영영 안 켜져 로비로 나갈 수단이 사라지며, 나중에 붙을 건너뛰기도 같은 함수를 부르면 된다. 사망 갈래는 **가드가 아니라 빈 목록으로** 갈라(생성 루프 둘이 자연히 비워진다) 문구·색과 함께 `ClassifyResult()` 한 곳에서 판정하고, Enter 잠금의 근거는 확인 버튼의 활성 상태 하나로 뒀다.

---

## 진행 중 / 다음 할 것들

> **T번호는 과거 작업 참조용으로만 남아 있다.** T1~T18은 서버 변경분 반영 작업(`server-sync-todo.md`, 2026-08-27 삭제), T19~T24는 클라 작업이며 **전부 완료됐다**(T19 크로스헤어 / T20·T21 총알 궤적 / T22 빈 탄창 발사 모션 / T23 인게임 옵션 UI / T24 재장전·행동 잠금). 신규 작업에는 T번호를 새로 붙이지 않는다 — 완료 항목의 날짜 표기로 충분하다.

### 진행 우선사항

1. **마우스 감도 기본값 확정 + 설정 영속화 검증 (혼자 가능) — 배선은 확인됐고 값 결정이 남았다** — **슬라이더를 움직이면 시점 속도가 실제로 바뀌는 것까지 확인됐다**(2026-08-27). 슬라이더 → `Managers.Setting` → `PlayerController`가 매 프레임 읽는 경로가 끊기지 않았다는 뜻이다. **이 항목의 목적은 "적용되는가"가 아니라 기본값을 확정하는 것이므로 아직 닫히지 않았다.** (2026-08-26 #5)에서 감도가 **슬라이더 값 × 0.1 도/픽셀**로 바뀌어, `SettingManager` 기본값 1.0 기준 **예전보다 10배 느린 0.1 deg/px로 시작한다**(세로 가동 범위 170도가 마우스 170픽셀에서 1700픽셀이 된다). 볼 것: ① 0.1 deg/px가 과하게 느리지 않은지 ② **연사 중 총구를 끌어내리는 조작이 되는 감도인지** — 반동을 손으로 잡는 설계라 이게 이 값의 진짜 기준이다. **(2026-08-29 #4)에서 상한이 5.0으로 넓어져 슬라이더만으로 0.5 deg/px까지 올려볼 수 있다** — 편한 지점을 먼저 찾고, 그 값이 1.0에서 크게 벗어나면 `PlayerController.MOUSE_SENSITIVITY_DEG_PER_PIXEL`에 반영해 슬라이더를 1.0으로 되돌릴 것(기본값이 조작 범위 한가운데 있어야 위아래 여지가 남는다).
    - **저장 검증이 같은 자리에서 끝난다** — 감도를 맞추려면 어차피 껐다 켜야 하고, **설정 창을 이미 열어 본 상태라 셋 다 몇 초면 끝난다.** ① 감도·볼륨이 재실행에 유지되는지 ② **설정 창을 닫고 정상 종료가 아닌 방법으로**(에디터 플레이 모드 강제 정지 등) 끈 뒤에도 남는지 — `OnApplicationQuit` 하나로는 부족해서 '닫힘'에 건 이유를 이게 검증한다 ③ **취소로 닫아도 되돌린 값이 저장되는지**(빠뜨리면 메모리와 디스크가 어긋난 채 남는다)
    - **손상값 방어는 저장소를 직접 편집해 본다** — Windows는 `HKCU\Software\<회사>\<제품>`이다. `Setting.MouseSensitivity`를 999로 바꾸면 **5.0**으로 clamp되고(로드가 setter를 통과한다는 증거), `Setting.Resolution`에 없는 문자열을 넣으면 경고 로그와 함께 기본값으로 돌아간다
    - **(2026-08-29 #4)의 설정 적용분이 같은 판에 얹힌다 — 셋은 에디터에서 바로 보이고 하나만 빌드가 필요하다.** ① 프레임 상한이 30→60→120→144로 순환하고 실제로 적용되는지 ② **VSync 체크박스** — 창을 열었을 때 체크가 **풀린 채로** 보이는지(프리팹 `m_IsOn`이 아니라 저장값을 읽는다는 증거), 체크만 바꾸고 '적용'에서 재확인 팝업이 뜨는지(`HasChanges()` 누락은 변경이 조용히 버려지는 실패 모드다), 켠 상태에서 프레임을 30으로 해도 주사율로 도는지 ③ **FOV** — 로비에서 90으로 올리고 매치 진입 시 화각이 넓어지는지(**로비에서 안 바뀌는 것이 정상**이다. 인게임 카메라가 없다) ④ **해상도·창모드는 빌드에서만 확인된다** — `Screen.SetResolution()`이 스탠드얼론 전용이라 에디터에서는 아무 일도 일어나지 않는다. 재실행 시 저장된 해상도로 뜨는지까지 볼 것
2. **로비 경로 잔여 검증 (혼자 가능)** — 두 건 다 "로비로 돌아가는 경로가 없어 여태 실행된 적 없는 코드"였고, 재매칭까지는 이미 통과했다.
    - **세션 유지의 실패 분기 둘** — (2026-08-24 #0) 판단의 핵심이었는데 정작 미검증이다. ① 서버 중지 상태로 복귀 → `Unreachable`, 재시도/취소 팝업이 뜨고 취소 시 추가 팝업 없이 TestStart 플로우로 합류하는지 — **ESC도 취소와 같은 경로가 됐으므로**(2026-08-31 #1) `FallbackToStartFlow`가 도는지 같은 자리에서 함께 본다 ② Redis 세션 삭제 후 복귀 → `Expired`, **폴백이 먼저 수행되고 안내가 나중에** 뜨는지(`LobbyReconfirmUI.isActive` 때문에 순서가 뒤집히면 안내가 조용히 삼켜진다) ③ 게스트 로그인으로 같은 루프 → Header가 Guest로 복귀하는지 ④ 일반 최초 실행 경로 회귀 확인
    - **로그아웃 → ESC 종료** — (2026-08-24 #2)의 `OnDestroy` null 가드가 아직 한 번도 실행된 적이 없다
3. **2인 접속 한 판 — 사망 경로 세부 관측** — 골격(데미지 주고받기 · 사망 후 로비 복귀 · 귀환 후 로비 복귀)과 두 맵의 이동 동기화는 이미 통과했다. **2인을 붙이는 비용이 가장 비싸므로 아래를 한 판에 몰아서 볼 것.**
    - **사망 경로 7항목**: ① 진입 직후 HP·실드 만피 → 방어구 해제 시 0 ② 1/2 키 전환이 유예 뒤 반영 / 전환 대기 중 클릭이 안 나감 / 주무기 해제 시 보조무기가 손에 ③ 상대 화면에서 내 손의 무기가 바뀌는지 ④ 피격 시 게이지가 서버값으로 점프 ⑤ 처치 시 킬러 화면 `[KillFeed]` ⑥ 피해자 화면 탑뷰 연출 + 4초 조작 차단 + **상대는 계속 움직이는지**(관전 유지) ⑦ 킬러 쪽 `킬수 — 플레이어: 1`. **⑤가 안 나오면 ③을 볼 것** — `weapon_dbid`가 어긋나면 사격이 통째로 조용히 무시된다
    - **실드 기대값 대조** — 진입 직후 실드 만피(`[ShieldInit] 스폰 실드 최대치 적용 — N` 로그와 대조. 방어구 없이 스폰하면 `N=0`이 정상) → 방어구 **해제** 시 0 → 재착용 후 초당 100(=1%) 상승 → 피격 시 서버값으로 점프. HP 쪽은 (2026-08-25 #5)에서 닫혔다. 어긋나면 후보는 `SyncHealthBarMax()` 호출 시점과 실드 예측 누적 로직 둘뿐이다
    - **탑뷰 연출에서 시신이 보이는지** — 컬링 마스크를 원본 카메라에서 복사하므로, 1인칭에서 자기 몸을 숨기려고 레이어를 빼뒀다면 탑뷰에서도 안 보인다(마스크에 더하는 한 줄이면 된다). URP 후처리·전용 렌더러도 연출 카메라에는 복사되지 않는다
    - **히트박스 반지름 확인(30초)** — 별도 튜닝 세션은 불필요하다고 판정했다. 여러 판의 피격 실험에서 이상이 없었고 **부위별 차등 데미지 계획이 없어**(2026-08-27 서버 확인) 캡슐 11개는 실루엣 판정용일 뿐이다. 다만 **"너무 두껍다"는 실패 모드는 원리상 플레이로 드러나지 않으므로**(빗나간 탄이 맞는 건 쏘는 쪽이 못 느끼고 맞는 쪽도 원인을 못 짚는다) **상대를 선택해 Scene 뷰 캡슐 기즈모와 모델 실루엣이 겹치는지만 볼 것.** 어긋나면 `HITBOX_DEFS.RadiusFactor` 비율만 만진다. 판정이 다소 관대한 것 자체는 결함이 아니다
    - **시신·킬 피드의 남은 절반이 이번 판에서 처음 밟힌다**((2026-08-27 #6)에서 씬 초기화·컨테이너 상호작용까지는 통과했다) — **`SingleKillLog` 하위의 `KillerId`/`VictimId`가 없으면 첫 킬에서 NRE가 난다**(`transform.Find` 직후 `.GetComponent<TextMeshProUGUI>()`를 바로 부른다) — ⑤는 이제 로그가 아니라 **화면의 킬 로그가 5초 뒤 사라지는지**까지 보고, 죽은 자리에 **`PlayerLoot` 컨테이너가 서고 열리는지**를 함께 볼 것. 정상인데 오해하기 쉬운 것은 `오브젝트: 0` 하나만 남았다(전투 오브젝트가 아직 없다)
    - **킬 로그 표시명이 (2026-08-27 #7)에서 서버 이름(userId)으로 바뀌어 이번 판이 첫 실측이다** — ⓐ 킬러·피해자 자리에 아이디가 뜨는지(`objectId=N`이 보이면 서버가 이름을 비워 보낸 것이다) ⓑ **자기 킬·자기 죽음도 아이디로 뜨는지**(`"나"` 특별 취급을 폐기했다) ⓒ **가해자 없는 죽음**(낙하 등)에서 킬러 칸이 빈 문자열로 들어가 `[KillIcon] [피해자]` 꼴이 되는데 **실제 화면에서 어색하지 않은지**. **프리팹 구조는 (2026-08-28 #0)에서 확인됐고**(자식은 `KillIcon`·`KillerId`·`VictimId` 셋) **빈 문자열 유지가 확정이라 이건 눈으로 보는 것만 남았다** — 어색하면 고칠 곳은 `SingleKillLog.Init()` 한 곳(아이콘도 함께 `SetActive(false)`)이다
    - **명중 로그 `[Fire] 피격 대상 objectId=N`(`TEMP:`) 대조** — (2026-08-28 #1)에서 우선순위 10번을 닫을 때 이것만 남았다. **대상이 있어야 밟히므로 2인 판이 유일한 확인 자리**이고, ⑤가 나오는데 이 로그의 objectId가 상대와 다르면 히트스캔이 엉뚱한 것을 잡고 있는 것이다. 확인되면 `TEMP:` 회수
    - **오포 소리가 이번 판에서 처음 밟힌다 (2026-08-29 #0·#1)** — ⓐ 상대 발소리가 **상대 위치에서** 들리는지(좌우 정위) ⓑ **상대 총성이 들리는지, 그리고 몇 m에서 끊기는지** — `MaxDistance` 30이 총성에 좁을 가능성이 높고, 좁으면 해법은 `SoundPoint` 분리다(고려사항) ⓒ **점프로 발소리를 지울 수 없는지** — 상대에게 제자리 점프와 이동 점프를 시켜 보고, 이동 중 점프에서 소리가 이어지고 제자리 점프는 조용한지 ⓓ **빗맞은 총도 소리가 나는지**(궤적은 안 그려지는 것이 정상이다) ⓔ 내 총성이 두 번 울리지 않는지(`_oppoPlayers` 가드가 이중 재생을 겸한다). **`OppoPlayerObject` 프리팹에도 `SoundPoint`가 이미 있으므로 배치 작업은 없다**
    - **상대 재장전 3단계가 들리는지 (2026-08-29 #1)** — 0·1은 상대 클라가 보낸 것을 중계한 값이고 **완료(15)는 서버가 발행한 것**이라, 완료만 들리고 앞의 둘이 안 들리면 **C2D 44번이 버려지고 있는 것이다**(상태 패킷과 `uSeqNum`을 공유해 늦으면 버려진다 — **한두 번 빠지는 것은 정상이고 매번 빠지면 문제다**). 반대로 앞의 둘만 들리고 완료가 없으면 서버가 15를 안 뿌리는 것이다. **단계가 가끔 빠지는 것을 결함으로 보고 reliable로 올리지 말 것**
    - **오브젝트 킬 로그의 `name=`(`TEMP:`)** — 전투 오브젝트가 없어 이번 판에서는 밟히지 않는다. `ICombatTarget`이 보급되면 그때 `[ObjectKill] name=…`으로 킬러가 식별되는지 보고 `TEMP:` 둘을 함께 회수한다(우선순위 6번)
4. **귀환 스팟 씬 배치** — 맵 씬에 귀환 단말기 오브젝트 배치, `RecallSpotController` 부착 후 인스펙터에서 `_recallSpotIndex`를 서버 테이블 값에 맞춤. 조준 레이가 맞아야 하므로 트리거가 아닌 일반 콜라이더 사용
5. **귀환·탈출 연출 둘 (코드만, 프리팹 불필요)** — ① **귀환 진행 중 UI 피드백**: 승인~결과 사이 5초 구간 표시(카운트다운 등). `InteractText`는 매 프레임 재조회되므로 `virtual` 프로퍼티화하면 동적 텍스트 전환이 된다 ② **탈출(귀환 성공) 연출이 `BeginMatchExit()`에 자리만 비어 있다** — 결과 씬 쪽은 (2026-08-31 #2)에서 끝났으므로 남은 것은 매치를 떠나는 4초 유예 동안의 화면뿐이다
6. **TEMP 회수 3건 — 워치독 2건 제거 검토 + 오브젝트 킬 이름 로그** — 후자는 (2026-08-27 #7)에서 확인용으로만 실은 `killer_object_name`이며, **원복 지점이 `Handle_D2CNotifyObjectKilled`의 추출과 `IngameScene.HandleObjectKilled`의 파라미터 두 곳이라 한 쌍으로 되돌린다**(오브젝트 킬은 킬 피드 미적용이 확정이라 소비처가 생길 일이 없다). 워치독 둘은 귀환(`RECALL_TIMEOUT`/`_recallTimer`)과 행동 잠금(`ACTION_PENDING_TIMEOUT`). 후자는 무기 교체 전용 워치독이 흡수되며 재장전까지 함께 커버하게 됐다 — 응답이 유실되면 발사가 그 판 내내 막히므로 결과를 추측하지 않고 로컬 잠금만 푼다(판정 권한은 서버에 남는다). 서버 응답 신뢰성이 실측으로 확인되면 제거. **귀환 쪽은 실처리가 붙어 이제 검토 가능**하다
    - **`TODO:` 귀환·상호작용의 행동 잠금 편입 범위 (미결)** — 귀환(`_recallRequested`)은 구조가 사실상 같아 그대로 흡수되고 **워치독도 하나로 합쳐진다.** 반면 **상호작용은 컨테이너가 열려 있는 동안 지속되는 상태라 `Pending`으로 잡으면 여는 내내 모든 행동이 잠긴다** — "요청~응답 구간만" 잠그고 열린 상태는 기존 `_isContainerOpen`/`_uiOpenCount`에 맡기는 쪽이 유력하다. 코드에는 `IngameScene.RequestRecall()` 위에 같은 `TODO:`가 걸려 있다
7. **탄착 이펙트 프리팹 준비 → 구현 — 범위가 절반으로 줄었다** — **총성은 (2026-08-29 #0)에서 로컬·오포 양쪽 끝났고 머즐 플래시는 만들지 않기로 확정됐다.** 남은 것은 **탄착 이펙트 둘뿐**이다 — 로컬(`ProcessHit`)과 수신 측(`HandleWeaponFireBroadcast`의 `hasHitPoint` 블록). 파티클 에셋이 `Resources/Prefabs/` 아래에 필요하다. **이펙트 기준점은 `_shotPoint`가 아니라 총구다** — `_shotPoint`(가슴팍)는 피격 판정 전용이고 시각 표현은 별도 기준을 쓰기로 확정했다. 판정선과 이펙트선이 어긋나는 것은 의도된 것이므로 맞추려 들지 말 것. **총구 기준점 추적은 (2026-08-26 #1)에서 이미 끝났다** — `GameObjectController.FindMuzzlePoint()`와 `PlayerController._muzzlePointTr`을 그대로 쓸 것
    - **탄착 이펙트에도 `hit_point` 비대칭이 그대로 걸린다** — 수신 측은 빗맞은 발사의 좌표를 모르므로 그릴 수 없다. **소리는 그 가드 밖에 있고 이펙트는 안에 있는 것이 정상**이며, 맞추려고 소리를 안으로 옮기지 말 것
8. **실제 맵 씬 디자인·에셋 배치 → IngameScene 상속 완성** — Test 맵이 아닌 실사용 맵 씬을 만들고(레벨 디자인 + 에셋 배치), `IngameScene`을 상속하는 맵별 씬 컴포넌트를 구현한다. **씬 내장 UI 8종(`IngameInventoryUI`·`IngameDragGhost`·`InteractUI`·`IngameHealthBarUI`·`IngameSettingUI`·`IngameKillLogUI`·`IngameWeaponUI`·`IngameStaminaBarUI`)을 맵 씬마다 배치해야 한다**(크로스헤어만 코드가 세우므로 예외). **(2026-08-29 #8)에서 바인딩이 `BindSceneComponent<T>`로 통합돼 빠뜨린 UI가 조용히 죽지 않고 콘솔에 원인이 뜬다** — 배치 누락은 이제 로그로 잡히므로, 남은 것은 씬 작업과 맵별 컴포넌트뿐이다. **씬에는 활성 상태로 저장할 것**(`GameObject.Find`가 비활성을 못 찾는다)
    - **맵이 서고 나서야 시작할 수 있는 후속 UI 둘** — 지형이 확정돼야 만들 수 있으므로 이 항목이 닫히기 전에는 착수하지 않는다. ① **`IngameMapUI`** — `M` 키로 열고 대략적인 지도와 현재 위치를 표시 ② **`IngameCompassBarUI`** — 화면 상단에 상주하며 바라보는 방향의 방위(0~360)를 표시. **둘 다 씬 내장 UI가 되므로 배치 대상이 8종에서 10종으로 늘어난다**
9. **인게임 ESC 2지 선택 UI 신설** — 지금은 ESC가 곧장 `IngameSettingUI`를 연다. 앞에 **옵션 / 게임 종료** 두 갈래를 제시하는 UI를 세워 **인게임에서 판을 뜨는 수단을 만든다**(현재 이탈 경로는 사망과 귀환 둘뿐이다). 종료는 **매치를 정상 이탈 처리한 뒤 그대로 앱을 종료**하는 순서다 — 로비까지 돌아가지 않는다. **`CompleteMatchExit()`을 우회해 프로세스만 죽이지 말 것**(결과가 저장되지 않고 서버에 세션이 남는다). 포기를 서버에 알리는 수단이 proto에 있는지부터 확인할 것
10. **피격 화면 연출 — 내가 맞았을 때** — 피해자 본인 화면의 HUD 피드백(붉은 비네트·피격 방향 표시 등). **7번 탄착 이펙트와는 다른 것이다** — 그쪽은 맞은 지점에 그리는 월드 파티클이고 이것은 화면 오버레이라 기준점도 배치처도 겹치지 않는다. 발동 시점은 HP가 서버값으로 갱신되는 자리를 그대로 쓴다
11. **인벤토리 아이템 스프라이트를 실사용 이미지로 교체** — `Images/Items/icon_item_{item_id}.png` 7종이 테스트용 임시 이미지다. 1:1 비율 권장(반드시 1:1이어야 하는 것은 아니다). **파일명 규약만 지키면 코드 변경이 없다** — 네 곳(`ISlot`·`IngameISlot`·`SSlot`·`UI_Shop`)이 `item_id`로 경로를 조립한다. 교체 후 `ItemsAtlas`에 새 스프라이트가 물렸는지 함께 볼 것
12. **캐릭터 description 다듬기** — `Define.CharacterDescriptions` 3종(0 스크랩 / 1 G-EXPlorer-04 / 2 UHM) 문안 수정. 코드 구조 변경은 없다
13. **외형 다듬기 3건 (자산·씬 작업, 코드 변경 거의 없음)**
    - **`LobbyScene` 에셋 배치** — 배경·소품 배치로 로비를 꾸민다
    - **`MapSelectUI` 외형** — 맵 스프라이트(`Images/MapSprites/map_sprite_{mapId}`)는 이미 배선돼 있으므로 레이아웃과 스프라이트 품질 쪽이다
    - **`LoadingScene` 다듬기**
14. **혼자 가능한 실측 잔여 — 탄창 언로드 4건 + UI 사운드 + 로비 ESC** — 전부 인벤토리·UI 조작이라 2인이 필요 없다. **탄창 언로드는 컴파일과 일반 플레이를 통과했고**(2026-08-31) 그 과정에서 배치 규칙 ①(같은 탄종 합산)·②(빈 칸 이동)가 지나갔다. 남은 넷은 **의도적으로 만들어야 밟힌다.**
    - **탄창 언로드 잔여 4건** — ③ **인벤토리가 꽉 찼을 때**(탄약 폐기 — **조작 자체는 성공하는 것이 정상**이다) ④ **빈 탄창으로 해제**(잔탄 0 처리가 서버와 같은지) ⑤ 컨테이너를 열어둔 채 **컨테이너 칸으로 해제** — 무기는 컨테이너로, **탄약은 내 인벤토리로** 갈리는지 ⑥ **탄약 칸에 무기 해제** → `[EquipItemDeny] denyReasonMask=0x40`이 **경고로** 뜨고 인벤토리가 그대로인지
    - **`[EquipItem] 탄창 언로드 배치 불일치` 하나가 두 가지를 다 드러낸다** — 재현 규칙이 서버와 갈렸거나, 서버가 아직 미배포라 언로드를 안 해 클라만 옮긴 꼴이거나다. 어느 쪽이든 재동기화로 복구되므로 로그를 보고 원인을 가르면 된다
    - **UI 사운드 4건** — ⓐ 안 나는 소리가 있으면 **호출부가 아니라 파일명부터**(무음이고 로그도 안 남는다) ⓑ 매치 시작·로비 복귀에서 **소리가 잘리는지**(`Managers.Clear()`가 소스를 `Stop()`한다 — 코드로 막기 어려워 거슬리는지만 본다) ⓒ 설정 창 **좌우 화살표 연타**가 시끄럽지 않은지(넣기로 판단한 자리라 여기가 제일 먼저 거슬릴 후보다) ⓓ 팝업 경유 버튼(로그아웃·설정 적용)에서 **두 번 나지 않는지**
    - **로비 ESC 4건 (2026-08-31 #1)** — 컴파일과 **팝업 재개**(ESC로 닫은 뒤 다음 팝업이 뜨는지)는 통과했다. 남은 것: ⓐ 인벤토리·상점·캐릭터에서 ESC → Main + `ui_return` ⓑ 옵션창 ESC 두 갈래(변경 없으면 즉시 닫힘 / 변경 있으면 재확인 팝업) ⓒ **팝업이 뜬 채 뒤 화면이 바뀌지 않는지** — 인벤토리 화면에서 로그아웃 버튼을 눌러 팝업을 띄우고 ESC. **고치려던 기존 버그의 재현 자리다** ⓓ "로그아웃되었습니다." 팝업에서 ESC → 클릭과 똑같이 `OnLogoutComplete`이 도는지
    - **`.proto`의 인계용 언로드 규칙 블록은 회수했다** — 영속 정보는 `Scenes/CLAUDE.md`의 '탄창 언로드'에 있고 필드 계약인 `unloaded_ammo_slot` 인라인 주석은 남겼다. **회수한 파일을 서버 사본에 다시 전달할 것** — 클라에서만 지우면 두 사본이 갈라진다

### 진행 고려사항

> 코드를 고칠 때 지켜야 할 제약·불변식은 **모듈 `CLAUDE.md`**(`Assets/Scripts/*/CLAUDE.md`)에 있다. 여기에는 **아직 코드가 없어 적을 자리가 없는 것**과 **오해를 막기 위한 관찰**만 남긴다.

- **재장전·행동 잠금의 방어 경로 넷은 미검증으로 둔다** — 2인 검증은 정상 경로를 덮었을 뿐이다. ① `DENY_VERSION_MISMATCH` 1회 재요청(R 직후 2초 안에 인벤토리에서 탄약을 옮기면 재현된다. **재요청이 한 번만 나가는지**가 관건 — 무한 재시도는 버전이 계속 바뀌면 루프가 된다) ② 낡은 스냅샷 폐기(`[Reload] 낡은 스냅샷 폐기` 경고로 드러난다) ③ `fire_sequence` 복구(**되돌아간 시퀀스의 유일한 복구 경로**라 여기가 죽으면 그 무기의 발사가 계속 거부된다) ④ `ACTION_PENDING_TIMEOUT` 워치독. **①②는 마음먹으면 재현되고 ③④는 자연 재현이 어려워 방어 코드로 둔다** — `[Action] … 응답 미수신 (3초)`가 반복되면 워치독이 아니라 **서버가 43번을 안 보내는 것**이므로 서버 쪽부터 볼 것
- **결과 씬 연출 건너뛰기는 자리만 열려 있다** — `GameResultSceneUI.CompleteImmediately()`가 그 지점이고 키 배선만 하면 된다. **Enter를 겸용하지 말 것** — 확인 버튼과 같은 키라 "연출을 건너뛰려다 로비로 나가는" 오조작이 된다. 전리품 28칸이 꽉 차면 대기가 약 6.3초인데, 길다고 느껴지기 전에는 붙이지 않는다
- **변경사항이 있는 로비 옵션창은 ESC만으로 닫히지 않는다 (버그 아님)** — ESC가 모든 층에서 '취소'라, 옵션창 ESC → "변경사항을 취소하시겠습니까?" → ESC(= 아니오) → 옵션창 유지가 반복된다. 닫으려면 그 팝업에서 확인을 누른다. **깨려면 ESC가 층마다 다른 뜻을 갖게 되므로 그대로 둔다**
- **컨테이너가 저절로 닫히는 것이 결함이 아닐 수 있다 (2026-08-29 #6)** — `0x0400` 거부에 `container_object_id`가 실리지 않아, 앞 컨테이너 대상 조작의 늦은 거부가 방금 연 컨테이너를 닫는다. **판별할 방법이 패킷에 없어 감수한 오작동**이고 복구는 다시 여는 것이다. 자주 나오면 그때 서버에 id 추가를 요청할 것
- **컨테이너 닫기는 서버 응답을 기다리지 않는다 — 기다릴 응답이 애초에 없다** — proto에 `D2CResponseCloseContainer`가 존재하지 않고 `C2DCloseContainer`(20)는 필드 없는 단방향 reliable이다. "응답 대기를 없애자"는 개선 항목으로 다시 올리지 말 것
- **탄약 칸에 무기를 떨구는 오조작을 클라가 미리 막지 않는다** — 서버가 `0x0040`으로 거부하고 **아무것도 바뀌지 않아** 동작에는 문제가 없다(검사가 모든 변경보다 앞이라 롤백할 로컬 상태도 없다). `IngameISlot.OnDrop`에 타입 검사를 넣으면 헛 요청이 줄지만 `CanReload` 문턱과 같은 성격의 UX 장치라 보류한다
- **R을 눌러도 재장전이 시작되지 않는 것은 버그가 아닐 수 있다** — 탄창이 가득이거나 예비탄이 0이면 `CanReload`가 진입을 막고 **알림음·경고가 없어 아무 반응도 나지 않는다**(확정). 문턱은 '확실할 때만 막는' 형태라 스펙 조회 실패·맨손에서는 막지 않고 서버에 맡긴다
- **resume 경로의 로비는 재화·인벤토리가 비어 보인다 (버그 아님)** — 서버 `/api/session/resume` 핸들러가 아직 뼈대라 `money: 0, inventory: []`를 반환한다. 서버 구현이 끝나기 전까지 정상이므로 클라 파싱 버그로 오해하지 말 것. **`http-api-spec.yaml`은 서버 사본과 동기화되어 있으므로 한쪽만 수정하지 말 것**
- **`SoundPoint` 하나가 모든 월드 소리의 `MaxDistance`(30)를 공유한다 — 오포 총성이 30m에서 끊긴다** — `SoundManager.PlayOneShotAt`에 `OPTION:`으로 달려 있다. **재생마다 거리 값을 갈아끼우는 해법은 쓰지 말 것**(소스 단위 속성이라 울리던 소리에 소급 적용되고, 걸으며 쏘는 상황이 상시라 실제로 겹친다) — 실제 해법은 프리팹에 근거리·원거리 소스를 나누는 것이라 **프리팹 작업이 선행된다.** 같은 이유로 **소리별 음량 밸런스는 `volumeScale` 인자로만** 잡고 프리팹 Volume은 건드리지 않는다(코드가 덮는다)
- **`Resources/`는 참조 여부와 무관하게 전량 빌드에 포함된다** — 코드 참조가 없는 자산이 확인된 것만 여럿이다(`Prefabs/Scene/*` 4종, `System/@Managers`, `System/EventSystem`, `TestPlayer`, `TestLobbyScene/`, `LobbySettingUIBackup`). **`System/EventSystem`은 코드가 쓰는 `UI/EventSystem`과 같은 것 두 벌**이다. 알파 단계라 용량이 문제는 아니고 **삭제 판단은 사용자 몫이므로 사실만 남긴다** — 목록은 `Assets/Resources/CLAUDE.md`에 있다
- **씬 이름 컨벤션 잔여 (판단 보류)** — `TestIngame2.unity`는 컨벤션대로면 `TestIngame2Scene.unity`이지만 `Define.Scene` enum에 없어 코드 영향이 없고, `WinchesterAlpha.unity`는 맵 에셋 성격이다. **enum에 올리는 순간 파일 이름을 맞춰야 한다**(`Scenes/CLAUDE.md`의 씬 이름 규칙)
