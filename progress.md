# 프로젝트 진행 상황

> 최종 수정: 2026-09-02
> 장르: 멀티플레이어 Extraction 게임 (알파 단계)
> 엔진: Unity 6000.4.0f1 / URP 17.4.0

---

## 완료된 것들

### 네트워크/UI

- [x] (2026-09-01 #12) **좌각/우각 시야 전환 배선 — 어깨 너머 시점을 좌우로 바꾼다** — 에디터에서 선 앵커 둘(`ViewPoint/RightCamPoint`·`LeftCamPoint`)을 `Setup()`이 잡고 `UpdateCameraAnchor()`가 `_isLeftAngleCam`이 가리키는 쪽으로 매 프레임 `Pcam.localPosition`을 민다 — **회전은 부모 `ViewPoint`가 쥐고 위치만 여기가 쥐어 `ApplyViewRotation()`과 순서를 다투지 않지만, `ProcessAim`·`ProcessFire`보다는 앞이어야 한다**(카메라 위치에서 레이가 나가 뒤에 두면 직전 위치 기준으로 조준이 나간다). 즉시 대입은 스케일 2배에서 월드 0.8을 한 프레임에 건너뛰므로 반동과 같은 형태로 보간했고(`CAM_ANGLE_MOVE_SPEED` 15), 토글인 `ChangeCamAngle()`은 `IsInputLocked`·`IsAnyUIOpen`에서 막는다 — 전자를 빼면 **연출이 없는 귀환·연결 끊김 이탈 중에 화면이 옆으로 튄다**. 키는 **`Q`로 확정**했고 등록·해제는 `IngameScene`이 아니라 `PlayerController`에 있다(좌우 피킹의 표준이 Q·E인데 E를 상호작용이 점유해 Q 단독 토글이다). **중앙 상태 없음과 기울이기(린·roll) 미구현이 확정이고 옮기는 것은 카메라뿐이라 모델·히트박스·`_shotPoint`가 전부 그대로다** — 크로스헤어는 모서리 너머에 걸려도 발사선 원점이 가슴팍이라 **보이지만 못 쏘는 것이 정상**이며(peeker's advantage가 없다), 내 몸이 움직이지 않으니 서버에 자세를 알릴 필요도 없다.

- [x] (2026-09-02 #0) **총성 가청 거리 분리 — 오포 총성이 발소리와 함께 30m에서 끊기던 것이 닫혔다** — `OppoPlayerObject`에 원거리 소스 `GunSoundPoint`(120m)를 두고 `PlayFireSound()`만 그쪽으로 옮겼으며, **발소리와 재장전음은 `SoundPoint`(30m)에 남겼다** — 재장전 통보(44/45)가 unreliable이라 단계가 통째로 빠지는 것이 정상 계약이어서, 멀리서 듣는 단편적 재장전음은 없는 정보를 만든다. **로컬은 코드 변경이 없고 `PlayerObject`의 `GunSoundPoint`는 지웠다** — `AudioListener`가 자기 가슴팍 바로 위라 두 소스 모두 `Min Distance` 안쪽이고 감쇠 구간에 들어가질 않아 원거리 소스를 둬도 결과가 완전히 같다(의도된 비대칭이라 근거를 `Controller/CLAUDE.md`에 남겼다). **Rolloff는 Logarithmic 프리셋이 아니라 Custom으로 잡아 `Max Distance`에서 0에 닿게 했다** — 그 프리셋에서 `Max Distance`는 컷오프가 아니라 감쇠가 멈추는 지점이라 그 너머로 볼륨이 고정된 채 계속 들리고, 지금 값에서는 **발소리 바닥값(2/30)이 총성 바닥값(5/120)보다 커지는 역전**이 났다.

- [x] (2026-09-02 #1) **걷기 속도 1.5 → 2 · 점프 중 발소리 문턱 2 → 4.5** — `PlayerController.walkSpeed`만 올리면 끝나는 작업이 아니었다. `OppoPlayerController.FOOTSTEP_RUN_SPEED`가 2였는데 **걷기 속도와 정확히 같아져**, 판정이 `HorizontalSpeed >= FOOTSTEP_RUN_SPEED`이고 `ProcessMovement`가 대각선을 정규화해 걷기 최대 속도가 정확히 `walkSpeed`라 **걸으며 점프한 오포의 발소리가 달리기로 잡히던** 상태였다(속도가 네트워크로 와 오차까지 섞인다). 문턱을 걷기 2와 달리기 7의 **중간인 4.5**로 옮기고 "한쪽 값에 붙이지 말고 중간에 둘 것"을 상수 옆과 `Controller/CLAUDE.md` 양쪽에 근거로 남겼다.

- [x] (2026-09-02 #2) **Tenerife 컨테이너 5종 배선 — 코드까지 서고 프리팹·서버 번호가 남았다** — `TenerifeBlueCar`·`YellowCar`·`BrownCar`·`RedCar`·`Bus`를 `Define.ObjectType`(최종 **4~8**, 아래 #4에서 확정)과 `ObjectPaths`에 넣고 파생 컨트롤러 5개를 만들었다. **번호는 `MaxCount` 바로 앞에 append했고 그 이유를 enum 위에 주석으로 박았다** — proto의 `object_type`이 `int32`라 enum이 없어 컴파일러가 지켜주지 않고, 중간에 끼우면 뒤 번호가 밀려 **에러 없이 엉뚱한 프리팹이 뜬다.** `Controller/CLAUDE.md`에는 컨테이너 추가 시 손댈 곳 셋(enum / `ObjectPaths` / 파생 클래스)과 각각을 빠뜨렸을 때의 증상을 적었다.

- [x] (2026-09-02 #3) **사운드 문서 정정 — Custom Rolloff로 바꾼 뒤에도 남아 있던 Logarithmic 유도 수치를 걷어냈다** — `Resources/CLAUDE.md`에 "`Min Distance` 2 vs 5가 곧 상대 음량이고 5m 밖에서 총성이 2.5배"라고 적혀 있었는데, **프리팹의 실제 커브를 대조하니 거짓이었다**(음량비는 4m 2배 → 16m 4배로 커지고 30m 너머는 발소리가 0이라 성립하지 않는다). 참인 것은 거리비 쪽이라 **"같은 볼륨에 도달하는 거리가 총성이 약 2.6배 멀다"**로 바꾸고, Custom Rolloff에서는 **커브의 x축이 각자의 `Max Distance`로 정규화되고 볼륨을 정하는 것은 `Min Distance`가 아니라 커브**임을 함께 적었다. 같은 오해에서 나온 표현 셋(`Min Distance` 하한 규칙·로컬 비대칭 근거)도 "커브의 첫 키 이전 구간"으로 정정했다 — **결론(로컬에 `GunSoundPoint` 불필요)은 그대로 유효하다.**

- [x] (2026-09-02 #4) **안 쓰는 박스 컨테이너 4종 제거 — 차 5종이 4~8로 내려왔다** — `GreenBox`·`YellowBox`·`SmallYellowBox`·`SmallWhiteBox`의 컨트롤러(+`.cs.meta`)와 `Define.ObjectType`·`ObjectPaths` 항목을 지워, 차 5종이 8~12에서 **4~8**로, `MaxCount`가 13에서 9로 바뀌었다. **이것은 서버 계약 변경이다** — 서버가 옛 번호를 들고 있으면 `4`를 보냈을 때 매핑이 존재하므로 **에러 없이 엉뚱한 차가 뜬다.** 프리팹 4개도 함께 제거돼 `Resources/Prefabs/GameObject/`에서 이 계열이 완전히 빠졌다.

- [x] (2026-09-02 #5) **컨테이너 상호작용 후 조작이 통째로 굳던 버그 수정 — `ShowOpenedContainer()`를 멱등으로 만들었다** — `OnUIOpened()`를 부르는 자리 중 **유일하게 상태 가드가 없어**, 수신 reliable 중복 디스패치나 응답 전 E 재입력으로 두 번 돌면 `_uiOpenCount`가 2가 되고 닫기는 `_isContainerOpen` 가드에 걸려 한 번뿐이라 **1로 굳었다**(커서가 풀린 채 시점·발사·E·Q가 그 판 내내 막히고, `ToggleMyInventory`만 `IsAnyUIOpen`을 안 봐서 I/Tab은 열렸다 — 이 비대칭이 원인을 특정하는 단서였다). 가드는 **표시 갱신 뒤·카운트 조작 앞**에 두어 중복 응답에서도 스냅샷 반영은 유지했고, **인벤토리 반납 블록은 가드 안쪽**이다 — 밖에 두면 이번엔 반대로 과소 계상돼 커서가 잠긴 채 UI를 못 누른다. `Scenes/CLAUDE.md`에 **"`OnUIOpened`/`OnUIClosed`는 상태 플래그의 전이에서만 부를 것"**을 일반 규칙으로 올렸다(기존에는 과소 계상 방향만 적혀 있었다).

- [x] (2026-09-02 #6) **캐릭터 선택창이 인벤토리·상점으로 넘어가도 남던 버그 수정** — `ShowInventory`·`ShowShop`·`EnterMatchingState` 셋이 `UI_CharacterSelect`를 끄지 않았다. 헤더 버튼이 각 전이를 직접 불러 **모든 상태에서 모든 상태로 한 번에 도달**하는데 전이 함수마다 끌 목록을 개별로 나열하는 구조라, 가장 늦게 추가된 `UI_CharacterSelect`가 자기 함수·뒤로가기·로그아웃에만 들어가고 나머지 셋에서 빠진 것이다(뒤로가기를 거치면 정리돼 재현 경로가 좁았다). **구조 개편(전이 한 곳으로 통합) 대신 세 줄 추가로 막기로 했으므로**, 다음 패널 추가 때 다시 밟지 않도록 "끄는 목록 다섯을 모두 고칠 것"과 `UI_Warehouse`가 두 상태 공유라는 점을 `Scenes/CLAUDE.md`에 남겼다.

- [x] (2026-09-02 #7) **`TenerifeScene` 에디터 배치 작업 넷 완료 — 지형·최적화·Boundary·귀환 스팟** — ① 지형지물 정렬·배치 → ② Object 덜어내어 최적화 → ③ Boundary 확정 + 투명 `BoundaryObject` 배치 → ④ 귀환 스팟 위치 확정·바인딩이 전부 끝났고, **매치 진입과 귀환까지 실측으로 확인**돼 Build Settings 등재와 `_recallSpotIndex` ↔ 서버 테이블 대조도 함께 닫혔다. **경계 근처 사격이 `BoundaryObject`에 막히는 것은 확정된 의도**이고(콜라이더라 히트스캔이 그대로 걸린다) **서버는 경계를 모르므로 경계 이탈 처리를 서버에 기대지 말 것** — 이 둘은 코드가 없어 progress.md에 남겼다. 항목이 닫히면서 사라질 지식이던 **`_recallSpotIndex` 불일치 증상**("카운트다운이 떴다가 안내 없이 사라진다")은 `Controller/CLAUDE.md`로 옮겼고, 후속 UI 둘(`IngameMapUI`·`IngameCompassBarUI`)은 착수 조건이 풀려 우선사항 6번으로 승격했다.

- [x] (2026-09-02 #8) **아이템 구매 흐름 전반 — 대상 슬롯 창고 한정 · 재확인 팝업 · 응답 6갈래 분기** — `http-api-spec.yaml`이 구매 대상 슬롯을 **창고(0~79)로 못박고 400을 5종 코드로 세분화**해서, `FindEmptyPurchaseSlotIndex`의 인벤토리 폴백(80~104)을 걷어냈다 — 그대로 두면 창고 만재 시 `ERR_INVALID_SLOT`으로 조용히 400이 나며, **스냅샷은 여전히 0~107 전체를 싣는다**(제한은 대상 슬롯에만 걸린다). `TryPurchase`를 진입점과 `ExecutePurchase`로 갈라 **공간 검사 → 재확인 팝업 → 요청** 순으로 뒀는데 **검사가 팝업보다 앞인 것은 취향이 아니라 구조다** — 확인 콜백이 도는 동안 `isActive`가 아직 true라 거기서 띄우는 팝업은 조용히 무시된다(구매 버튼은 어느 갈래로도 팝업을 열게 되어 `PlayUISubmit()`을 뺐다). `PostPurchaseCall`의 반환을 `bool` → `PurchaseResult` 6상태로 올리고 `OutOfSync`·`Rejected`에서만 `GetInventoryCall`로 재조회하며(그 첫 소비자다), 갈래를 402·409·400/`ERR_SLOT_OCCUPIED`로만 가른 것은 **사용자가 할 수 있는 일이 같으면 묶는다**는 `LoginResult` 원칙 그대로다 — 세 곳으로 늘어난 `Inventory` → 3배열 재구축은 `RebuildSlotsFromInventory()`로 뽑았고, 이 과정에서 `OnLoginComplete`가 `_loadoutSlots`만 비우지 않던 비대칭도 함께 닫혔다.

---

## 진행 중 / 다음 할 것들

> **T번호는 과거 작업 참조용으로만 남아 있다.** T1~T18은 서버 변경분 반영 작업(`server-sync-todo.md`, 2026-08-27 삭제), T19~T24는 클라 작업이며 **전부 완료됐다**(T19 크로스헤어 / T20·T21 총알 궤적 / T22 빈 탄창 발사 모션 / T23 인게임 옵션 UI / T24 재장전·행동 잠금). 신규 작업에는 T번호를 새로 붙이지 않는다 — 완료 항목의 날짜 표기로 충분하다.

### 진행 우선사항

1. **`TEMP:` 회수 1건 — 오브젝트 킬 이름 로그 (대기)** — 워치독 둘은 (2026-09-01 #2)에서 항구 방어 코드로 승격돼 회수 대상이 아니고, 명중 로그는 (2026-08-31 #4)에서, 좌각/우각 임시 키는 (2026-09-01 #12)에서 회수했다. 남은 하나는 (2026-08-27 #7)에서 확인용으로만 실은 `killer_object_name`이며, **원복 지점이 `Handle_D2CNotifyObjectKilled`의 추출과 `IngameScene.HandleObjectKilled`의 파라미터 두 곳이라 한 쌍으로 되돌린다**(오브젝트 킬은 킬 피드 미적용이 확정이라 소비처가 생길 일이 없다). **`ICombatTarget` 보급에 종속된다** — 전투 오브젝트가 없으면 `[ObjectKill] name=…`이 아예 밟히지 않아 확인할 수 없으므로, 그전까지는 손댈 수 없는 대기 항목이다
    - **`TODO:` 귀환·상호작용의 행동 잠금 편입 범위 (미결)** — 귀환(`_recallRequested`)은 구조가 사실상 같아 그대로 흡수되고 **워치독도 하나로 합쳐진다.** 반면 **상호작용은 컨테이너가 열려 있는 동안 지속되는 상태라 `Pending`으로 잡으면 여는 내내 모든 행동이 잠긴다** — "요청~응답 구간만" 잠그고 열린 상태는 기존 `_isContainerOpen`/`_uiOpenCount`에 맡기는 쪽이 유력하다. 코드에는 `IngameScene.RequestRecall()` 위에 같은 `TODO:`가 걸려 있다
2. **아이템 판매 (`POST /api/items/sell`) — 스펙만 서 있고 클라 심볼이 하나도 없다** — `SellRequest`(`item_id`·`slot_index`·`quantity`·`inventory` 전부 required) / `SellData`(`money`·판매 후 전체 인벤토리) / `_sellUrl` / `PostSellCall` / 판매 UI 전부 미존재다. **구매와 갈리는 지점 셋**: ① 판매 슬롯은 **0~107 전체**라 창고 밖(인벤토리·로드아웃)도 대상이고 ② `quantity`는 부분 판매 지시가 아니라 **그 슬롯 스택 전체 수량에 대한 주장**이라 스냅샷과 정확히 일치해야 하며(어긋나면 `ERR_ITEM_MISMATCH`) 부분 판매는 클라가 스택을 먼저 나눈 스냅샷을 보내야 한다 ③ 단가는 `items.price`로 **구매가와 같다(감가 없음)**. 400은 `ERR_BAD_REQUEST`·`ERR_DUPLICATE_SLOT`·`ERR_SLOT_EMPTY`·`ERR_ITEM_MISMATCH` 넷이다 — **구매 쪽 `PurchaseResult`와 같은 형태로 묶되 갈래는 같지 않다**(402가 없고 `ERR_SLOT_EMPTY`가 `OutOfSync` 쪽이다)
3. **캐릭터 description 다듬기** — `Define.CharacterDescriptions` 3종(0 스크랩 / 1 G-EXPlorer-04 / 2 UHM) 문안 수정. 코드 구조 변경은 없다
4. **외형 다듬기 3건 (자산·씬 작업, 코드 변경 거의 없음)**
    - **`LobbyScene` 에셋 배치** — 배경·소품 배치로 로비를 꾸민다
    - **`MapSelectUI` 외형** — 스프라이트(`Images/MapSprites/map_sprite_{mapId}`)와 맵 이름(`Define.MapNames`) 둘 다 배선이 끝났으므로(2026-09-01 #10) 레이아웃과 **스프라이트 내용** 쪽이다 — `map_sprite_1`이 아직 Winchester 시안이면 Tenerife 것으로 갈아야 한다(파일명은 mapId 키라 그대로 둔다)
    - **`LoadingScene` 다듬기**
5. **`TenerifeScene` — 에디터 배치 작업 넷이 끝났고 테스트 전 확인만 남았다** — 씬 컴포넌트와 mapId 라우팅은 (2026-09-01 #10)에서 닫혔고, **① 지형지물 정렬·배치 → ② Object 덜어내어 최적화 → ③ Boundary 확정 + 투명 `BoundaryObject` 배치 → ④ 귀환 스팟 위치 확정·바인딩**이 전부 완료됐다. **매치 진입과 귀환까지 실측으로 확인됐으므로 Build Settings 등재는 통과했다.** 남은 확인은 **씬 내장 UI 12종(`IngameInventoryUI`·`IngameDragGhost`·`InteractUI`·`IngameHealthBarUI`·`IngameSettingUI`·`IngameKillLogUI`·`IngameWeaponUI`·`IngameStaminaBarUI`·`IngameEscapeCountdownUI`·`IngameTimeoutUI`·`IngameEscUI`·`IngameDamageIndicatorUI`)의 이름·활성 상태**(크로스헤어만 코드가 세우므로 예외). 배치 누락은 `BindSceneComponent<T>`가 콘솔에 원인을 띄우므로 **콘솔부터 볼 것**이며, **씬에는 활성 상태로 저장한다**(`GameObject.Find`가 비활성을 못 찾는다).
    - **씬을 `TestIngameScene.unity` 복제로 만들었다면 루트에 `TestIngameScene` 스크립트가 남아 있어도 증상 없이 정상 동작한다** — `SceneType`은 읽는 코드가 한 곳도 없고 인게임 배선이 전부 `is IngameScene`으로 걸려서, 맵별 분기를 `TenerifeScene`에 넣는 순간에야 "안 먹는다"로 드러난다. 착수 전에 붙어 있는 컴포넌트를 확인할 것
    - **차·버스 컨테이너 5종은 서버 스폰이라 씬에 배경으로 놓으면 두 벌이 된다** — 코드는 (2026-09-02 #2)에서 섰고 프리팹 5개도 만들어졌다(콜라이더 포함). **남은 것은 각 프리팹에 해당 컨트롤러 컴포넌트를 붙이는 것과 서버의 번호(4~8) 확정**이다 — 새 스크립트의 `.cs.meta`가 아직 없어 **Unity가 임포트하기 전까지는 붙일 수 없다.** 컴포넌트가 없으면 `ResourceManager`가 `GetComponent<GameObjectController>()`에서 null을 받는다. 배경용 차를 함께 쓸 거면 프리팹을 갈라둘 것
    - **Boundary는 코드 참조가 없는 순수 씬 콜라이더다**(`BoundaryCollider` 프리팹). **경계 근처 사격이 벽에 막히는 것은 확정된 의도다** — 콜라이더라 히트스캔이 그대로 걸리며 `ICombatTarget`을 구현하지 않아 맞아도 데미지는 없다. **서버는 경계를 모른다**(위치로 판정하는 것은 귀환의 `OUT_OF_ZONE`뿐이라 맵 경계 계약 자체가 없다) — 경계 이탈 처리를 서버에 기대지 말 것
6. **맵이 서서 착수 가능해진 후속 UI 둘** — ① **`IngameMapUI`** — `M` 키로 열고 대략적인 지도와 현재 위치를 표시 ② **`IngameCompassBarUI`** — 화면 상단에 상주하며 바라보는 방향의 방위(0~360)를 표시. **둘 다 씬 내장 UI가 되므로 `TenerifeScene`의 배치 대상이 12종에서 14종으로 늘어난다**(이름·활성 상태 규칙은 5번과 같다)
7. **적대 오브젝트 터렛 — 첫 `ICombatTarget` 비플레이어 구현이고 우선사항 1번을 푼다** — 전투 오브젝트가 없어 `[ObjectKill] name=…`을 밟을 수 없던 `TEMP:` 회수가 터렛이 서면 가능해진다(그때까지 `ObjectKillCount`는 항상 0이다). **착수 전에 서버 스폰인지 씬 배치인지 갈라야 한다** — HP·데미지·처치가 서버 권위여야 하므로 blueprint 스폰 쪽이 유력하고, 그러면 `Define.ObjectType`·`ObjectPaths` 등록과 **서버 계약 변경이 선행**된다(컨테이너들과 같은 경로). **`ICombatTarget`을 구현하지 않으면 `hit_object_id`에 `0xFFFFFFFF`가 실려 맞아도 아무 일이 없다.** 수신 측은 이미 준비돼 있다 — `D2CNotifyObjectKilled`(41)가 킬 기록과 `DespawnObject()`까지 한다
8. **`PlayerObject`·`OppoPlayer` 감정표현 (코드·자산·서버 계약 전부 없음)** — 착수 전에 **표현 수단**(캐릭터 3종이 전부 로봇이라 표정 리깅이 없을 가능성이 높다)과 **종류 수·키 배선**을 정해야 한다. **네트워크는 재장전 연출 통보(44/45)가 그대로 선례다** — 서버가 값을 해석하지도 보관하지도 않고 `object_id`만 채워 중계하는 양방향 unreliable이며, 절차는 `Network/CLAUDE.md`의 '새 UDP 패킷 타입 추가'에 있다. 그 선례를 따르면 제약이 함께 온다: **취소 통보가 없어 수신 측 연출이 스스로 끝나야 하는데, 에모트는 애니메이션이라 "다음이 오지 않으면 종료" 조건을 직접 만들어야 하고 빠뜨리면 취소된 모션이 영원히 돈다**

### 진행 고려사항

> 코드를 고칠 때 지켜야 할 제약·불변식은 **모듈 `CLAUDE.md`**(`Assets/Scripts/*/CLAUDE.md`)에 있다. 여기에는 **아직 코드가 없어 적을 자리가 없는 것**과 **오해를 막기 위한 관찰**만 남긴다.

- **재장전 방어 경로 ③은 미검증으로 남는다 (①②④는 정리됨)** — ① `DENY_VERSION_MISMATCH` 1회 재요청과 ② 낡은 스냅샷 폐기는 (2026-09-01 #2)에서 실측 통과했고, ④ `ACTION_PENDING_TIMEOUT` 워치독은 같은 항목에서 항구 방어 코드로 확정됐다. 남은 ③ `fire_sequence` 복구는 **되돌아간 시퀀스의 유일한 복구 경로**라 죽으면 그 무기의 발사가 계속 거부되는데, **자연 재현이 어려워 방어 코드로 둔다.** `[Action] … 응답 미수신 (3초)`가 반복되면 워치독이 아니라 **서버가 43번을 안 보내는 것**이므로 서버 쪽부터 볼 것
- **결과 씬 연출 건너뛰기는 자리만 열려 있다** — `GameResultSceneUI.CompleteImmediately()`가 그 지점이고 키 배선만 하면 된다. **Enter를 겸용하지 말 것** — 확인 버튼과 같은 키라 "연출을 건너뛰려다 로비로 나가는" 오조작이 된다. 전리품 28칸이 꽉 차면 대기가 약 6.3초인데, 길다고 느껴지기 전에는 붙이지 않는다
- **변경사항이 있는 로비 옵션창은 ESC만으로 닫히지 않는다 (버그 아님)** — ESC가 모든 층에서 '취소'라, 옵션창 ESC → "변경사항을 취소하시겠습니까?" → ESC(= 아니오) → 옵션창 유지가 반복된다. 닫으려면 그 팝업에서 확인을 누른다. **깨려면 ESC가 층마다 다른 뜻을 갖게 되므로 그대로 둔다**
- **로비 패널 전환은 나열형으로 남겨두기로 했다 (구조 개편 보류)** — 전이 함수마다 끌 패널을 나열하는 형태라 **패널이 늘 때마다 고칠 곳이 여섯**이고, 실제로 `UI_CharacterSelect`가 셋에서 빠져 (2026-09-02 #6)이 났다. 통합하려면 **`UI_Warehouse`가 두 상태 공유**라 '상태 → 패널 집합' 형태여야 하고, `EnterMatchingState`·`OnLogoutComplete`는 목표가 `UserState`로 표현되지 않아(`UI_MatchProcess`·`UI_Auth`) 그대로 흡수되지 않는다. 규칙은 `Scenes/CLAUDE.md`에 있다
- **컨테이너 열기 요청에 in-flight 가드는 넣지 않았다** — `ShowOpenedContainer()`가 멱등이 되어 정합성 문제는 없고, 남는 것은 응답 전 E를 다시 눌렀을 때 **헛 reliable이 한 장 더 나가는 것**뿐이다. 거슬리면 그때 붙일 것
- **컨테이너가 저절로 닫히는 것이 결함이 아닐 수 있다 (2026-08-29 #6)** — `0x0400` 거부에 `container_object_id`가 실리지 않아, 앞 컨테이너 대상 조작의 늦은 거부가 방금 연 컨테이너를 닫는다. **판별할 방법이 패킷에 없어 감수한 오작동**이고 복구는 다시 여는 것이다. 자주 나오면 그때 서버에 id 추가를 요청할 것
- **컨테이너 닫기는 서버 응답을 기다리지 않는다 — 기다릴 응답이 애초에 없다** — proto에 `D2CResponseCloseContainer`가 존재하지 않고 `C2DCloseContainer`(20)는 필드 없는 단방향 reliable이다. "응답 대기를 없애자"는 개선 항목으로 다시 올리지 말 것
- **탄약 칸에 무기를 떨구는 오조작을 클라가 미리 막지 않는다** — 서버가 `0x0040`으로 거부하고 **아무것도 바뀌지 않아** 동작에는 문제가 없다(검사가 모든 변경보다 앞이라 롤백할 로컬 상태도 없다). `IngameISlot.OnDrop`에 타입 검사를 넣으면 헛 요청이 줄지만 `CanReload` 문턱과 같은 성격의 UX 장치라 보류한다
- **R을 눌러도 재장전이 시작되지 않는 것은 버그가 아닐 수 있다** — 탄창이 가득이거나 예비탄이 0이면 `CanReload`가 진입을 막고 **알림음·경고가 없어 아무 반응도 나지 않는다**(확정). 문턱은 '확실할 때만 막는' 형태라 스펙 조회 실패·맨손에서는 막지 않고 서버에 맡긴다
- **409의 두 사유를 나눠 안내하려면 본문 `error.code`가 필요하다** — 상태 코드가 같아 `ERR_ALREADY_IN_GAME`과 `ERR_MATCH_ALREADY_SUCCESS`를 가를 수 없다. 안내를 묶기로 확정했으므로 지금은 파싱하지 않는다. **409 응답 본문 스키마가 명세에 없는 것은 미해결**이라 서버가 본문을 싣는지부터 확인이 필요하고, 상태 코드로만 가르는 현재 구조는 본문이 오든 안 오든 같은 결과를 낸다
- **구매 400 본문에 `error.code`가 실려 오는지 미확인 (동작은 성립)** — 실패 응답 본문 스키마가 명세에 없어 `ERR_SLOT_OCCUPIED`를 `OutOfSync`로 갈라내는 경로가 실측되지 않았다. **안 실려 와도 `ClassifyPurchaseBadRequest`가 `Rejected`로 흘리고 그쪽도 재조회를 하므로 결과가 거의 같다** — 차이는 안내 문구뿐이다. 확인 방법은 `[구매] 요청이 거부되었습니다. code=`가 비어 찍히는지 보는 것
- **`GET /api/items/inventory`는 `money`를 돌려주지 않는다** — 재조회로 인벤토리는 맞출 수 있어도 **표시 잔액은 갱신되지 않는다.** `Money`가 갱신되는 곳은 로그인·세션 유지·구매 성공 셋뿐이고, 로비에서 돈이 줄어드는 경로가 구매뿐이라 지금은 문제가 없다 — 판매가 붙거나 다른 세션이 재화를 건드리게 되면 그때 이 응답에 `money`를 실어달라고 요청할 것
- **계정 생성·게스트 로그인에는 아직 빈 본문·비-JSON 방어가 없다** — `PostCreateAccountCall`·`PostGuestLoginCall`이 여전히 `SendRequestAsync` + 무방비 `JsonUtility.FromJson`이라, 본문 없는 실패 응답이나 JSON이 아닌 본문(프록시 에러 페이지 등)이 오면 예외가 `async void` 호출부로 빠져나가 **팝업 없이 버튼만 굳는다.** 로그인은 (2026-08-31 #6), 로그아웃은 (2026-08-31 #7), 버전 확인은 (2026-09-01 #0)에서 닫혔고 남은 둘은 그런 응답이 실제로 오는지 확인되지 않아 보류한다
- **비밀번호에 특수문자가 필수인지는 서버에 확인되지 않았다** — `IsValidPassword`의 정규식은 영문+숫자만 요구하고 특수문자는 허용 문자일 뿐인데 안내 문구만 "특수문자 포함"이라 갈려 있었다. **검사가 아니라 문구를 사실에 맞췄다** — 정규식을 조이면 서버가 받아주는 비밀번호를 클라가 막게 되므로, 서버가 실제로 요구한다는 것이 확인되면 그때 lookahead를 더한다
- **세션 만료 안내는 다른 팝업이 떠 있으면 묻힌다 (감수)** — `ActiveOnlyConfirm`이 `isActive`면 아무것도 하지 않는다. **정리·전이가 팝업보다 먼저 끝나므로 상태는 안전하지만**, 확인/취소 팝업이 떠 있었다면 그것이 Auth 화면 위에 남고 확인을 누르면 `AuthState == None` 가드에 걸려 실패 안내가 뜬다. 기존 팝업을 강제 해제하는 해법은 콜백 부작용이 딸려와 넣지 않았다
- **resume 경로의 로비는 재화·인벤토리가 비어 보인다 (버그 아님)** — 서버 `/api/session/resume` 핸들러가 아직 뼈대라 `money: 0, inventory: []`를 반환한다. 서버 구현이 끝나기 전까지 정상이므로 클라 파싱 버그로 오해하지 말 것. **`http-api-spec.yaml`은 서버 사본과 동기화되어 있으므로 한쪽만 수정하지 말 것**
- **소리별 음량 밸런스는 `volumeScale` 인자로만 잡고 프리팹 Volume은 건드리지 않는다** — `PlayOneShotAt`이 매 재생마다 소스의 `volume`을 덮으므로 프리팹 값은 남지 않는다. **소스를 근거리·원거리로 나눠도 이 항목은 그대로 남는다** — 리스너가 가슴팍 바로 위라 두 커브 모두 **첫 키 이전 구간(볼륨 1.0)**이고, 내 총성과 내 발소리가 여전히 같은 크기다
- **큰 컨테이너(차·버스)는 상호작용 거리 판정과 부딪힐 수 있다 (실물 확인 전까지 보류)** — `PlayerController.CheckInteractable`이 거리를 **탄착점이 아니라 `hit.collider.transform.position`(콜라이더 원점) 기준 2m**로 재고, 서버도 3m로 다시 검사한다. 버스 옆에 바짝 서도 원점까지 2m를 넘으면 **프롬프트가 아예 안 뜬다.** `TenerifeBlueCar` 기준 **콜라이더 5개가 전부 루트에 붙어 있어 어디에 맞아도 원점이 같다** — 자식으로 흩어 놓으면 맞은 콜라이더 기준으로 재지므로 이것부터가 갈림길이다. 인게임에서 실제로 나오면 그때 고를 것 — ① 프리팹 피벗을 접근 지점(트렁크·문짝)에 두기(서버 계약 변경 없음, 유력) ② 상호작용용 작은 자식 콜라이더(①과 병행해야 서버 3m가 성립) ③ 오브젝트별 반경을 서버가 갖기(계약 변경)
- **총성 120m는 아직 실측 미검증이고 두 가지에 종속된다** — ① **Custom Rolloff 커브가 `Max Distance`에서 실제로 0에 닿는지**(닿지 않으면 그 너머로 고정 볼륨의 소리가 계속 들린다) ② **서버가 상태 스트림을 거리로 컬링하는지** — `IngameScene.HandleWeaponFireBroadcast`가 미스폰 발사자에서 return하므로 컬링 반경 밖 총성은 프리팹 값을 올려도 오지 않는다. **특정 거리부터 안 들리면 프리팹이 아니라 서버부터 볼 것**
- **`Resources/`는 참조 여부와 무관하게 전량 빌드에 포함된다** — 코드 참조가 없는 자산이 확인된 것만 여럿이다(`Prefabs/Scene/*` 4종, `System/@Managers`, `System/EventSystem`, `TestPlayer`, `TestLobbyScene/`, `LobbySettingUIBackup`). **`System/EventSystem`은 코드가 쓰는 `UI/EventSystem`과 같은 것 두 벌**이다. 알파 단계라 용량이 문제는 아니고 **삭제 판단은 사용자 몫이므로 사실만 남긴다** — 목록은 `Assets/Resources/CLAUDE.md`에 있다
- **씬 이름 컨벤션 잔여 (판단 보류)** — `TestIngame2.unity`는 컨벤션대로면 `TestIngame2Scene.unity`이지만 `Define.Scene` enum에 없어 코드 영향이 없고, `WinchesterAlpha.unity`는 맵 에셋 성격이다. **enum에 올리는 순간 파일 이름을 맞춰야 한다**(`Scenes/CLAUDE.md`의 씬 이름 규칙). 맵 명칭이 Tenerife로 확정됐으므로 `WinchesterAlpha.unity`의 처분(리네임·삭제·존치)도 함께 볼 것 — **코드 참조는 없다**
- **맵 리네임이 `http-api-spec.yaml`에도 반영됐다 (`1: MAP_TENERIFE`) — 서버 사본 전달은 사용자 몫이다** — 이 파일은 서버 사본과 동기화되므로 **클라에서만 고친 상태로 두면 두 사본이 갈라진다.** 다만 **계약은 mapId 값(1)이고 이름은 표기일 뿐이라 전달 전에도 통신에는 영향이 없다.** 같은 파일에 `latestVersion` 설명·예시(`"alpha-1"`) 동기화도 함께 들어 있다
