# 프로젝트 진행 상황

> 최종 수정: 2026-08-25
> 장르: 멀티플레이어 Extraction 게임 (알파 단계)
> 엔진: Unity 6000.4.0f1 / URP 17.4.0

---

## 완료된 것들

### 네트워크/UI
- [x] (2026-08-21 #4) 매치 이탈 출구 + 사망 유예 (server-sync T18·T15 일부·T10 수정) — **서버가 사망 후 5초 유예를 신설**(proto 공통 사항 6)하면서 사망 확정 신호가 `D2CNotifyPlayerKilled`로 바뀌고 **피해자에게도 오게 됐다**. 이에 맞춰 `HandleHealthChange`의 HP 0 감지를 걷어내고 사망 기점을 그쪽으로 일원화(두 패킷이 모두 도착해 기점이 이중화되던 문제). **사망·귀환 성공·연결 끊김 셋이 하나의 출구를 쓴다** — `BeginMatchExit(reason)` → `MATCH_EXIT_DELAY`(4초) → `CompleteMatchExit()`(UDP 종료). **4초는 서버 계약(5초)보다 짧게 잡은 값이며**(서버보다 먼저 정리 + ACK 시간 확보) 상수 주석에 근거를 남겼다. **ACK 함정 하나를 해소**: ACK는 다음 송신에 piggyback되는데 유예 중에는 상태 전송이 멈춰 송신이 하트비트뿐이고 주기가 3초라, 그냥 두면 통보 ACK가 최대 3초 늦어 4초 예산의 여유가 1초뿐이었다 → 이탈 시작 시 `SendHeartbeatNow()`로 강제 송신. 입력 차단은 `IsInputLocked` 하나로 모아 사격·시점·이동·조준/상호작용 판정과 `RequestXXX` 전부가 참조하게 했고, **이동은 입력만 끊고 중력은 유지**한다(공중에서 죽어도 시신이 뜨지 않게). **수신은 막지 않는다** — 브로드캐스트가 계속 오는 것이 관전 유지의 근거다. 연결 끊김 통보는 `UDPManager`의 수신 워치독 지점에 뒀다(`Disconnect()`는 재연결 직전 정리에서도 불려 접속마다 이탈 처리가 돌게 된다). 씬 정리는 `IngameScene.Clear()` 오버라이드가 맡아 `Managers.Clear()` 경로로 자동 실행된다. 귀환 취소는 `reason`별 분기 완료(`PLAYER_DEAD`는 사망 흐름에 양보, `SESSION_LOST`는 출구, 나머지는 재시도 허용). **결과 씬 전환과 사망 연출은 `TODO:`** — 지금은 4초 뒤 연결만 끊고 인게임에 머문다(테스트 종료는 당분간 강제 종료). **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-21 #5) 사망 탑뷰 연출 — `DeathCameraController` 신설(`Assets/Scripts/Controller/`, **독립 MonoBehaviour·프리팹 없음**). `Play(sourceCamera, deadPlayer)` 정적 호출이 런타임에 `@DeathCamera` 리그를 만들어, 원본 카메라의 fov·클리핑·컬링 마스크를 복사한 카메라를 같은 시점에 생성 → 원본 비활성 → 시신 위 +3의 `TopViewPoint`로 2초간 상승하며 시선을 시신으로 회전(smoothstep). **기존 카메라를 옮기지 않고 새로 만드는 것이 핵심** — 기존 카메라는 `PlayerController` 하위라 `ApplyViewRotation()`이 매 프레임 시점을 덮어써 보간이 되돌려진다. 처리한 함정 2건: ① **`AudioListener`를 옮기지 않는다** — 새 카메라에 붙이면 씬에 둘이 되어 경고가 뜨고, 그대로 두면 소리가 시신 위치에서 들려 연출과도 맞는다 ② **도착 시점 시선이 정확히 수직**이라 `LookRotation`의 up 힌트를 원래 보던 수평 방향으로 잡았다(forward와 up이 평행하면 회전이 튄다). 튜닝값(높이 3, 시간 2초)은 파일 상단 상수이며 **연출 시간은 유예 4초보다 짧아야 한다**. **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-23 #0) 게임 결과 데이터 저장 + 킬 카운트 (server-sync T15 데이터 준비) — `GameResult` 구조체·`Managers.Scene.LastGameResult`(`GameResult?`) 신설. `CompleteMatchExit()`이 이탈 사유·인벤토리(25슬롯 배치 유지 + 무기 2·방어구, **탄창 제외** — 판단 확정)·킬수 2종을 스냅샷해 저장한다. **스냅샷 시점은 유예 종료 후다** — 킬 통보가 사격보다 한 틱 뒤라 죽기 직전에 쏜 탄의 킬(상호 킬)이 유예 중에 도착하며, `BeginMatchExit`으로 당기면 그 킬이 빠진다. **킬수는 카운터 증가가 아니라 피해자 objectId `HashSet` 2개** — 수신 reliable에 중복 제거가 없어 재전송 통보가 두 번 디스패치될 수 있고, objectId 재사용 없음 계약 덕에 Set이면 멱등이다. 플레이어 킬은 `HandlePlayerKilled`에서 `killer == 내 objectId`로 판정(서버 권위, `_spawnCompleted` 가드 필수 — 초기값 0이 실재 objectId). **오브젝트 킬은 통보 패킷 자체가 없어**(`D2CNotifyDespawnObject`에 파괴자 정보 없음, 클라 추론은 오귀속 구멍 셋이라 기각) 서버가 `D2CNotifyPlayerKilled` 동형으로 브로드캐스트한다고 가정하고 `RecordObjectKill()`만 미리 작성(`TODO:`, 배선은 패킷 확정 후). `MatchExitReason`은 `GameResult`가 쓰도록 `IngameScene` 중첩에서 `SceneManagerEx.cs` 파일 스코프로 이동. `LastGameResult`는 `ResetLoadSceneOp()`에서 지우지 않는다 — 결과 씬 진입 초기화에서도 불려 소비 전에 날아간다. 인벤토리는 서버 재조회 없이 클라 로컬 상태 기준(표시용으로 감수 — 판단 확정). **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-23 #1) 오브젝트 처치 통보 반영 (proto 3차 변경 추적) — **서버가 `D2CNotifyObjectKilled`(41)를 신설했고, (#0)에서 가정했던 형태와 정확히 일치했다**(victim·killer objectId, 가해자 없음=0xFFFFFFFF, 무기 미탑재). 덕분에 미리 둔 `RecordObjectKill()`의 가드를 손대지 않고 `PacketHandler` 등록 + `Handle_D2CNotifyObjectKilled` + `IngameScene.HandleObjectKilled()`만 붙였다. **함께 바뀐 것이 더 중요하다** — `D2CNotifyDespawnObject`(39)의 의미가 좁아져 **처치로 인한 제거는 39가 아니라 41로 온다.** 41을 제거 신호로도 쓰지 않으면 파괴된 오브젝트가 맵에 영원히 남으므로 `HandleObjectKilled`가 `DespawnObject()`까지 책임진다. 39는 현재 서버가 보내는 경로가 없는 예약 패킷이 됐지만(컨테이너가 게임 종료까지 유지) 핸들러는 유지한다. 미스폰 킬러는 **요청하지 않는다** — 표시부가 없어 소비처가 없고 근처 플레이어는 상태 스트림이 채우므로 불필요한 reliable을 늘리지 않는다(오브젝트 킬 피드가 생기면 재검토). **막힌 곳이 두 번 있었다**: ① proto의 `enum PktId`에 41이 빠져 있어(주석은 "PktId 41"이라 명시) 추가했다 — 36~40에서도 있었던 같은 누락이며 서버측에서 확정 ② 생성 스텁이 메시지 클래스만 있고 `PktId` 멤버가 없는 반쪽 상태여서 배선을 `TEMP:`로 막아뒀다가, protoc 재실행 확인 후 해제했다. **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-24 #0) 게임 결과 씬 연결 + 세션 유지로 Login 스킵 (server-sync T15·T18 완결) — `CompleteMatchExit()`의 마지막 `TODO:`를 채워 매치 종료(사망·귀환·연결 끊김) → `GameResultScene` → 엔터 → 로비 복귀 흐름을 이었다. 전환은 잡큐로 예약해 다음 프레임에 수행한다(업데이트 루프 안에서 씬을 내리지 않는다). 결과 씬은 `LastGameResult`를 읽어 **아직 로그로만** 출력하고(표시 UI는 프리팹 대기, 아래 우선순위 3번) `Init`에서 `ResetLoadSceneOp()` → 엔터 리스너 등록, 복귀 시 `ClearGameResult()`로 소비를 확정한다. **로비 복귀에서 재로그인을 없앴다** — 서버 세션은 슬라이딩 TTL(1시간)로 살아 있고 클라의 `SessionId`/`AuthState`도 `Managers.Clear()`가 건드리지 않아 씬 전환 전체에서 생존하므로, `POST /api/session/resume`(신설, 서버 사본과 동기화)로 유효성만 확인하고 매치 결과가 반영된 재화·인벤토리를 재조회한다. `SceneManagerEx.IsReturnFromGameResult`로 결과 씬 경유와 게임 최초 시작을 구분하며, 이 플래그도 `LastGameResult`와 같은 이유로 **`ResetLoadSceneOp()`에서 지우지 않고** 소비자인 `LobbyScene.Init()`이 읽은 뒤 직접 리셋한다. 버전 체크(`GetVersionCall`)는 프로세스 재시작이 아니라 생략. 성공 시 `OnLoginComplete(AuthState 기준 Logined/Guest)`를 그대로 재사용해 별도 진입 경로를 만들지 않았다. **실패를 둘로 나눈 것이 이번 판단의 핵심이다** — `PostResumeSessionCall`이 `bool` 대신 `ResumeResult`(Success/Expired/Unreachable)를 돌려준다. 뭉뚱그리면 막다른 길이 생기기 때문이다: `PostLoginCall`은 `AuthState != None`이면 거부하므로 **네트워크 오류에서 인증 상태를 남긴 채 로그인 화면으로 보내면 로그인 자체가 막히고**, 그렇다고 지우면 멀쩡히 살아 있는 세션을 클라가 스스로 버린다. 그래서 `Expired`(HTTP 401 또는 200으로 감싼 본문 `code == 401`)만 `ClearAuthStateLocal()` + 폴백 확정이고, `Unreachable`은 재시도/취소를 묻는다(자동 재시도는 타임아웃 5초가 그대로 검은 화면이 되어 기각). 상태 코드를 얻기 위해 `SendRequestAsync`의 본체를 `SendRequestWithStatusAsync`로 옮기고 기존 함수는 본문만 꺼내는 래퍼로 남겨 **다른 호출부는 한 줄도 건드리지 않았다**. 폴백 안내는 `LobbyReconfirmUI.isActive` 때문에 **팝업 콜백 안에서 여는 다음 팝업이 조용히 무시되므로**, 만료 경로는 폴백을 먼저 수행하고 안내를 나중에 띄우며 재시도 취소 경로에서는 추가 팝업을 띄우지 않는다. `ClearAuthStateLocal()`은 로그아웃 성공 블록과 공유한다. `ShopItems`는 세션 중 불변이라 resume 응답에 실리지 않고 로그인 캐시를 유지한다. **Unity 컴파일·런타임 모두 미검증**
- [x] (2026-08-24 #1) 씬 이름 컨벤션 일관화 — `SceneManagerEx.GetSceneName()`이 enum 이름을 그대로 씬 이름으로 넘기는데 `Define.Scene.Lobby`·`GameResult`가 실제 파일(`LobbyScene.unity`·`GameResultScene.unity`)과 어긋나 있었다. `TestIngame`·`LoadingScene`만 우연히 이름이 맞아 지금까지 드러나지 않았고, **`Lobby`는 (#0)에서 처음으로 `LoadScene()` 대상이 되면서 노출됐다**(결과 씬 → 로비 복귀가 통째로 실패할 상태였다). **컨벤션 확정: 씬 파일 이름은 모두 `Scene`으로 끝내고 enum 항목 이름을 파일 이름과 일치시킨다.** 씬 담당 컴포넌트 클래스(`LobbyScene` 등)와 이름이 겹치지만 **enum 멤버는 항상 `Define.Scene.X`로 한정 접근되므로 충돌하지 않는다** — `LoadingScene`이 이미 같은 형태로 공존하며 컴파일되던 것이 선례다. 깨지는 경우는 `using static Define.Scene;`으로 멤버를 한정 없이 끌어다 쓸 때뿐이고 이 프로젝트는 쓰지 않는다. `Lobby`→`LobbyScene`, `GameResult`→`GameResultScene`, `TestIngame`→`TestIngameScene`으로 바꾸고 참조 전수(`Define.cs`·`LobbyScene.cs`·`GameResultScene.cs`·`TestIngameScene.cs`·`IngameScene.cs`·`HTTPManager.cs`) 확인. 근거는 `Define.Scene` 선언부 주석으로 남겼다. **`TestIngame.unity` 파일 리네임은 에디터 작업 대기** — 그 전까지 매치 진입이 씬을 찾지 못한다
- [x] (2026-08-24 #2) 로비 복귀 경로가 드러낸 잠복 결함 2건 + **첫 Unity 컴파일 검증** — 8/21 이후 쌓인 작업 전체가 여태 "컴파일 미검증"이었는데 이번에 **에러 0건**을 확인했다. 함께 런타임 에러 2건이 드러났고 **둘 다 이번 작업이 만든 게 아니라, 로비로 돌아오는 경로가 없어 여태 실행되지 않던 코드였다**(씬 이름 불일치까지 합쳐 같은 유형이 세 번째다). ① `InputManager.OnUpdate()` 순회 중 `InvalidOperationException` — 키 리스너가 씬 전환을 **콜백 안에서 동기적으로** 일으키면 `LoadScene()`의 `Managers.Clear()`가 순회 중인 `_keyActions`를 비운다. `GameResultScene.OnEnterInput`이 키 리스너에서 직접 씬을 내린 첫 사례였다(`QuitGameApplication`이 굳이 `ExecuteAtMainThread`로 감싸져 있던 것이 같은 함정을 피한 흔적). `MoveToLobby()`의 전환을 잡큐로 옮겨 해소 — 잡큐는 같은 `Managers.Update()` 안에서 `_input.OnUpdate()` **뒤에** 소진되므로 프레임 지연은 없다 ② 종료 시 `Managers.get_Input()` NRE — `OnApplicationQuit`이 `s_isQuitting`을 세우면 `Instance` 게터가 null을 반환하는데 `LobbyScene`·`GameResultScene`의 `OnDestroy`에 가드가 없었다. **`IngameScene`과 `PlayerController`에는 이미 같은 가드가 있었다** — 과거에 한 번 겪고 그 자리만 막은 흔적이며, 테스트 종료가 늘 "인게임에서 강제 종료"였던 탓에 로비 쪽은 드러날 기회가 없었다. `LobbyScene`은 early return 대신 입력 해제 블록만 감쌌다(그래야 `_cts.Cancel()`/`Dispose()`가 종료 경로에서도 실행된다)
- [x] (2026-08-24 #3) `InputManager` 디스패치 안전화 + 정리 — 위 ①의 근본 수정(방어층). **키 목록과 액션 목록을 재사용 버퍼로 스냅샷해 순회한다** — 이제 리스너가 콜백 안에서 씬 전환이나 구독 해제를 해도 예외가 아니라 "다음 프레임 반영"이 된다. **`Clear()`가 `_keyBuffer`를 건드리지 않는 것이 핵심 불변조건**이다(비우는 순간 열거 중인 리스트가 바뀌어 같은 예외가 되살아난다 — 주석으로 못박음). `Clear()` 이후의 남은 키들은 `TryGetValue` 실패로 건너뛰므로 죽은 씬의 리스너가 이어서 불리지도 않는다. **키 버퍼는 더티 플래그로 씬 로드당 1회만 재구축**한다 — 키 집합이 바뀌는 지점이 `AddKeyListener`의 새 키 추가와 `Clear()` 둘뿐이라 가능하다(`RemoveKeyListener`는 안쪽 리스트만 비우고 딕셔너리 항목은 남긴다). 매 프레임 복사는 최대 40바이트로 실측상 무의미했지만 이유 없는 일이라 없앴다. 액션 버퍼 복사는 **키 이벤트가 실제 발생한 프레임에만** 돌므로 유지. 분기 3벌이 똑같은 내부 루프를 갖고 있던 것을 `firedState` 하나로 합쳐 1벌로 줄였다(`else if`라 어차피 하나만 실행되던 구조). 그 외 ① `RemoveKeyListener`의 `Find(람다)` → 역순 for문 — 람다가 `action`·`state`를 캡처해 **호출마다 디스플레이 클래스 + 델리게이트를 할당**하고 있었다. `ContainsKey` + 인덱서 이중 조회도 `TryGetValue`로 정리 ② `DecrementCounter`에 CAS 언더플로우 가드 — 짝이 안 맞는 호출이 `errStack`을 음수로 만들면 **입력이 영구히 잠긴다**. 읽기는 `Volatile.Read`(CAS 루프에서 평범한 읽기는 JIT 호이스팅 시 무한 루프가 된다). 다만 `Increment`/`DecrementCounter`는 **아직 호출자가 0개**라 규약은 "짝이 안 맞으면 무시"로 잠정 확정한 상태다 ③ 이 파일만 CP949였던 것을 **UTF-8로 변환** — Edit 도구가 한글 주석을 깨뜨리던 유일한 파일이었다. 주석은 원문 복원 후 이관
- [x] (2026-08-25 #0) **매치 루프 첫 런타임 검증 (2인 접속)** — 8/21 이후 쌓인 작업 전체가 "런타임 미검증"이었는데 **2인 매칭 → 각자 탈출(귀환) 성공 → 결과 씬 → 로비에서 세션 유지 확인 → 두 클라이언트 재매칭**까지 한 바퀴가 정상 동작했다. 이 한 번으로 확정된 것: **T15 귀환 경로 전체**(2단계 프로토콜 → `BeginMatchExit(Recalled)` → 4초 유예 → `CompleteMatchExit()` → 결과 스냅샷 → 결과 씬), **T18의 출구 공유 구조**(Recalled 한정), **세션 유지 `Success` 경로**(`/api/session/resume` + `IsReturnFromGameResult` 소비, Login UI 스킵), (2026-08-24 #2·#3)의 입력 디스패치·씬 이름 수정, 그리고 선행 필수였던 **씬 리네임·Build Settings 등재**(에디터 작업). **가장 값진 것은 409 판단이 실증된 것이다** — "결과 씬 경유 + 로비 재진입이면 DB 반영이 끝나 있다"고 보고 재매칭 409를 다루지 않기로 확정했었는데, 재매칭이 실제로 통과했다. 2인 동시라 매칭 락 해제까지 함께 확인된 셈이다. **잠복 결함이 세 번 연속 나오던 '로비 복귀 이후' 구간이 이걸로 깨끗해졌다.** 남은 미검증은 **사망 경로 전체**(탑뷰 연출·킬 피드·유예 중 관전·킬 카운트), 연결 끊김 경로, resume 실패 2경로(`Expired`/`Unreachable`), 게스트 로그인, 종료 시 NRE 가드
- [x] (2026-08-25 #1) 스폰 시 실드 최대치 반영 (서버 계약 추가) — 서버가 **스폰 시점의 실드를 최대치로 준다**는 것이 확인됐는데 클라는 `_currentShieldPoint`가 0에서 시작해 재생 예측으로 채우고 있어, 매치 진입 직후 실드가 0으로 보이고 서버와 어긋났다. **HP처럼 필드 초기값으로 박을 수 없는 것이 이 작업의 제약이다** — 실드 최대치의 출처는 방어구 스펙(`_maxShieldPoint`)뿐이고 그건 `D2CFullInventorySync`가 도착해야 알 수 있다(HP는 `MAX_HEALTH_POINT` 상수라 초기값으로 끝난다). 그래서 `SyncHealthBarMax()`가 최대치를 채운 직후 일회성 `TryInitShield()`가 적용하고, 게이지 푸시는 바로 아래 기존 `SetArmor(_currentShieldPoint)`가 그대로 처리해 UI 코드는 건드리지 않았다. **`_itemLoaded` 가드가 핵심이다** — `SyncHealthBarMax()`는 인벤토리 도착 전에도 불리고 그때는 방어구가 null이라 최대치가 0인데, 가드가 없으면 **0을 '최대치'로 적용하면서 플래그만 소진돼 실드가 그 판 내내 0이 된다**. 반대로 `_spawnCompleted`는 **일부러 보지 않았다** — 캐릭터가 서기 전에 실드가 깎일 경로가 없고, 조건을 늘리면 도착 순서가 보장되지 않아 스폰 완료 지점에도 호출부를 둬야 한다(`TryInitWeapon()`이 두 조건을 다 보는 건 무기 장착이 `_playerController` 실체를 요구하기 때문이라 사정이 다르다). `ResetShieldPrediction()`은 그대로 뒀다 — "방어구 착용·해제·교체는 0에서 다시 찬다"는 기존 서버 규칙과 이번 규칙은 별개이며 **일회성 플래그가 둘을 가른다**(방어구 없이 스폰 후 나중에 착용하면 여전히 0에서 찬다). **Unity 컴파일·런타임 모두 미검증**

---

## 다음 작업 우선순위 (제안)

> **서버 변경분(2026-08-12 이후) 반영 작업은 별도 문서로 분리** — `server-sync-todo.md` (T1~T17) 참조.
> proto의 `[작업사항]` 주석을 추적한 임시 문서이며, 전 항목 반영 후 이 파일로 이관하고 삭제할 것.
> 아래 목록과의 대응: 3번(결과 표시 UI) = T15의 남은 표시부, 6번(워치독 제거 검토) = 3번에 종속

0. **사망 경로 런타임 검증 (2인 접속, 최우선)** — 귀환 경로는 (2026-08-25 #0)에서 한 바퀴 돌았지만 **사망 경로는 통째로 미검증이고, 지금 남은 미검증분 중 가장 크다.** 2인을 붙이는 비용이 가장 비싸므로 붙어 있는 동안 몰아서 할 것 — **전투 한 판이 T8·T9·T10·T11·T12·T18을 동시에 태운다**(싸우려면 무기를 들고, 맞고, 죽어야 한다). 순서: ① 진입 직후 HP·실드 만피 → 방어구 해제 시 0 (T11·T12, (2026-08-25 #1) 반영분) ② 1/2 키 전환이 한 박자 뒤에 반영 / 전환 대기 중 클릭이 안 나감 / 주무기 해제 시 보조무기가 손에 (T9·T9-a) ③ 상대 화면에서 내 손의 무기가 바뀌는지 (T8) ④ 피격 시 게이지가 서버값으로 점프 ⑤ 처치 시 킬러 화면 `[KillFeed]` (T10) ⑥ 피해자 화면 탑뷰 연출 + 4초 조작 차단 + **상대는 계속 움직이는지**(관전 유지) ⑦ `[MatchExit] 이탈 시작 (reason=Dead)` → 결과 씬 ⑧ 킬러 쪽 `킬수 — 플레이어: 1`. **③이 어긋나면 `weapon_dbid` 불일치로 사격이 통째로 조용히 무시되므로 ⑤가 안 나올 때 여기부터 볼 것.** 정상인데 오해하기 쉬운 것 둘 — `오브젝트: 0`(T14 대기)과 시신 컨테이너의 `매핑되지 않은 objectType=3` 에러(T13 대기)
1. **서버 변경분 반영 (`server-sync-todo.md`)** — T1~T11 완료(핸들러는 41번까지 전부 등록 완료 — proto의 `[작업사항]` 중 패킷 배선이 필요한 항목은 남은 것이 없다), T10은 데이터 경로가 끝나고 **UI 프리팹 대기**, T12는 **런타임 실측만 남음**, **T15·T18은 귀환 경로 한정으로 런타임까지 검증됐고**((2026-08-25 #0)) 표시 UI와 **사망 경로 검증**(위 0번)이 남았다. 남은 것은 T16·T17. **T13은 시신 프리팹 제작 대기(아래 2번), T14는 서버 목록 대기**. 전 항목 반영이 끝나면 proto의 `[작업사항]` 블록을 일괄 삭제할 것(proto 11~12행 지시) — 그 전에 지우면 잔여 작업 추적 근거가 사라진다
2. **시신(Corpse) 오브젝트 매핑 — 지금 새고 있는 결함** — `object_type=3`(Corpse)이 `Define.ObjectPaths`에 없어, 플레이어가 죽어 시신 컨테이너가 `D2CNotifySpawnObject`로 와도 `SpawnObject()`가 "매핑되지 않은 objectType=3" 에러만 남기고 반환한다. **시신이 보이지도 털리지도 않는다.** `Define.ObjectType`에 `Corpse`를 **`MaxCount` 앞에** 넣으면 값이 자동으로 3이 되며(현재 `MaxCount`가 3을 차지), `ObjectType.MaxCount`는 코드 어디서도 쓰이지 않는 것을 확인했으므로 4로 밀려도 안전하다. 컨테이너 조작은 일반 컨테이너와 동일하니 **프리팹만 생기면 코드는 두 줄**이다
3. **게임 결과 표시 UI** — 흐름(전환·엔터 복귀·세션 유지)은 끝났고 `GameResultScene.LogGameResult()`가 `Util.Log`로만 뿌린다. 데이터는 `Managers.Scene.LastGameResult`(이탈 사유 + 인벤토리 25슬롯 + 무기 2·방어구 + 킬수 2종)이며, **사유별 의미 차이를 표시할 것** — Recalled=반출 확정, Dead·ConnectionLost=잃은 것. 프리팹은 `Assets/Resources/Prefabs/UI/GameResultSceneUI/`에 자리가 잡혀 있고 `UI/CLAUDE.md` 규칙을 따른다. **전환은 반드시 `CompleteMatchExit()`을 거칠 것** — 우회하는 강제 전환은 결과가 저장되지 않는다. 탈출(귀환) 연출은 여전히 `BeginMatchExit()`에 자리만 비어 있다
4. **귀환 스팟 씬 배치** — 맵 씬에 귀환 단말기 오브젝트 배치, `RecallSpotController` 부착 후 인스펙터에서 `_recallSpotIndex`를 서버 테이블 값에 맞춤. 조준 레이가 맞아야 하므로 트리거가 아닌 일반 콜라이더 사용
5. **귀환 진행 중 UI 피드백** — 승인~결과 사이 5초 구간 표시(카운트다운 등). `InteractText`는 매 프레임 재조회되므로 `virtual` 프로퍼티화하면 동적 텍스트 전환 가능
6. **TEMP 워치독 2건 제거 검토** — 귀환(`RECALL_TIMEOUT`/`_recallTimer`)과 무기 교체(`SWITCH_WEAPON_TIMEOUT`/`_switchWeaponTimer`). 서버 통지 신뢰성이 실측으로 확인되면 제거. **귀환 쪽은 실처리가 붙었으므로 이제 검토 가능**하고, 무기 교체 쪽은 통보 유실 시 발사가 영구 차단되는 것을 막는 안전장치다
7. **킬 피드 UI 프리팹 준비 → 표시부 연결** — 데이터 경로는 완료(T10). `HandlePlayerKilled()`의 `TODO:` 자리에서 `Util.Log`를 표시부로 교체하면 되며, **내 사망 줄은 `HandleHealthChange` 쪽 경로라 함께 봐야 한다**
8. **발사 이펙트 프리팹 준비 → 이펙트 구현** — 로컬 탄착 이펙트(`ProcessHit`), 수신 측 머즐 플래시/총성/탄착 이펙트(`HandleWeaponFireBroadcast`). 파티클·사운드 에셋이 `Resources/Prefabs/` 아래에 필요
9. **탄약 차감 주석 해제** — `Fire()` 내 `magazine.quantity--` 및 빈 탄창 가드. 테스트 완료 후 활성화
10. **EmptyAmmoFire() 구현** — 빈 탄창 사운드, 재장전 유도 UI
11. **인벤토리 열기/닫기 키바인딩** — Tab키로 MyInventory 토글 등 추가 입력 연결 (컨테이너 E/I키 닫기, 무기 전환 1/2 키는 완료)
12. **실제 맵 씬에서 IngameScene 상속 완성** — `IngameScene`을 상속하는 맵별 씬 컴포넌트 구현
13. **설정값 실제 적용** — 해상도/창모드/FOV 변경이 `Screen.SetResolution()`, `Camera.fieldOfView` 등에 반영되도록 구현

---

## 메모

### 확인 필요
- **(2026-08-25) 세션 유지 — `Success` 경로만 검증됐다** — (2026-08-25 #0)에서 정상 경로는 확인됐고 **실패 분기 둘이 남았다. 이 분기가 (2026-08-24 #0) 판단의 핵심이었는데 정작 미검증이다.** ① 서버 중지 상태로 복귀 → `Unreachable`, 재시도/취소 팝업이 뜨고 취소 시 추가 팝업 없이 TestStart 플로우로 합류하는지 ② Redis 세션 삭제 후 복귀 → `Expired`, **폴백이 먼저 수행되고 안내가 나중에** 뜨는지(`LobbyReconfirmUI.isActive` 때문에 순서가 뒤집히면 안내가 조용히 삼켜진다) ③ 게스트 로그인으로 같은 루프 → Header가 Guest로 복귀하는지 ④ **일반 최초 실행 경로(게임 시작 → 로비) 회귀 확인**
- **(2026-08-25) 로비 복귀 구간은 정리됐다 — 종료 경로만 남았다** — 잠복 결함이 세 번 연속 이 경로에서 나왔었고(씬 이름 불일치, 입력 디스패치 중 컬렉션 변경, 종료 시 `Managers` null) 셋 다 "로비로 돌아가는 경로가 없어 여태 실행되지 않던 코드"였다. (2026-08-25 #0)의 **재매칭까지 통과해 이 구간의 회귀 위험은 걷혔다.** 남은 것은 **로그아웃 → ESC 종료** 하나 — (2026-08-24 #2)의 `OnDestroy` null 가드가 아직 실행된 적이 없다(2인 없이 혼자 가능)
- **(2026-08-24) `The referenced script (Unknown) on this Behaviour is missing!` 경고** — 도메인 리로드 직후 뜬다. 현재 열려 있는 씬의 오브젝트에 스크립트가 빠진 컴포넌트가 있다는 뜻이며, 아래 `Poolable` 항목과 증상이 일치한다. 새로 만든 `GameResultScene` 프리팹 쪽일 가능성도 있어 단정은 못 한 상태다(씬·프리팹 파일 확인 필요)
- **(2026-08-24) resume 경로의 로비는 재화·인벤토리가 비어 보인다 (버그 아님)** — 서버 `/api/session/resume` 핸들러가 아직 뼈대라 `money: 0, inventory: []`를 반환한다. 서버 세부 구현(`LinuxServerTest/` 세션 담당)이 끝나기 전까지는 정상이므로 클라 파싱 버그로 오해하지 말 것. **스펙 파일(`http-api-spec.yaml`)은 서버 사본과 동기화되어 있으므로 한쪽만 수정하지 말 것**
- **(2026-08-21) 탑뷰 연출에서 시신이 보이는지** — 컬링 마스크를 원본 카메라에서 복사하므로, **1인칭에서 자기 몸을 숨기려고 레이어를 빼뒀다면 탑뷰에서도 시신이 안 보인다.** 그 경우 마스크에 해당 레이어를 더하는 한 줄이면 된다. URP 후처리·전용 렌더러를 원본 카메라에 지정해뒀다면 그것도 연출 카메라에는 적용되지 않는다(`UniversalAdditionalCameraData`는 복사하지 않음)
- **(2026-08-21) T18 검증 → 위 우선순위 0번으로 이관** — 4초 유예·결과 씬 전환·`Clear()` 정리는 (2026-08-25 #0)의 귀환 경로로 확인됐고, **사망 경로 고유분**(탑뷰 연출·유예 중 관전·`reason=Dead`)만 남았다
- **(2026-08-25) 씬 이름 컨벤션 잔여** — `TestIngame2.unity`도 컨벤션대로면 `TestIngame2Scene.unity`이지만 `Define.Scene` enum에 없어 코드 영향이 없고, `WinchesterAlpha.unity`는 맵 에셋 성격이라 **둘 다 판단 보류**. enum에 올리는 순간 파일 이름을 맞춰야 한다(`Scenes/CLAUDE.md`의 씬 이름 규칙)
- **(2026-08-21) T10 검증** — 2인 접속. A가 B를 죽였을 때 **A 화면**에 `[KillFeed] 나 objectId=A(weaponId=N) → objectId=B(...)`, **B 화면**에는 이 패킷이 오지 않으므로 `[HealthChange]` 뒤에 내 사망 줄이 찍혀야 한다. 제3자 화면에는 양쪽 objectId와 킬러 무기. `weaponId`가 0이나 `미스폰`으로 나오면 T8의 무기 추적부터 볼 것
- **(2026-08-21) T9 컴파일 + 검증** — ① 1/2 전환이 **한 박자 뒤에** 반영되는지(예측 없음이라 정상) ② **주무기 해제 시 보조무기가 손에 들리는지**(T9-a. 서버와 어긋나면 사격이 통째로 무시된다) ③ 전환 대기 중 클릭이 발사되지 않는지 ④ 빈 슬롯 키에 패킷이 안 나가는지 ⑤ 버전 불일치 거부는 자연 재현이 어려우니 낡은 버전을 싣는 `TEMP:` 코드로 재동기화 경로를 한 번
- **(2026-08-21) T8 컴파일 + 2인 접속 검증** — ① A가 무기를 장착하면 B 화면에서 A 손에 무기가 생기는지(경로 1, 이번 작업의 실질 검증) ② A가 해제하면 B 화면 맨손 + **A 본인 화면도 맨손인지**(해제 버그 수정 확인) ③ 미스폰 플레이어가 있을 때 `C2DRequestSpawnByObjectId`가 **한 번만** 나가는지. 본인 롤백(경로 3)은 `C2DRequestSwitchWeapon`을 보내야 발생하므로 T9 전까지 검증 불가
- **(2026-08-25 갱신) 체력/실드 게이지 런타임 실측** — Fill 이미지 설정이 끝나 눈으로 확인만 남았다. **(2026-08-25 #1)로 기대값이 바뀌었다** — 순서: 매치 진입 직후 HP 만피 + **실드도 만피**(`[ShieldInit] 스폰 실드 최대치 적용 — N` 로그와 대조. 방어구 없이 스폰하면 `N=0`이 정상) → 방어구 **해제** 시 0 → 재착용 후 초당 100(=1%) 상승 → 피격 시 서버값으로 점프(`[HealthChange]` 로그와 대조). 통과하면 T11·T12를 함께 종료 처리한다. **어긋나면 원인은 이제 스크립트 쪽이다** — Editor 설정이 유일한 잔여 변수였으므로 남는 후보는 `SyncHealthBarMax()` 호출 시점과 실드 예측 누적 로직
- **(2026-08-23) `ObjectKillCount`는 `ICombatTarget` 보급 전까지 항상 0이다** — 패킷 배선(41)은 끝났지만, 전투 오브젝트가 `ICombatTarget`을 구현하지 않으면 `PlayerController`가 `hit_object_id`에 `0xFFFFFFFF`를 실어 보내 서버가 데미지를 넣지 않는다. **41번 검증은 아래 T14가 풀린 뒤에야 가능하다** — 그 전에 킬수가 0인 것은 정상이므로 핸들러 버그로 오해하지 말 것
- **(2026-08-23) 오브젝트 처치 검증** — T14 해소 후 2인 접속. 전투 오브젝트를 파괴했을 때 ① `[ObjectKill] 나 objectId=A(...) → objectId=N` 로그 ② **그 오브젝트가 화면에서 사라지는지**(41이 제거 신호를 겸하므로, 남아 있으면 `HandleObjectKilled`의 `DespawnObject()` 경로부터 볼 것) ③ 결과에 킬수가 잡히는지. **39번(`D2CNotifyDespawnObject`)은 현재 서버가 보내지 않으므로 그쪽 로그를 기다리지 말 것**
- **(2026-08-20) T14용 HP 보유 비플레이어 오브젝트 목록** — 서버 답변 대기 중. 이게 와야 `ICombatTarget` 구현 대상이 정해진다. **위 두 항목의 선행 조건이다**
- **(2026-08-20) `Poolable` 삭제 후 Unity 콘솔 확인** — MonoBehaviour라 프리팹·씬 오브젝트에 수동으로 붙어 있었다면 "Missing (Mono Script)" 경고가 뜬다. 코드가 붙이는 경로는 없었으므로 가능성은 낮다

### 서버 계약 (2026-08-25 — 스폰 실드)
- **플레이어가 처음 스폰될 때 실드는 최대치다.** 0에서 재생으로 차오르는 게 아니다
- **어떤 패킷으로도 오지 않는다** — 최대치의 출처가 방어구 스펙(`ArmorSpec.MaxShieldPoint`)뿐이라, 클라는 `D2CFullInventorySync`로 방어구를 알게 된 뒤에야 값을 채울 수 있다. HP 최대치(`MAX_HEALTH_POINT` 상수)와 사정이 다르다
- **방어구 착용·해제·교체는 여전히 0에서 다시 찬다.** 스폰과 규칙이 갈리므로 `TryInitShield()`의 일회성 플래그로 구분한다 — 한쪽 규칙으로 통일하지 말 것

### 서버 계약 (2026-08-23 proto 변경 3차 — 오브젝트 처치)
- **비플레이어 오브젝트의 제거 통보가 둘로 갈렸다.** 처치는 `D2CNotifyObjectKilled`(41), 그 외 사유는 `D2CNotifyDespawnObject`(39). **41이 제거 신호를 겸하므로** 이걸 받고 치우지 않으면 파괴된 오브젝트가 맵에 영원히 남는다
- **39는 현재 서버가 보내는 경로가 없다** — 컨테이너가 게임 종료까지 유지되어 처치 외 제거 사유가 없기 때문이다. 사유가 생길 때를 위한 예약이므로 핸들러는 지우지 말 것
- 41의 필드·규약은 `D2CNotifyPlayerKilled`와 동일하다(`0xFFFFFFFF`=가해자 없음, 무기 미탑재, 룸 전체 브로드캐스트). 피해자가 플레이어인 죽음은 여전히 40번이다
- **`enum PktId` 누락이 두 번째다** — 36~40에 이어 41도 메시지 주석에는 번호가 적혀 있는데 enum에는 빠져 있었다(서버측에서 확정). 새 패킷을 받을 때 **enum 등재 여부를 먼저 확인할 것**. 생성 스텁이 메시지 클래스만 있고 `PktId` 멤버가 없는 반쪽 상태도 실제로 발생했다

### 서버 계약 (2026-08-21 proto 변경 2차 — 사망 유예)
- **사망해도 세션이 즉시 끊기지 않고 5초의 유예가 있다.** 기점은 `D2CNotifyPlayerKilled.victim_object_id == 내 objectId`이며, 이 패킷은 **피해자에게도 온다**(구 계약에서 바뀜). 사망 판정을 HP 0으로 하면 기점이 이중화된다
- **유예 중 하향은 전부 계속 온다**(관전 유지 가능), **상향은 하트비트를 뺀 전부가 버려진다.** 유예 길이는 어떤 패킷으로도 오지 않아 클라 상수로 둘 수밖에 없다
- **자기 캐릭터의 디스폰 통보는 오지 않는다** — 유예 동안 남겨두고 스스로 치우라는 뜻
- 게임 결과·인벤토리 반영·매칭 락 해제는 **사망하는 순간 이미 시작**된다. 유예 중 연결을 끊어도 결과가 달라지지 않는다
- 귀환 성공 통보의 ACK는 **세션이 언제 끊기느냐만** 좌우한다. 인벤토리 반출은 성공 시점에 확정된다

### 서버 계약 (2026-08-21 proto 변경 1차 — 무기)
- **'장착한 무기'와 '손에 든 무기'는 다른 개념이다.** 무기 슬롯 2개는 장착이고 손에 든 것은 하나뿐이며, `C2DRequestWeaponFire.weapon_dbid`는 손에 든 쪽이어야 한다. 어긋나면 서버가 발사를 **조용히 버린다**(에러 응답 없음). 추적 출처가 넷이라 한 묶음으로 볼 것 — 상세는 `Scenes/CLAUDE.md`의 '손에 든 무기'
- **내 장착·해제로 손에 든 슬롯이 바뀌는 경우는 통보가 없다.** 클라가 서버 규칙을 직접 들고 있어야 하는 유일한 상태다
- `D2CNotifyWeaponChanged`의 `inventory_version`은 남의 통보에서 `0xFFFFFFFF`다. `0`은 실재하는 버전(세션 시작값)이라 미설정으로 읽으면 안 된다 — `attacker_object_id`와 같은 함정

### 서버 계약 (2026-08-20 확인)
- **objectId는 한 게임 안에서 단조 증가하며 재사용되지 않는다. 죽은 오브젝트가 살아나지도 않는다.** proto에 문서화된 보장은 아니다. `_despawnedObjectIds`를 만료 없이 씬 수명 내내 들고 가는 설계의 근거이며, 서버 계약이 바뀌면 이 설계를 다시 봐야 한다(증상은 "특정 오브젝트가 끝까지 안 보임")

### 해소된 버그
- **키 리스너 안에서 씬을 전환하면 입력 디스패치가 예외로 터지던 문제** — (2026-08-24 #2·#3)에서 해소. `LoadScene()`의 `Managers.Clear()`가 `InputManager`가 순회 중인 `_keyActions`를 비웠다. **씬 전환은 리스너 안에서 직접 하지 말고 잡큐로 미룰 것**(`Managers.ExecuteAtMainThread`). `InputManager`가 스냅샷 순회로 바뀌어 이제 예외는 나지 않지만, 잡큐 예약이 여전히 올바른 형태다
- **종료 시 `OnDestroy`에서 `Managers.Instance`가 null이던 문제** — (2026-08-24 #2)에서 해소. `OnApplicationQuit`이 먼저 돌아 `s_isQuitting`이 서면 `Instance` 게터가 null을 반환한다. **`OnDestroy`에서 `Managers.*`를 건드리는 모든 컴포넌트는 null 가드가 필요하다** — `IngameScene`·`PlayerController`에만 있고 로비 쪽에 없었던 것은, 테스트 종료가 늘 인게임에서 이뤄져 드러날 기회가 없었기 때문이다
- **`RequestSpawnIfUnknown()`의 자기 objectId 가드 누락** — (2026-08-21 #3)에서 해소. 나는 `_oppoPlayers`에 들어가지 않아, 킬러가 나인 킬 피드마다 **내 스폰을 서버에 요청**하게 된다. 기존 호출부 2곳이 호출 전에 자기 id를 걸러내고 있어 드러나지 않다가 세 번째 호출자에서 노출됐다 — 가드는 호출부가 아니라 헬퍼 안쪽에 있어야 한다
- **장착·해제 시 손에 든 무기가 어긋나던 문제** — (2026-08-21 #2)에서 해소. 두 번 틀렸다: 원래는 `currentWeapon != null`일 때만 `EquipWeapon`을 호출해 **해제해도 이전 무기가 손에 남았고**, (#1)에서 맨손으로 고친 것은 **서버 규칙(반대쪽 슬롯으로 옮김)과 어긋났다**. 서버가 보조무기를 들려준 상태에서 클라만 맨손이면 `weapon_dbid` 불일치로 사격이 통째로 무시된다. 이 경로는 **본인에게 통보가 오지 않아** 클라가 규칙을 직접 들고 있어야 한다
- **미스폰 objectId에 대한 스폰 요청 폭주** — (2026-08-21 #1)에서 해소. `UpdatePlayerStates()`가 10Hz로 새 reliable을 만들어 in-flight 32슬롯을 채우면 **무관한 다른 패킷이 덮어써져 조용히 유실된다**. 재전송 한도를 없앤 뒤(T2) 슬롯 상한을 지키던 장치가 없어져 생긴 노출이다. 새 통보 패킷을 붙일 때 `RequestSpawnIfUnknown()`을 쓰지 않고 직접 전송하면 같은 문제가 재발한다
- **디스폰된 플레이어의 유령 재스폰** — (2026-08-20 #1)에서 해소. 최초 조사 때는 `UpdatePlayerStates()`의 요청 억제 한 곳만 지목했으나, 실제로는 이미 보낸 요청의 늦은 응답이 `SpawnPlayerObject()`로 들어가는 경로가 더 위험해 **두 곳 모두** 막아야 했다

### 서버 변경분 반영 관련 (2026-08-19 조사)
- **`ExternalProtocol.cs`는 이미 새 proto 기준으로 재생성되어 있다** — `.gitignore` 대상이라 git diff에 안 잡히지만 `PktId` 35~40, `D2CNotifyWeaponChanged`, `C2DRequestSwitchWeapon`, `D2CNotifyHealthChange.AttackerObjectId` 스텁이 전부 존재. protoc 재실행 불필요
- **`0xFFFFFFFF`가 '없음', `0`은 실재 objectId** — `D2CNotifyHealthChange.attacker_object_id`, `D2CNotifyPlayerKilled.killer_object_id` 모두 proto3 기본값 0을 미설정으로 해석하면 오귀속이 된다
- **UDP 송신 경로는 전부 메인 스레드다** — 하트비트·재전송 모두 `Managers.Update()` → `UDPManager.OnUpdate()`에서 나가고, 워커 스레드는 미리 만들어진 바이트만 큐에서 꺼내 보낸다. 따라서 메인 스레드가 멈추면 수신 측 상태를 아무리 최신으로 유지해도 내보낼 주체가 없다. 수신 상태 갱신을 워커 스레드로 옮기는 최적화는 이 이유로 이득이 없다(T1에서 검토 후 기각)
- **남의 방어구·실드·HP는 어떤 패킷으로도 오지 않는다 (의도된 설계)** — `blueprint_id` 하나가 `ArmorSpec` 전체와 동치라 수치 노출이 되기 때문. 구 `D2CNotifyEquipmentChanged`의 `armor_id`가 삭제된 이유
- **HP 최대치도 서버가 보내지 않는다** — `MAX_HEALTH_POINT`(100000) 상수가 유일한 출처다. 이 때문에 `_currentHealthPoint`를 0으로 초기화하면 스폰 직후부터 사망으로 오판되어 실드 재생이 멈춘다(T12에서 발견). HP를 다루는 로직을 추가할 때 같은 함정을 주의할 것
- **실드 재생은 정수 누적으로 구현할 것** — 서버가 `(재생량 × 경과ms)`를 누적해 1000마다 1 회복하는 방식이라, 초당 실수 보간으로 바꾸면 값이 미세하게 어긋난다. 검증 지점이 피격 시점뿐이라 어긋남이 눈에 잘 띄지 않는다

### 귀환(Recall) 설계 확정 사항
- **2단계 프로토콜** — ① `C2DRequestRecall` → `D2CResponseRecall`(승인/거부) ② 승인 시 서버가 1초 간격 5회 검사 후 `D2CNotifyRecallResult`(성공/취소 + 사유)
- **사유 전달 범위가 단계별로 다르다 (의도된 설계)** — **요청 거부**(`D2CResponseRecall`)는 사유를 알리지 않고 bool만. **진행 중 취소**(`D2CNotifyRecallResult`)는 `RecallResultReason`으로 사유를 알린다
- **복수 요청 허용 안 함** — 서버가 멱등성으로 처리, 클라도 `IngameScene._recallRequested`로 막는다. 스팟별이 아닌 씬 단위 플래그여야 다른 스팟 재요청 경로가 막힌다. 요청/응답 대조는 `recall_spot_index` 에코로 충분
- **응답 도달 시점에 씬은 항상 `IngameScene`** — 로딩 중 도달은 논리적으로 불가하므로 핸들러의 씬 가드는 방어 코드일 뿐, 별도 pending 처리 불필요
- **귀환 스팟은 작은 프롭** — 상호작용 판정이 `hit.collider.transform.position` 기준 2m 고정(`PlayerController.CheckInteractable`)이라 넓은 트리거 존은 성립하지 않는다. 영역형으로 바꾸려면 `InteractDistance` virtual화 + `hit.point` 기준 거리로 판정부 리팩터링 필요
- **귀환 스팟은 서버 스폰 대상이 아님** — `ObjectData`에 인덱스 필드가 없으므로 맵 씬에 직접 배치하고 `[SerializeField] _recallSpotIndex`로 부여
- **"실패 시 서버가 반드시 알린다"는 전제 + 워치독** — 전제 자체는 유지하되, 전제가 깨졌을 때 복구 불가가 되지 않도록 클라에 타임아웃을 둔다. `SESSION_LOST`·`SERVER_INTERNAL`은 사유 자체가 통지 경로의 불안정을 가리키고, 서버가 이미 멱등 처리하므로 안전장치 비용은 사실상 0. 반대로 없을 때의 손실은 "그 판 탈출 영구 불가"로 최대치라 비용이 비대칭
- **알려진 한계** — 타임아웃 후 재요청 시 1차 시도의 늦은 응답을 2차 결과로 오인할 수 있다(`recall_spot_index` 에코만으로는 시도 구분 불가). 타임아웃을 10초로 넉넉히 잡아 실무상 회피 중. 엄밀히 하려면 `C2DRequestWeaponFire`의 `FireSequence`처럼 요청 시퀀스를 proto에 추가해야 함
