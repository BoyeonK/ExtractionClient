# 프로젝트 진행 상황

> 최종 수정: 2026-09-19
> 장르: 멀티플레이어 Extraction 게임 (알파 단계)
> 엔진: Unity 6000.4.0f1 / URP 17.4.0

---

## 완료된 것들

### 네트워크/UI

- [x] (2026-09-03 #0) **캐릭터 Description 3종 다듬기 — 문안 재작성과 설명 패널 확장** — `Define.CharacterDescriptions`의 스크랩·G-EXPlorer-04·프로메테우스 문안을 전면 재작성해, 문단을 짧게 끊고 마지막 한 줄에 결론을 두는 형태로 셋을 통일했다(데이터 교체이고 코드 구조 변경은 없다). 분량이 대략 두 배가 되면서 `UI_CharacterSelect` 프리팹의 `DescriptionPanel`도 680×500 → 820×670으로 함께 키웠는데, **`Description` 텍스트가 오토사이즈도 스크롤도 없는 Overflow라 문안만 늘리면 패널 밖으로 그대로 삐져나간다** — 코드와 프리팹이 짝으로 움직여야 한다는 이 종속을 `UI/CLAUDE.md`에 남겼다.

- [x] (2026-09-04 #0) **`IngameMapViewUI` — M으로 여는 전면 지도, 여는 순간 한 프레임만 촬영한다** — 탑뷰 카메라가 `MapTopView.renderTexture`에 그린 화면을 `RawImage`로 띄우는 구조라 **UI 쪽에 좌표 환산이 없다**: 플레이어 위치는 `ShownOnlyOnMap`(레이어 8)에 놓인 월드 마커가 이미지 안에 함께 찍혀 표현되고, 그래서 `Pcam`은 그 레이어만 빼고(`247`) 탑뷰는 `Terrain`(7)과 함께 담는다(`406`). **카메라는 평소 꺼져 있고 `Show()`가 한 프레임만 켠다** — 상시 렌더는 지도를 보지 않는 동안에도 매 프레임 씬을 한 번 더 그리기 때문이며, 그 대가로 **연 채로 움직이면 마커가 열었던 자리에 멈추는 것을 감수했다**(지도를 보며 이동하는 것이 의도가 아니다). `Camera.Render()`가 URP에서 지원되지 않아 `WaitForEndOfFrame` 코루틴으로 끄고 **`Hide()`가 카메라를 한 번 더 끄는 것은 그 코루틴이 오브젝트 비활성화로 죽어 카메라가 켜진 채 남는 경로 때문이며**, 키 라우팅은 **M이 다른 창이 하나라도 떠 있으면 무시**되게 해(`IsAnyUIOpen`) 지도가 어떤 UI와도 겹치지 않고 ESC 층·`ToggleMyInventory` 가드·`BeginMatchExit` 정리 목록에 모두 편입했다.

- [x] (2026-09-04 #1) **지도 마커 배치 — `PlayerObject`에 `MapPoint/BluePoint`가 붙어 지도가 코드·자산 양쪽으로 섰다** — 레이어 `ShownOnlyOnMap`(8)의 납작한 원반(월드 지름 8m, 플레이어 위 20m)이며 **콜라이더가 없어** `ICombatTarget` 탐색(`GetComponentInParent`)에 걸리지 않는다. **`Cast Shadows`는 껐다 — 게임 화면에서 그림자가 관측되지는 않았지만 카메라 컬링 마스크가 그림자까지 보장하지는 않아 선제로 막은 것이고**, 되돌리면 증상이 "플레이어 주변의 정체불명 원형 그림자"라 원인을 짚기 어렵다. 코드는 이 두 이름을 찾지 않으므로 `SoundPoint`·`MuzzlePoint`처럼 이름이 계약인 자리와는 갈린다.

- [x] (2026-09-04 #2) **설정 FOV 상한 90 → 80 · 맵 선택 초기값을 Tenerife로** — `SettingManager.SetFov`의 클램프를 좁혔는데, **로비 프리팹 `FovSlider`의 Max도 80으로 함께 맞춰야 한다**(아직 90이다) — 한쪽만 고치면 슬라이더로 넣은 값이 저장 시점에 잘려 **표시와 실제 화각이 갈리고 창을 다시 열면 값이 되돌아가 보인다.** 감도 슬라이더와 같은 성격의 짝이라 `Resources/CLAUDE.md`의 그 항목 옆에 FOV도 짝이라는 줄을 추가했다. `UI_MapSelect._selectedMapId`의 초기값은 `0` → `(int)Define.Map.Tenerife`이며, 숫자 대신 enum을 쓴 것은 맵이 추가되거나 순서가 바뀔 때 초기 선택이 조용히 다른 맵으로 넘어가는 것을 막기 위해서다.

- [x] (2026-09-18 #0) **`IngameCompassUI` — 눈금을 `Init`에서 만들고 시야 방위에 따라 strip을 통째로 민다** — 보이는 각도 폭 `SPAN_DEG`(120) 하나에서 `pxPerDeg`와 복제 패딩이 파생되며, 10도 작은 눈금·30도 큰 눈금 49개를 `Init`에서 찍은 뒤 매 프레임 `Strips`의 x만 쓴다(값은 `IngameScene.UpdateCompass()`가 `PlayerController.Yaw`로 민다 — 씬이 미는 다른 HUD와 같은 형태다). **범위가 0~360이 아니라 -60~420인 것이 이 구현의 요점이다** — 0↔360 되감기 프레임에 화면 양 끝이 비지 않으려면 **보이는 폭의 절반**만큼 양쪽에 복제해야 하고, 큰 눈금 간격(30)의 배수로 올리는 것은 복제 구간의 큰 눈금이 본 구간과 같은 자리에 와야 하기 때문이다. `pxPerDeg`를 `rect.width`가 바뀐 프레임에만 다시 잡는 것은 `Init()`이 `Awake` 경로라 그 시점 폭이 0일 수 있어서이며(한 번 재고 끝내면 **눈금 49개가 전부 중앙에 겹쳐 쌓인다**), **씬 배치와 중앙 기준 마커는 에디터 쪽 작업으로 남아 있다.**

- [x] (2026-09-18 #1) **`IngameCompassUI` 방위 라벨 N/E/W/S · 눈금 6px 하강** — 눈금 세로를 `TICK_OFFSET_Y`(-6) 하나로 내리고 큰 눈금 위에 방위 라벨을 코드로 붙였다(`Strips`의 y는 여전히 저작값이라 세로를 옮길 지점이 상수 한 곳으로 모여 있다). **라벨은 눈금 각도가 아니라 `deg - NORTH_DEG`(180)를 감은 값으로 방위를 정해 180도 자리가 N이 되는데**(270 E / 0·360 S / 90 W), 감지 않으면 복제 구간(-60·420)이 어느 방위에도 안 걸려 되감기 프레임에 라벨만 사라진다. 라벨을 해당 눈금의 **자식**으로 둔 것은 x를 눈금이 끌고 가 `LayoutTicks`가 따로 밀 것이 없기 때문이고, 이 기준이면 **월드 -Z가 북쪽이라 탑뷰 카메라 yaw 180(월드 +Z가 화면 아래)과 방위가 맞아떨어진다** — 실측은 씬 배치 뒤로 남는다.

- [x] (2026-09-18 #2) **`HostileNPC` 계층과 주도권 구동 배선 (프로토콜 제외)** — `targetId`가 가리키는 플레이어의 클라이언트가 구동하는 전투 NPC 계층(`HostileNPC`/`TurretNPC`)을 세우고, 씬이 **내가 주체인 objectId만** `_myHostileNpcIds`(HashSet)로 들고 매 프레임 `OnOwnedUpdate()`를 돌리게 했다 — 비주체 보간은 각자의 `Update()`가 맡고 둘은 `IsMine`으로 배타라 이중 구동이 구조적으로 불가능하다. **판정 권한은 전부 서버에 뒀다**: `aggro`는 캐시이고 `GetAggro`의 비교는 헛 요청을 줄이는 문턱이며, `_targetId` 대입은 `ApplyServerAuthority` 한 곳뿐이고 **클라에 주도권을 스스로 버리는 경로를 만들지 않았다**(재설정은 서버 몫, 워치독은 요청 잠금만 푼다). 씬에 `IsMyObjectId()`를 새로 둬 "**0이 실재 objectId라 스폰 전 비교가 성립하지 않는다**"는 조건을 접근자 안에 접었고(생으로 비교하면 `targetId == 0`인 NPC가 전부 내 것이 된다), 피격 방향 표시가 `_sceneObjects`까지 보게 해 NPC 가해자도 방향이 뜨게 했다 — 패킷 정의·채널은 별도 설계로 분리하고 이음매 셋(`RequestNpcAggro`/`ApplyNpcAuthority`/`ApplyRemoteState`)만 비워 뒀다.

- [x] (2026-09-18 #3) **적대 오브젝트 프로토콜(46~51) 배선 — 송신 넷 · 수신 셋** — 서버가 내려준 NPC 패킷 6종을 붙였다: 46·47·48(**전부 reliable**)이 aggro 절대 지정 · 주도권 이전 요청 · 공격 보고이고, 49·50·51이 주도권 통보 · 공격 연출 · 상태 스트림이다. **NPC 상태는 별도 패킷이 아니라 `C2DUpdatePlayerState.npc_states`에 얹혀 나가고**(서버가 항목마다 주도권을 확인해 어긋난 것만 버리므로 **부분 거부가 정상**), 51번이 룸 전체로 와 내 것도 돌아오므로 `ApplyRemoteState`에 `IsMine` 가드를 둬 **내 구동 결과가 한 틱 낡은 값으로 덮이는 되먹임**을 막았다. 계약에 맞춰 이음매를 `SendNpcAggro`(46)/`RequestNpcAuthority`(47)/`ReportNpcAttack`(48)로 갈랐고(46은 aggro 변경, 47은 주도권 요청이라 한 이름이면 헷갈린다), **`DecreaseAggro`로 MIN까지 내리는 것이 곧 반납 요청**이라는 계약을 모듈 문서에 반영했다 — 그래도 주도권은 통보가 오기 전까지 놓지 않는다.

- [x] (2026-09-19 #0) **터렛 조준 — 서버가 통보한 대상을 20Hz로 추적한다** — `TurretNPC`가 좌표(`_targetPos`)가 아니라 대상 참조(`_targetObj`)를 들게 했다: 대상이 움직이므로 좌표는 반드시 낡고, 갱신하려면 매 프레임 변경 함수를 불러 "값이 바뀔 때만 실행"이라는 `AimTargetChange`(주도권 통보 훅, 조준 전환 사운드가 붙을 자리)의 존재 이유가 무너진다. **`_targetObj`는 `_targetId`에서 유도한 캐시라 통보가 대상 스폰보다 먼저 오면 null이고 그때는 `AimTarget`이 다시 푼다**(주도권을 다시 알려주는 패킷이 없어 한 번만 풀면 그 포탑은 판 내내 조준하지 않는다) — 조회는 `IngameScene.FindCombatObjectTransform`을 public으로 올리고 **로컬 플레이어 가지를 더해** 셋(나·오포·씬 오브젝트)을 모두 보게 했다(로컬 플레이어가 어느 레지스트리에도 없어서, 빠뜨리면 정작 총을 쏘는 주체 클라에서만 조준이 죽는다). 조준은 주도권과 무관한 표현이라 `HostileNPC`에 `OnTargetedUpdate(float)` 훅을 새로 둬 주체·비주체 양쪽에서 돌리고(통보가 룸 전체에 오므로 각 클라가 스스로 대상을 찾아 돌릴 수 있어 **상태 스트림에 피치를 싣지 않아도 남의 화면에서 포신이 움직인다**) 회전은 자식 `Top/pointer`의 월드 회전만 건드리며(루트를 돌리면 비주체 쪽 `ProcessRemoteMovement`의 yaw Lerp와 싸운다), 주기는 **기반 클래스의 20Hz 게이트**(발소리·발사 타이머와 같은 `Mathf.Min` 상한 + `-=` 차감, `Init`에서 `Random`으로 위상 분산)가 재고 **tick 간격을 인자로 넘긴다** — 144Hz에서 NPC 대수만큼 헛 계산이 쌓이는 것을 막으려는 것이고, 구현부가 `Time.deltaTime`을 쓰면 프레임률이 높을수록 조준이 느려진다(`FixedUpdate`는 물리 전체의 주기라 쓸 수 없다). 실험은 `IngameScene` 없이 도는 `TestTurretNPC`(`HostileNPC` 상속을 끊은 독립 `MonoBehaviour`)와 x·y·z 주기가 서로 다른 왕복 표적 `TestObj`에서 했고, 프리팹은 **`ObjectPaths`가 찾는 이름과 어긋나 있던 것을 `GameObject/Turret`으로 통일**한 뒤(그대로 두면 로드 실패로 스폰이 통째로 건너뛰어진다) 조준 대상 `Top/pointer`와 `Top`의 `AudioSource`를 `Init()`에서 함께 잡게 했다 — 둘 다 이름이 계약이고 회전 속도·가청 거리는 코드가 아니라 프리팹이 정한다.

- [x] (2026-09-19 #1) **aggro 하한을 1로 내리고 피격 aggro의 소관을 서버로 확정** — `MinAggro`를 4 → 1로 내려 단계가 8칸이 됐다(스폰 초기값과 클램프 두 곳이 모두 이 프로퍼티를 읽어 값 하나만 바꾸면 되고, **MIN 지정이 곧 주도권 반납 요청이라 반납 지점도 함께 1로 옮겨간다**). 피격으로 생기는 aggro는 서버가 정하기로 확정돼 `ProcessHit`에 넣었던 `GetAggro(8)` 호출을 되돌렸다 — 클라가 피격마다 요청하면 판정이 두 곳이 되고, 요청값이 MAX면 **첫 한 발로 주도권이 굳어 아무도 뺏을 수 없으며 터렛이 이미 떠난 사람을 계속 조준한다**. 반영 경로가 통보(49) → `ApplyServerAuthority` 하나라는 것과, **그래도 `GetAggro`는 죽은 코드가 아니라는 것**(피격 외 적대 행동은 여전히 클라가 요청한다)을 모듈 문서에 남겼다.

---

## 진행 중 / 다음 할 것들

> **T번호는 과거 작업 참조용으로만 남아 있다.** T1~T18은 서버 변경분 반영 작업(`server-sync-todo.md`, 2026-08-27 삭제), T19~T24는 클라 작업이며 **전부 완료됐다**(T19 크로스헤어 / T20·T21 총알 궤적 / T22 빈 탄창 발사 모션 / T23 인게임 옵션 UI / T24 재장전·행동 잠금). 신규 작업에는 T번호를 새로 붙이지 않는다 — 완료 항목의 날짜 표기로 충분하다.

### 진행 우선사항

1. **`TEMP:` 회수 1건 — 오브젝트 킬 이름 로그 (대기)** — 워치독 둘은 (2026-09-01 #2)에서 항구 방어 코드로 승격돼 회수 대상이 아니고, 명중 로그는 (2026-08-31 #4)에서, 좌각/우각 임시 키는 (2026-09-01 #12)에서 회수했다. 남은 하나는 (2026-08-27 #7)에서 확인용으로만 실은 `killer_object_name`이며, **원복 지점이 `Handle_D2CNotifyObjectKilled`의 추출과 `IngameScene.HandleObjectKilled`의 파라미터 두 곳이라 한 쌍으로 되돌린다**(오브젝트 킬은 킬 피드 미적용이 확정이라 소비처가 생길 일이 없다). **실제로 처치 가능한 전투 오브젝트에 종속된다** — `HostileNPC` 계층은 섰지만 프리팹·프로토콜이 없어 아직 `[ObjectKill] name=…`을 밟을 수 없다(우선사항 4). 그전까지는 손댈 수 없는 대기 항목이다
    - **`TODO:` 귀환·상호작용의 행동 잠금 편입 범위 (미결)** — 귀환(`_recallRequested`)은 구조가 사실상 같아 그대로 흡수되고 **워치독도 하나로 합쳐진다.** 반면 **상호작용은 컨테이너가 열려 있는 동안 지속되는 상태라 `Pending`으로 잡으면 여는 내내 모든 행동이 잠긴다** — "요청~응답 구간만" 잠그고 열린 상태는 기존 `_isContainerOpen`/`_uiOpenCount`에 맡기는 쪽이 유력하다. 코드에는 `IngameScene.RequestRecall()` 위에 같은 `TODO:`가 걸려 있다
2. **나침반·지도 마무리 — 둘 다 코드는 섰고 남은 것이 에디터 작업과 인게임 확인이다**
    - **`IngameCompassUI` 씬 배치 + 중앙 기준 마커** — `TenerifeScene`의 배치 대상이 13종에서 14종으로 늘어난다(이름 고정·활성 저장, 누락은 `BindSceneComponent<T>`가 콘솔에 띄우므로 **콘솔부터 볼 것**). **지금 프리팹에는 "어느 눈금이 현재 방위인가"를 가리키는 것이 없어** 화면 정중앙을 암묵적 기준으로 읽어야 하며, 고정 요소이므로 `Strips`가 아니라 `ViewportArea` 직계에 놓는다(`Strips` 아래 것은 눈금과 함께 흘러간다). 방위 라벨은 (2026-09-18 #1)에서 붙어 절대 방위는 읽을 수 있고, **30도 눈금의 숫자 라벨은 붙이지 않았다** — 필요해지면 그때 판단할 것
    - **지도에 남은 것은 인게임 확인과 마커 높이 판단 둘이다** — 확인의 핵심은 닫은 뒤 **커서가 다시 잠기고 사격이 되는지**(`_uiOpenCount` 짝이 맞는지)다. 높이 쪽은 **마커가 플레이어 위 20m라 Perspective에서 화면 위치가 `x/(200−20)`이 되어 지도 중심에서 바깥으로 약 11% 밀려 찍히는 것**을 그대로 둘지의 문제이며(중심에서 100m면 111m 자리), 낮추면 오차가 1% 미만이 되는 대신 실내·지붕 아래에서 마커가 가려진다. **카메라 실측이 마커 추가보다 앞이라 아직 안 본 항목이다**
    - **방위 기준은 "월드 -Z = 북"으로 정해졌다**(`NORTH_DEG` 180). 나침반은 여전히 월드 yaw(+Z = 0°)를 그대로 받고 라벨만 180 어긋나게 읽으며, **탑뷰 카메라 yaw 180 때문에 지도에서도 월드 -Z가 화면 위**라 둘이 일치한다 — 지도에 방위 표기를 붙인다면 화면 위가 N이다
3. **외형 다듬기 3건 (자산·씬 작업, 코드 변경 거의 없음)**
    - **`LobbyScene` 에셋 배치** — 배경·소품 배치로 로비를 꾸민다. 배치용 프리팹은 `Resources/Prefabs/Scene/LobbySceneContent/`에 모으고 있다
    - **`MapSelectUI` 외형 — 남은 것은 레이아웃뿐이다** — 스프라이트·맵 이름 배선이 끝난 데 더해(2026-09-01 #10) `map_sprite_1`이 Tenerife 시안으로 교체됐고 선택창의 초기 선택도 Tenerife다(파일명은 mapId 키라 그대로 둔다)
    - **`LoadingScene` 다듬기**
4. **적대 NPC(터렛) — 계층·프로토콜·조준·프리팹이 섰고 남은 것은 사격이다** — (2026-09-18 #2·#3)에서 계층과 패킷 46~51이, (2026-09-19 #0)에서 조준과 프리팹 등재가 닫혔다. 남은 것:
    - **`Resources/Prefabs/GameObject/Turret` 프리팹이 생겼다**(관례대로 프리팹 이름 = `ObjectType` 항목 이름이고 클래스 접미사를 붙이지 않는다 — `Turret` ↔ `TurretNPC`). **하위 경로 둘이 이름째로 계약이다**: `Top/pointer`(조준 회전 대상)와 `Top`의 `AudioSource`(월드 소리 소스) — 없으면 `BindComponent`가 에러를 남기고 그 기능만 죽는다. 회전 속도 `_aimSpeed`(`[SerializeField]`, 초당 도)와 가청 거리·Rolloff는 **코드에 값이 없고 프리팹이 정한다**. **하위에 붙은 콜라이더는 무엇이든 피격 부위가 된다**(`GetComponentInParent`). 총구가 생기면 `MuzzlePosition`을 override해 궤적 원점을 옮길 것
    - **전투 동작 수치** — `TurretNPC.FindTarget`/`TryAttack`이 `TODO:`로 비어 있다. 사거리·시야·연사 간격이 설계되지 않았고, **보고는 `ReportAttack(hit, hitPoint)` 한 줄이면 서버까지 닿는다**. 사거리·시야 판정은 `OnOwnedUpdate` 쪽이라 **매 프레임이므로**, 레이캐스트를 넣을 때 조준과 같은 20Hz로 묶을지 함께 볼 것
    - **피격 외 aggro 계기가 미설계다** — 피격은 서버 소관으로 확정됐고, 클라가 `GetAggro`로 요청할 계기(근접·소음·시야 등)는 정해지지 않았다. 판정이 **비주체 NPC에도 필요하므로**(주도권을 뺏으려는 요청이다) `Update()`의 20Hz 틱이 얹을 자리이고, 재료는 이미 있다 — 로컬 플레이어 위치·`MovementState`의 RUN·`IsShooting`
    - **조준 전환 사운드(`AimTargetChange`의 `TODO:`)** — 소스(`Top`의 `AudioSource`)는 `Init`에서 잡아 뒀고 남은 것은 클립과 재생 호출이다. 룸 전체가 받는 통보에서 불리므로 2D `Play()`가 아니라 `PlayOneShotAt(path, _soundAudio)`여야 한다
    - **서버 주도 NPC의 이동 정보 경로는 아직 없다** — `D2CUpdateNpcStates`에 서버 주도 오브젝트가 실리지 않는다(서버 계약). 터렛은 움직이지 않아 문제가 없고, **움직이는 HostileNPC가 추가될 때 그 경로를 함께 설계한다**
    - **`ICombatTarget`을 구현하지 않으면 `hit_object_id`에 `0xFFFFFFFF`가 실려 맞아도 아무 일이 없다.** 수신 측은 이미 준비돼 있다 — `D2CNotifyObjectKilled`(41)가 킬 기록과 `DespawnObject()`까지 한다
5. **사망 연출 — 충돌을 모두 끄고 사망 모션 재생 (로컬·오포 공통)** — 죽는 순간 해당 캐릭터의 **모든 콜라이더를 끄고 사망 모션을 재생**한다. **충돌을 끄는 쪽은 보기 문제가 아니다** — 오포는 `BuildHitboxes`가 본에 붙인 캡슐 11개가 `ICombatTarget` 히트스캔에 그대로 걸려서, 끄지 않으면 **시신이 총알을 계속 먹고 이미 죽은 `objectId`가 `hit_object_id`에 실린다**(로컬 쪽은 `CharacterController` 하나뿐이다). **착수 전에 갈라야 할 것은 오포의 수명이다** — 내 캐릭터는 `BeginMatchExit(Dead)`의 `MATCH_EXIT_DELAY`(4초) 유예 안에서 재생하면 되고 `DeathCameraController`가 이미 그 창을 쓰고 있지만, 남의 캐릭터는 킬 통보(`D2CNotifyPlayerKilled`)와 제거(`D2CDespawnPlayerObject`)가 **별개 패킷이라 모션이 끝날 때까지 오브젝트가 남는다는 보장이 없다.** 애니메이터는 양쪽 다 `_anim`으로 이미 잡혀 있어 배선보다 **사망 클립 자산과 전이 조건**이 먼저다
6. **`PlayerObject`·`OppoPlayer` 감정표현 (코드·자산·서버 계약 전부 없음)** — 착수 전에 **표현 수단**(캐릭터 3종이 전부 로봇이라 표정 리깅이 없을 가능성이 높다)과 **종류 수·키 배선**을 정해야 한다. **네트워크는 재장전 연출 통보(44/45)가 그대로 선례다** — 서버가 값을 해석하지도 보관하지도 않고 `object_id`만 채워 중계하는 양방향 unreliable이며, 절차는 `Network/CLAUDE.md`의 '새 UDP 패킷 타입 추가'에 있다. 그 선례를 따르면 제약이 함께 온다: **취소 통보가 없어 수신 측 연출이 스스로 끝나야 하는데, 에모트는 애니메이션이라 "다음이 오지 않으면 종료" 조건을 직접 만들어야 하고 빠뜨리면 취소된 모션이 영원히 돈다**

### 진행 고려사항

> 코드를 고칠 때 지켜야 할 제약·불변식은 **모듈 `CLAUDE.md`**(`Assets/Scripts/*/CLAUDE.md`)에 있다. 여기에는 **아직 코드가 없어 적을 자리가 없는 것**과 **오해를 막기 위한 관찰**만 남긴다.

- **aggro 범위 1~8은 서버와 손으로 맞추는 값이다 (미확인)** — proto는 "서버가 `[MIN, MAX]`로 클램프한다"고만 하고 숫자를 명시하지 않는다(`ExternalProtocol.cs:11727`). 서버 MIN이 4로 남아 있으면 클라가 주장한 1~3이 조용히 잘리고 **증상은 "반납했는데 주도권이 안 돌아간다"**다. `MAX_HEALTH_POINT`와 같은 성격의 종속이라 값을 옮길 때는 서버 쪽부터 확인할 것
- **터렛 조준이 20Hz라 계단이 보일 수 있다 (동작 문제는 아니다)** — 90°/s에서 tick당 4.5°씩 꺾이므로 144Hz에서는 7~8프레임 정지 후 도약으로 보인다. 거슬리면 2층으로 나눈다 — **목표 회전(`LookRotation`)만 20Hz로 갱신하고 그쪽으로 다가가는 `RotateTowards`는 매 프레임** 돌리는 형태이며, 앞으로 붙을 사거리·시야 레이캐스트가 20Hz에 남아 비용의 대부분은 그대로 준다. 실험용 `TestTurretNPC`에서 먼저 볼 수 있다
- **`AIM_TARGET_Y_OFFSET`(조준 높이 1.0)이 `TurretNPC`와 `TestTurretNPC` 두 곳에 있다** — 공용 파일을 만들지 않기로 했으므로 **실험에서 높이를 조정하면 본체도 함께 고칠 것.** 값의 근거는 대상 루트 피벗이 발밑이고 `PlayerObject` 스케일이 2라 가슴이 그 부근이라는 것이다
- **NPC 주도권 통보(49)에 순서 방어가 없다 — 알고 미룬 것이다** — proto 주석은 헤더 `rSeqNum`으로 낡은 통보를 버리라고 하지만 `HandlerFunc`가 `(ReadOnlySpan<byte>)`만 받아 핸들러가 시퀀스를 볼 수 없고, **델리게이트를 바꾸면 핸들러 28개가 전부 영향을 받는다.** 대신 **상태 전송에 실리는 오브젝트 정보로 서버가 불일치를 잡아 원복시키는 로직을 예정**하고 있으며, 그 전이라도 역전이 성립하는 경우의 수가 매우 한정적이라 감수한다(사용자 판단). 증상이 나온다면 "주도권이 잠깐 되돌아간 것처럼 보임"이다
- **맵 경계는 클라 콜라이더로만 존재한다 — 서버는 경계를 모른다** — `TenerifeScene`의 투명 `BoundaryObject`가 이탈을 막는 전부이므로 **경계 이탈 처리를 서버에 기대지 말 것**(알려오는 패킷이 없다). **경계 근처 사격이 여기에 막히는 것은 확정된 의도다** — 콜라이더라 히트스캔이 그대로 걸린다
- **지도 그림의 외형 선택지 셋은 열어둔 채로 뒀다 (동작에는 문제가 없다)** — ① `MapTopView`가 480×270이라 1500×808 표시 영역에서 3.1배 업스케일로 흐리다(1회 촬영이라 해상도를 올려도 비용은 그 한 프레임뿐이다) ② `Clear Flags`가 Skybox라 지형 밖이 하늘로 채워져 프리팹의 남색 배경이 지도 영역에 보이지 않는다 ③ `TopViewCamera`에 `AudioListener`가 꺼진 채 붙어 있어 **켜지면 씬에 리스너가 둘이 되어 경고가 뜨고 소리가 지도 카메라 위치에서 난다**
- **재장전 방어 경로 ③은 미검증으로 남는다 (①②④는 정리됨)** — ① `DENY_VERSION_MISMATCH` 1회 재요청과 ② 낡은 스냅샷 폐기는 (2026-09-01 #2)에서 실측 통과했고, ④ `ACTION_PENDING_TIMEOUT` 워치독은 같은 항목에서 항구 방어 코드로 확정됐다. 남은 ③ `fire_sequence` 복구는 **되돌아간 시퀀스의 유일한 복구 경로**라 죽으면 그 무기의 발사가 계속 거부되는데, **자연 재현이 어려워 방어 코드로 둔다.** `[Action] … 응답 미수신 (3초)`가 반복되면 워치독이 아니라 **서버가 43번을 안 보내는 것**이므로 서버 쪽부터 볼 것
- **결과 씬 연출 건너뛰기는 자리만 열려 있다** — `GameResultSceneUI.CompleteImmediately()`가 그 지점이고 키 배선만 하면 된다. **Enter를 겸용하지 말 것** — 확인 버튼과 같은 키라 "연출을 건너뛰려다 로비로 나가는" 오조작이 된다. 전리품 28칸이 꽉 차면 대기가 약 6.3초인데, 길다고 느껴지기 전에는 붙이지 않는다
- **변경사항이 있는 로비 옵션창은 ESC만으로 닫히지 않는다 (버그 아님)** — ESC가 모든 층에서 '취소'라, 옵션창 ESC → "변경사항을 취소하시겠습니까?" → ESC(= 아니오) → 옵션창 유지가 반복된다. 닫으려면 그 팝업에서 확인을 누른다. **깨려면 ESC가 층마다 다른 뜻을 갖게 되므로 그대로 둔다**
- **로비 패널 전환은 나열형으로 남겨두기로 했다 (구조 개편 보류)** — 전이 함수마다 끌 패널을 나열하는 형태라 **패널이 늘 때마다 고칠 곳이 여섯**이고, 실제로 `UI_CharacterSelect`가 셋에서 빠져 (2026-09-02 #6)이 났다. 통합하려면 **`UI_Warehouse`가 두 상태 공유**라 '상태 → 패널 집합' 형태여야 하고, `EnterMatchingState`·`OnLogoutComplete`는 목표가 `UserState`로 표현되지 않아(`UI_MatchProcess`·`UI_Auth`) 그대로 흡수되지 않는다. 규칙은 `Scenes/CLAUDE.md`에 있다
- **컨테이너 열기 요청에 in-flight 가드는 넣지 않았다** — `ShowOpenedContainer()`가 멱등이 되어 정합성 문제는 없고, 남는 것은 응답 전 E를 다시 눌렀을 때 **헛 reliable이 한 장 더 나가는 것**뿐이다. 거슬리면 그때 붙일 것
- **컨테이너가 저절로 닫히는 것이 결함이 아닐 수 있다 (2026-08-29 #6)** — `0x0400` 거부에 `container_object_id`가 실리지 않아, 앞 컨테이너 대상 조작의 늦은 거부가 방금 연 컨테이너를 닫는다. **판별할 방법이 패킷에 없어 감수한 오작동**이고 복구는 다시 여는 것이다. 자주 나오면 그때 서버에 id 추가를 요청할 것
- **컨테이너 닫기는 서버 응답을 기다리지 않는다 — 기다릴 응답이 애초에 없다** — proto에 `D2CResponseCloseContainer`가 존재하지 않고 `C2DCloseContainer`(20)는 필드 없는 단방향 reliable이다. "응답 대기를 없애자"는 개선 항목으로 다시 올리지 말 것
- **탄약 칸에 무기를 떨구는 오조작을 클라가 미리 막지 않는다** — 서버가 `0x0040`으로 거부하고 **아무것도 바뀌지 않아** 동작에는 문제가 없다(검사가 모든 변경보다 앞이라 롤백할 로컬 상태도 없다). `IngameISlot.OnDrop`에 타입 검사를 넣으면 헛 요청이 줄지만 `CanReload` 문턱과 같은 성격의 UX 장치라 보류한다
- **R을 눌러도 재장전이 시작되지 않는 것은 버그가 아닐 수 있다** — 탄창이 가득이거나 예비탄이 0이면 `CanReload`가 진입을 막고 **알림음·경고가 없어 아무 반응도 나지 않는다**(확정). 문턱은 '확실할 때만 막는' 형태라 스펙 조회 실패·맨손에서는 막지 않고 서버에 맡긴다
- **409의 두 사유를 나눠 안내하려면 본문 `error.code`가 필요하다** — 상태 코드가 같아 `ERR_ALREADY_IN_GAME`과 `ERR_MATCH_ALREADY_SUCCESS`를 가를 수 없다. 안내를 묶기로 확정했으므로 지금은 파싱하지 않는다. **409 응답 본문 스키마가 명세에 없는 것은 미해결**이라 서버가 본문을 싣는지부터 확인이 필요하고, 상태 코드로만 가르는 현재 구조는 본문이 오든 안 오든 같은 결과를 낸다
- **실패 응답 본문에 `error.code`가 실려 오는지 미확인 (동작은 성립)** — 본문 스키마가 명세에 없어, 매매 400에서 구매의 `ERR_SLOT_OCCUPIED`와 판매의 `ERR_SLOT_EMPTY`·`ERR_ITEM_MISMATCH`를 `OutOfSync`로 갈라내는 경로가 실측되지 않았다. **안 실려 와도 `Classify…BadRequest`가 `Rejected`로 흘리고 그쪽도 재조회를 하므로 결과가 거의 같다** — 차이는 안내 문구뿐이며, 확인 방법은 `[구매]`/`[판매]`/`[매칭] 요청이 거부되었습니다. code=`가 비어 찍히는지 보는 것이다. **매치 시작 409만 결이 다르다** — `ClassifyMatchStartConflict`가 `AlreadyInMatch`를 갈라내는 유일한 근거라, 본문이 비어 오면 그 갈래가 영영 안 밟히고 이미 매칭 중인 사용자에게 "인벤토리 정보가 갱신되었습니다"가 뜬다(잘못된 안내이되 막다른 길은 아니다)
- **FREE 매치 시작이 `OutOfSync`로 실패하면 장비칸이 다시 채워질 수 있다 (버그 아님)** — 재조회가 슬롯 셋을 서버 상태로 덮으므로, 방금 비웠던 인벤토리·로드아웃이 되살아나 다시 "장비칸과 인벤토리를 비워주세요"에 걸린다. **서버가 권위이고 사용자는 창고로 옮긴 뒤 다시 누르면 되므로 그대로 둔다** — 여기서 로컬 배치를 보존하면 스냅샷 불일치가 반복된다
- **로드아웃(`LSlot`) 판매를 클라가 막았지만 서버 쪽 일이 다 없어진 것은 아니다** — 매치 시작 요청과 판매가 같은 로드아웃을 두고 경합하는 트랜잭션을 피하려고 막았는데(사용자 판단), **로드아웃 → 인벤토리로 옮긴 뒤 파는 경로는 그대로 열려 있다.** 다만 그 경우 로드아웃이 비어 `TryMatchMake`의 "무기를 장착해주세요" 가드가 먼저 걸리므로 문제가 되는 창은 좁다
- **`GET /api/items/inventory`는 `money`를 돌려주지 않는다** — 재조회로 인벤토리는 맞출 수 있어도 **표시 잔액은 갱신되지 않는다.** `Money`가 갱신되는 곳은 로그인·세션 유지·구매 성공 셋뿐이고, 로비에서 돈이 줄어드는 경로가 구매뿐이라 지금은 문제가 없다 — 판매가 붙거나 다른 세션이 재화를 건드리게 되면 그때 이 응답에 `money`를 실어달라고 요청할 것
- **계정 생성·게스트 로그인에는 아직 빈 본문·비-JSON 방어가 없다** — `PostCreateAccountCall`·`PostGuestLoginCall`이 여전히 `SendRequestAsync` + 무방비 `JsonUtility.FromJson`이라, 본문 없는 실패 응답이나 JSON이 아닌 본문(프록시 에러 페이지 등)이 오면 예외가 `async void` 호출부로 빠져나가 **팝업 없이 버튼만 굳는다.** 로그인은 (2026-08-31 #6), 로그아웃은 (2026-08-31 #7), 버전 확인은 (2026-09-01 #0)에서 닫혔고 남은 둘은 그런 응답이 실제로 오는지 확인되지 않아 보류한다
- **비밀번호에 특수문자가 필수인지는 서버에 확인되지 않았다** — `IsValidPassword`의 정규식은 영문+숫자만 요구하고 특수문자는 허용 문자일 뿐인데 안내 문구만 "특수문자 포함"이라 갈려 있었다. **검사가 아니라 문구를 사실에 맞췄다** — 정규식을 조이면 서버가 받아주는 비밀번호를 클라가 막게 되므로, 서버가 실제로 요구한다는 것이 확인되면 그때 lookahead를 더한다
- **세션 만료 안내는 다른 팝업이 떠 있으면 묻힌다 (감수)** — `ActiveOnlyConfirm`이 `isActive`면 아무것도 하지 않는다. **정리·전이가 팝업보다 먼저 끝나므로 상태는 안전하지만**, 확인/취소 팝업이 떠 있었다면 그것이 Auth 화면 위에 남고 확인을 누르면 `AuthState == None` 가드에 걸려 실패 안내가 뜬다. 기존 팝업을 강제 해제하는 해법은 콜백 부작용이 딸려와 넣지 않았다
- **resume 경로의 로비는 재화·인벤토리가 비어 보인다 (버그 아님)** — 서버 `/api/session/resume` 핸들러가 아직 뼈대라 `money: 0, inventory: []`를 반환한다. 서버 구현이 끝나기 전까지 정상이므로 클라 파싱 버그로 오해하지 말 것. **`http-api-spec.yaml`은 서버 사본과 동기화되어 있으므로 한쪽만 수정하지 말 것**
- **소리별 음량 밸런스는 `volumeScale` 인자로만 잡고 프리팹 Volume은 건드리지 않는다** — `PlayOneShotAt`이 매 재생마다 소스의 `volume`을 덮으므로 프리팹 값은 남지 않는다. **소스를 근거리·원거리로 나눠도 이 항목은 그대로 남는다** — 리스너가 가슴팍 바로 위라 두 커브 모두 **첫 키 이전 구간(볼륨 1.0)**이고, 내 총성과 내 발소리가 여전히 같은 크기다
- **큰 컨테이너(차·버스)는 상호작용 거리 판정과 부딪힐 수 있다 (실물 확인 전까지 보류)** — `PlayerController.CheckInteractable`이 거리를 **탄착점이 아니라 `hit.collider.transform.position`(콜라이더 원점) 기준 2m**로 재고, 서버도 3m로 다시 검사한다. 버스 옆에 바짝 서도 원점까지 2m를 넘으면 **프롬프트가 아예 안 뜬다.** `TenerifeBlueCar` 기준 **콜라이더 5개가 전부 루트에 붙어 있어 어디에 맞아도 원점이 같다** — 자식으로 흩어 놓으면 맞은 콜라이더 기준으로 재지므로 이것부터가 갈림길이다. 인게임에서 실제로 나오면 그때 고를 것 — ① 프리팹 피벗을 접근 지점(트렁크·문짝)에 두기(서버 계약 변경 없음, 유력) ② 상호작용용 작은 자식 콜라이더(①과 병행해야 서버 3m가 성립) ③ 오브젝트별 반경을 서버가 갖기(계약 변경)
- **총성 120m는 아직 실측 미검증이고 두 가지에 종속된다** — ① **Custom Rolloff 커브가 `Max Distance`에서 실제로 0에 닿는지**(닿지 않으면 그 너머로 고정 볼륨의 소리가 계속 들린다) ② **서버가 상태 스트림을 거리로 컬링하는지** — `IngameScene.HandleWeaponFireBroadcast`가 미스폰 발사자에서 return하므로 컬링 반경 밖 총성은 프리팹 값을 올려도 오지 않는다. **특정 거리부터 안 들리면 프리팹이 아니라 서버부터 볼 것**
- **`Resources/`는 참조 여부와 무관하게 전량 빌드에 포함된다** — 코드 참조가 없는 자산이 확인된 것만 여럿이다(`Prefabs/Scene/*` 4종, `System/@Managers`, `System/EventSystem`, `TestPlayer`, `TestLobbyScene/`, `LobbySettingUIBackup`). **`System/EventSystem`은 코드가 쓰는 `UI/EventSystem`과 같은 것 두 벌**이다. 알파 단계라 용량이 문제는 아니고 **삭제 판단은 사용자 몫이므로 사실만 남긴다** — 목록은 `Assets/Resources/CLAUDE.md`에 있다
- **씬 이름 컨벤션 잔여 (판단 보류)** — `TestIngame2.unity`는 컨벤션대로면 `TestIngame2Scene.unity`이지만 `Define.Scene` enum에 없어 코드 영향이 없고, `WinchesterAlpha.unity`는 맵 에셋 성격이다. **enum에 올리는 순간 파일 이름을 맞춰야 한다**(`Scenes/CLAUDE.md`의 씬 이름 규칙). 맵 명칭이 Tenerife로 확정됐으므로 `WinchesterAlpha.unity`의 처분(리네임·삭제·존치)도 함께 볼 것 — **코드 참조는 없다**
- **맵 리네임이 `http-api-spec.yaml`에도 반영됐다 (`1: MAP_TENERIFE`) — 서버 사본 전달은 사용자 몫이다** — 이 파일은 서버 사본과 동기화되므로 **클라에서만 고친 상태로 두면 두 사본이 갈라진다.** 다만 **계약은 mapId 값(1)이고 이름은 표기일 뿐이라 전달 전에도 통신에는 영향이 없다.** 같은 파일에 `latestVersion` 설명·예시(`"alpha-1"`) 동기화도 함께 들어 있다
