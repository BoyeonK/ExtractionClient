# 프로젝트 진행 상황

> 최종 수정: 2026-08-28
> 장르: 멀티플레이어 Extraction 게임 (알파 단계)
> 엔진: Unity 6000.4.0f1 / URP 17.4.0

---

## 완료된 것들

### 네트워크/UI
- [x] (2026-08-27 #0) **재장전 구현 + '행동 잠금' 신설 — 무기 교체를 유예 전송으로 전환 (T24 · 구 우선순위 12번)** — proto에 `C2D_REQUEST_RELOAD`(42)·`D2C_RESPONSE_RELOAD`(43)가 추가되면서 **"탄창이 비면 그 판 내내 못 쏜다"와 "매치 중 주워 장착한 무기의 탄창이 영구히 비어 있다"가 함께 풀렸다.** **proto가 한 세션 안에서 두 번 바뀌어 1차 추적으로 세운 계획은 통째로 파기했다** — 1차 응답은 `magazine` + `changed_slots` **델타**였고 빈 칸을 `blueprint_id == 0xFFFFFFFF` 센티널로 표기했는데(proto3에서 0은 미설정과 구분되지 않아서), 2차에서 `D2CFullInventorySync inventory` **스냅샷 하나**로 바뀌었다(`reserved 3, 5, 6, 7`이 그 흔적이다). **스냅샷인 이유가 proto에 명시됐다 — 서버가 인벤토리를 0번부터 훑어 여러 칸에서 탄약을 나눠 빼고 0이 된 칸을 비우는데, 어느 칸이 몇 발씩 빠지는지가 서버 스캔 순서에 달려 있어 클라가 재현할 수 없다.** `D2CResponseEquipItem`(재현 가능한 단일 스왑이라 인덱스만 통보)과 형태가 다른 것도 같은 이유이며 **통일 대상으로 보지 말 것**. 1차 계획에서 서버에 물어보려던 "응답에 어느 무기의 탄창인지가 없다"는, 스냅샷이 주/보조 탄창을 모두 실으면서 질문 자체가 사라졌다. **`ExternalProtocol.cs`는 2차 스펙 기준으로 이미 재생성되어 있었다**(`{Result, DenyReasonMask, FireSequence, Inventory}`) — proto가 경고한 "3·5·6·7 필드를 가진 낡은 생성 코드"는 남아 있지 않았고, 과거 두 번 있었던 `PktId` 누락·반쪽 생성도 이번엔 없었다.
  **이번 작업의 실질은 재장전 자체가 아니라 행동 잠금이다(사용자 설계).** `IngameScene`에 **사유**(`PlayerActionKind`)와 **단계**(`PlayerActionPhase`)를 함께 든 잠금 하나를 세우고 재장전·무기 교체를 거기에 얹었다. **단계가 이 설계의 핵심이다** — `Local`(유예 중, 아직 **전송 전**)은 서버에 흔적이 없어 취소가 성립하고, `Pending`(전송 후)은 서버가 이미 처리했으므로 취소할 수 없다. 사유만 들고 있으면 **이미 나간 요청을 취소할 수 있다고 착각**하게 된다. 규칙은 넷 — 같은 사유+같은 대상은 **무시**(R 중 R, 1 중 1. 재요청이 자기 행동을 스스로 무효화하지 않게 한다), 같은 사유+다른 대상은 **재타게팅**, 다른 사유+`Local`은 **취소 후 진입**(재장전 중 무기 교체가 우선), `Pending`은 **전부 무시**(손에 든 무기·인벤토리 버전이 미정이라 무엇을 보내도 어긋난다). 진입은 `TryBeginAction()`, 해제는 `ClearAction()`, 구동은 `UpdateAction()` 각각 한 곳뿐이다.
  **무기 교체가 즉시 전송에서 0.5초 유예 전송으로 바뀐 것이 가장 큰 동작 변경이다.** **`Scenes/CLAUDE.md`가 "이 잠금(`IsWeaponSwitchPending`)을 푸는 변경은 `rSeqNum` 기반 순서 방어를 함께 가져와야 한다"고 못박아 둔 조건에 걸리는지 먼저 확인했고, 걸리지 않는다** — 재타게팅이 `Local`(전송 전)에서만 일어나므로 **in-flight 요청은 여전히 항상 1개**다. 이 근거를 문서에 남기지 않으면 다음 세션이 같은 판단을 처음부터 다시 해야 한다. **슬롯이 0/1 둘뿐이라 "다른 대상" = "손에 든 슬롯"이고, 즉 지금의 재타게팅은 항상 되돌리기(패킷 없이 소멸)다** — 유예 전송의 부수 효과로 "교체 취소"가 공짜로 생겼다. 3번째 슬롯(근접·투척)이 생기면 그때 이 경로가 진짜 재타게팅이 되며, 그 전까지 "타이머를 이어받을지 다시 돌릴지"는 성립하지 않는 질문이다. **요청에 실을 값은 전송 시점에 읽는다** — 유예 중 인벤토리를 조작하면 버전이 오르는데 시작 시점 값을 캐시하면 **자기 조작 때문에 `DENY_VERSION_MISMATCH`로 거부된다**. 대상 슬롯 유효성도 같은 이유로 전송 시점에 다시 본다.
  **`fire_sequence` 판정도 같은 proto 변경에서 완화됐다** — 완전 일치 요구에서 **"기대값보다 작으면 거부, 같거나 크면 수락 후 기대값 이동"** 으로 바뀌었다. 발사는 unreliable이라 재전송이 없고 `uSeqNum`을 이동 패킷과 공유해 재정렬만으로 한 장이 드롭되는데, **예전에는 그 한 장 때문에 이후 발사가 영구히 거부됐다.** 클라 계약(매 발사 무조건 증가 / 되돌리거나 재사용 금지)은 `NextFireSequence()`가 이미 만족하고 있어 **고칠 것이 없었고, 새로 필요한 건 되돌아간 값을 끌어올리는 경로 하나뿐**이었다(`RaiseFireSequenceTo()`, `max()` 단조 상승만 — **대입 setter를 만들지 말 것**. 요청을 보낸 뒤 응답 전에 쏜 발사가 이미 번호를 올려놨을 수 있다). **반영을 `ExecuteAtMainThread`로 모은 것은 lock을 피하기 위해서다(사용자 판단)** — 다른 접근자가 메인 전용이라 반영도 메인으로 모으면 단일 스레드 소유가 되어 동기화가 필요 없다. **그리고 이 값은 인벤토리 버전 가드보다 먼저, 스냅샷을 버리는 응답에서도 반영한다** — proto가 "인벤토리와 무관"이라 분리해 놨고 **되돌아간 시퀀스의 유일한 복구 경로**라, 가드 뒤에 두면 복구가 영영 안 된다.
  **`Handle_D2CFullInventorySync`를 재사용하지 않은 이유는 proto가 든 것과 다르다.** proto는 "최초 1회 초기화가 재장전마다 돈다"를 들었는데 우리 `TryInitWeapon`/`TryInitShield`는 이미 일회성 플래그로 막혀 있어 그 부분은 멱등했다. **진짜 이유는 버전 가드다 — 전체 동기화 핸들러에는 버전 비교가 아예 없어서 낡은 스냅샷을 버릴 자리가 없다.** 변환(`ToInventoryItem`/`ToInventorySlotArray`)만 밖으로 빼 공유했다. `DENY_VERSION_MISMATCH` 처리는 **proto 지시(새 버전으로 재요청)와 기존 규칙(자동 재시도는 버전이 계속 바뀌면 루프가 된다)이 정면으로 부딪혀 1회 한정으로 절충**했고, 뒤늦은 응답이 유저가 직접 시작한 다른 행동을 가로채지 않도록 `IsActionBusy`면 재요청을 포기한다. **유예 시간은 클라 상수다(재장전 2초 / 교체 0.5초)** — `WeaponSpec`에 재장전·교체 시간 필드가 없어 DB에서 오지 않으며, `ItemDBHelper`는 수정 금지라 필요해지면 DB 쪽에 요청해야 한다. **달리기는 재장전의 진입 조건이자 유지 조건이다** — 시작도 막고 `Local` 중 달리면 취소한다. **R을 누르면 달리기를 자동 해제하는 방식은 기각했다**(코드가 `_shift`를 뒤집으면 Shift를 누른 채인데 안 달리는 상태가 남는다). 부수 효과로 **흩어져 있던 `SWITCH_WEAPON_TIMEOUT` 워치독이 `ACTION_PENDING_TIMEOUT` 하나로 접혔다** — 재장전용 세 번째 타이머를 만들지 않아도 됐다. **상호작용·귀환의 편입은 `TODO:`로 남겼다(사용자 지시)** — 귀환은 구조가 같아 흡수되지만 상호작용은 컨테이너가 열려 있는 동안 지속되는 상태라 `Pending`으로 잡으면 여는 내내 모든 행동이 잠긴다. **검증은 (2026-08-27 #1)에서 별도로 수행했다.**

- [x] (2026-08-27 #1) **재장전·행동 잠금 2인 접속 검증 + 유예 시간 확정** — (2026-08-27 #0)의 컴파일이 통과했고 2인 접속에서 **눈에 띄는 문제가 나오지 않았다**(사용자 확인). 직전까지 우선순위 0번이던 항목이 닫혔다. **이 세션의 실질은 "유예 시간 2초 / 0.5초가 손에 맞는다"를 확정한 것이다(사용자 판단)** — 코드로 판단할 수 없고 상수 둘만 고치면 되는 자리라, 감이 남아 있을 때 못박아 두지 않으면 나중에 근거 없이 다시 흔들린다. 그래서 **상수 주석의 `TODO:` 방향을 뒤집었다** — 기존 문구("애니메이션이 붙으면 클립 길이로 대체")를 그대로 두면 **다음 세션이 클립 길이가 얼마든 거기에 맞춰 방금 확정한 조작 감각을 조용히 바꿔버린다.** 이제 **클립을 이 길이에 맞춰 만드는 것**이며 반대가 아니다(`IngameScene` 상수 주석 + `Scenes/CLAUDE.md` 양쪽). 무기별로 달라져야 하면 `ItemDBHelper`를 고치지 말고 DB 쪽에 필드 추가를 요청한다. **"문제 없음"이 전부를 덮지는 않는다** — 정상 플레이로는 밟히지 않는 방어 경로 넷(버전 불일치 재요청 / 낡은 스냅샷 폐기 / `fire_sequence` 복구 / 응답 워치독)과 **재장전이 실제로 몇 발을 채웠는지**가 남아 있다. 앞의 둘은 마음먹으면 재현되고 뒤의 하나는 탄약 HUD가 선행이라, 메모의 '확인 필요'로 옮겨 우선순위 목록에서는 내렸다. **전투 검증을 막던 제약이 이걸로 사라졌다** — 이제 탄창이 비어도 그 판을 계속 쓸 수 있어, 0번(히트박스 반지름 튜닝)처럼 사격을 반복해야 하는 작업이 탄약에 걸리지 않는다

- [x] (2026-08-27 #2) **proto `[작업사항]` 주석 정리 (T17) + 서버 계약 4건 확정 + 자기 제외 `TODO:` 해소** — `External_Protocol.proto`를 **651줄 → 392줄**로 줄였다(주석 259줄 제거). **필드·메시지 정의는 한 줄도 건드리지 않았다.** 착수 전 `[작업사항]` 블록 17개를 전수 조사해 완료·미완료를 갈랐고, **사용자가 주석 정책을 확정했다** — 항구적으로 남기는 것은 **"각 패킷이 어느 상황에 사용되는지"** 한 줄 설명, 남기지 않는 것은 **"받아서 어떻게 처리되는지의 과정"**. 여기에 "인계용은 작업이 안 끝났으면 남긴다"가 더해져 3분류가 됐다.
  **구조적으로 절반은 이미 정책대로였다** — `[작업사항]` 블록 아래에 항구 주석이 따로 붙어 있는 메시지가 10개라 **블록만 들어내면 그대로 충족**됐다. 항구 주석이 없던 5개(`D2CSpawnPlayerObject`·`C2DRequestEquipItem`·`C2DRequestWeaponFire`·`D2CNotifyHealthChange`·`C2DRequestRecall`)에만 한두 줄을 신설했다. **남긴 것은 둘뿐이다** — 헤더 (3) Corpse 매핑(프리팹 대기)과 `D2CUpdatePlayerStates`(윈체스터 실측 미완). `D2CNotifyPlayerKilled`(40)·`D2CNotifyObjectKilled`(41)은 **블록이 지시한 배선이 전부 끝났고 남은 게 UI 프리팹·`ICombatTarget` 보급이라 proto를 다시 읽을 일이 없어** 지웠다.
  **삭제 과정에서 스펙 모순 하나가 드러났다** — `C2DRequestWeaponFire` 블록이 *"비플레이어는 죽으면 `D2CNotifyDespawnObject`로 제거가 통보된다"* 라고 적고 있었는데, **41번이 추가되면서 규칙이 바뀐 뒤 갱신되지 않은 것**이었다(39번·41번 블록은 정반대로 적혀 있었다). 클라는 이미 41번 기준이라 동작 문제는 없었다. **처음엔 이 블록을 "T14 정보 대기"로 남기고 문장만 고칠 계획이었는데, "전투 오브젝트는 앞으로 동적으로 계속 추가된다"는 답이 오면서 블록을 남길 근거 자체가 사라져 삭제로 해소됐다.** 목록을 주석에 둘 수 없다는 뜻이고, 이걸로 **T14의 성격이 "서버 목록 대기"에서 "전투 오브젝트를 추가할 때마다 `ICombatTarget`을 붙이는 상시 규칙"으로 바뀌었다** — 2026-08-20부터 대기 중이던 항목이 대기가 아니게 됐다.
  **끊긴 참조도 하나 처리했다** — 남긴 `D2CUpdatePlayerStates` 블록이 `공통 사항 (4)`를 가리키는데 (4)를 지워서, 참조를 떼고 문장을 자립시켰다. 삭제한 내용 중 값진 것(재장전 스냅샷 설계 의도, 무기 교체 판정법, 사망 유예 계약, `timestampEcho`, 실드 재생 공식)은 **전부 모듈 CLAUDE.md에 이미 보존되어 있는 것을 확인하고 지웠다.** 중괄호 균형(48/48)으로 구조 무결성을 확인했고, **서버 사본 반영은 사용자가 직접 진행한다**(전달 사항 4건을 목록으로 정리했다).
  **함께 서버 계약 4건이 확정됐다(사용자 확인)** — ① **무기별 재장전·교체 시간 차등 계획 없음** → 유예 시간은 DB가 아니라 **클라이언트 주도 값**으로 확정, 상수 주석의 "DB에 필드 추가 요청" 문구를 걷어냈다 ② **부위별 차등 데미지 구현 예정 없음** → **히트박스 캡슐 11개는 부위 구분이 아니라 실루엣 판정용**이며 어느 캡슐에 맞든 데미지가 같다. `Controller/CLAUDE.md`에 명시해 다음 세션이 헤드샷 배율을 가정하고 코드를 넣는 것을 막았다 ③ `D2CNotifyDespawnObject`(39) **존치 확정**(비전투 제거 사유가 추후 생긴다) ④ **최대 정원 4명** → MTU 우려가 영구히 닫혔다.
  끝으로 **`Fire()`의 "자기 PlayerObject 레이캐스트 제외" `TODO:`를 새 레이어 없이 닫았다** — 여러 판의 사격에서 자탄 0건이 확인됐고, **운이 아니라 전제 셋이 맞물린 결과**다(무기 콜라이더는 장착 시 꺼짐 / 로컬에는 히트박스를 만들지 않음 / 레이가 `CharacterController` 캡슐 안에서 출발해 Unity가 보고하지 않음). **주석을 통째로 지우지 않고 근거와 함께 "셋 중 하나라도 깨지면 그때 레이어가 필요하다"를 남긴 것이 이 작업의 요점이다** — 안 적어두면 다음 세션이 "제외가 없네"를 발견하고 `TODO:`를 다시 세운다

- [x] (2026-08-27 #3) **설정값 영속화 (`PlayerPrefs`) — 우선순위 맨 아래 항목이 0번의 선행이었다** — `SettingManager`의 필드 8개(볼륨 3·감도·창모드·해상도·프레임레이트·FOV)가 **메모리에만 있어 게임을 끄면 전부 기본값으로 돌아가고 있었다.** 목록에서는 12번이었는데 **0번(감도 기본값 확정)의 실질적 선행**이라 끌어올렸다 — 0번이 "체감으로 수렴시켜야 하는 값이라 슬라이더로 몇 판 돌려 보고 정한다"인데, **매 실행마다 1.0으로 리셋되면 판을 거듭하며 수렴시키는 것 자체가 성립하지 않는다.** (2026-08-26 #5)로 감도가 인게임에 실제로 먹기 시작하면서 이 항목의 성격이 바뀐 것이고, 목록 순서만 보고 판단하면 놓치는 종류다.
  **범위를 절반으로 잘랐다** — 저장·로드만 하고 **해상도·창모드·FOV를 화면에 실제 적용하는 것**(`Screen.SetResolution()` 등)은 남겼다. 검증 비용이 다르기 때문이다(저장은 껐다 켜면 끝이지만 화면 모드는 에디터·빌드 동작이 갈리고 되돌리기도 번거롭다). **회귀가 아니다** — 그 셋은 지금도 적용되지 않으므로 "기억은 되는데 화면은 그대로"가 현재 동작과 같다.
  **저장 시점 설계가 이 작업의 핵심이고, 사용자 지적으로 한 번 고쳐졌다.** 원안은 `OnApplicationQuit` 한 곳이었는데(드래그 중 디스크를 때리는 것을 피하려던 것), 그 우려는 **setter에 거는 경우의 문제**라 확정 시점에는 적용되지 않는 논거였다. **결정적인 건 크래시 내성이다** — `OnApplicationQuit`은 정상 종료에서만 불려서, 감도를 맞추고 비정상 종료하면 그 조정이 통째로 날아간다. **이 작업의 목적이 "몇 판에 걸쳐 수렴시키는 것"이라 목적 자체가 훼손된다.** 최종 저장 지점은 셋 — `LobbySettingUI.ApplySetting()`(확정), `IngameSettingUI.Hide()`(닫힘), `Managers.OnApplicationQuit()`(backstop).
  **두 설정 창의 확정 지점이 서로 다른 것이 함정이었다.** 로비는 '적용'에서 setter를 한 번에 부르지만, **인게임은 슬라이더 즉시 반영이라 '적용'이 확정이 아니다**((2026-08-26 #5)에서 일부러 갈라 놓은 정책이다). 그래서 인게임은 **'닫힘'**에 걸었고, `CancelAndHide()`가 되돌린 뒤 `Hide()`로 오므로 **취소까지 한 자리로 덮인다** — 취소를 빠뜨리면 메모리와 디스크가 어긋난 채 남는다. **`Managers.OnApplicationQuit`에서는 `s_isQuitting`을 세우기 전에 불러야 한다** — 세운 뒤에는 `Instance` 게터가 null을 반환해 `Managers.Setting`으로 접근할 수 없다.
  **밟을 뻔한 함정 셋을 처리했다** — ⓐ **enum을 정수로 저장하면 안 된다.** `Define.Resolution`이 오름차순 정렬이라 새 해상도가 **목록 중간에 삽입되기 쉬운데**, 그 순간 저장된 정수가 다른 항목을 가리킨다(증상이 "설정이 멋대로 바뀐다"라 원인을 짚기 어렵다). 이름 기반으로 저장하고 `MaxCount`와 파싱 실패를 경고 로그와 함께 기본값으로 되돌린다 ⓑ **로드는 필드 직접 대입이 아니라 setter 경유** — 범위 검증이 setter에 있고 `PlayerPrefs`는 사용자가 편집할 수 있는 영역이다 ⓒ **로드 순서는 마스터 볼륨이 먼저다** — `SetVolume()`이 `(볼륨 × 마스터)`로 계산하므로 뒤집히면 첫 적용이 어긋난다. **`IngameSettingUI.CancelAndHide()`의 되돌리기에 이미 걸려 있던 것과 같은 규칙**이 로드 경로에서 다시 나왔다.
  함께 **`SetMouseSensitivity`에 clamp를 넣었다** — 지금까지 검증 없이 대입하고 있었는데 슬라이더가 유일한 입력원이라 드러나지 않았다. **저장이 붙는 순간 손상된 값이 영구화된다**(0이면 시점이 안 돌고 음수면 반전된다). `MIN/MAX_MOUSE_SENSITIVITY`(0.1~3.0)는 **슬라이더 Min/Max와 손으로 맞추는 값**이라는 것을 상수 주석과 `Managers/CLAUDE.md`에 남겼다. **컴파일 에러 1건이 났다 — `using System` 추가로 `Object.DontDestroyOnLoad`가 `System.Object`와 모호해졌다**(`UnityEngine.Object`로 한정해 해결, 주석으로 이유를 남겼다). **런타임 미검증**

- [x] (2026-08-27 #4) **progress.md 구조 개편 — `메모` 섹션 폐기 + `server-sync-todo.md` 삭제** — **210줄 → 85줄.** 문제는 분량이 아니라 **정보가 잘못된 문서에 있었다는 것이다** — `## 메모`가 13개 하위 섹션 140줄로 파일의 3분의 2를 차지하고 있었고, 그 안에 **설계 계약·불변식이 섞여 있었다.** progress.md는 *"무엇을 했고 무엇을 할 것인가"*(시간축)이고 모듈 `CLAUDE.md`는 *"이 코드를 고칠 때 지켜야 할 것"*(코드축)인데, **계약이 시간축 문서에 있으면 코드를 고치는 세션이 읽지 않는다.** 실제로 지켜지려면 모듈 문서에 있어야 한다.
  **서버 저장소의 progress.md를 대조본으로 받아 구조를 맞췄다**(사용자 제공). 서버에는 `메모`가 없고 `완료된 것들` / `진행 중·다음 할 것들`(**진행 우선사항** + **진행 고려사항**) 셋뿐이다. **다만 클라의 「진행 고려사항」은 서버보다 훨씬 얇아야 정상이다** — 서버는 단일 저장소라 그 섹션이 제약·불변식을 전부 떠안지만, 클라는 모듈 `CLAUDE.md` 5개가 이미 그 자리를 갖고 있다. 그래서 섹션 서두에 **"제약·불변식은 모듈 CLAUDE.md에 있고, 여기에는 아직 코드가 없어 적을 자리가 없는 것과 오해를 막기 위한 관찰만 남긴다"** 를 규칙으로 박았다 — 이게 없으면 같은 방식으로 다시 쌓인다.
  **처분 판단의 핵심은 "이미 옮겨져 있는가"였다.** 삭제 전에 대조했더니 `Queries Hit Triggers`·`lossyScale`·`MuzzlePoint`·`ResumeResult`·`objectId 단조 증가`·재전송 한도·실드 정수 누적이 **전부 모듈 문서에 있었다** — 반동/탄약/귀환 설계 확정, 서버 계약 6건, 해소된 버그 6건이 통째로 중복이었다는 뜻이다. **모듈 문서에 없던 것은 셋뿐**(캐릭터 실측 크기·레이어 목록·`LayerMask` 코드 0건)이었고, 그중 **"레이어 기반 해법은 신설부터 해야 한다"** 한 줄만 `Controller/CLAUDE.md`로 옮겼다 — 방금 닫은 자기제외 `TODO:`가 *"전제가 깨지면 전용 레이어가 필요하다"* 로 끝나는데, 그때 **쓸 수 있는 레이어가 없다는 것을 모르면 비용을 잘못 잡는다.** 실측표 둘(콜라이더 분포·발사-데미지 진단)은 사용자 판단으로 삭제했다.
  **`확인 필요` 23건은 지우지 않고 해당 작업 항목에 흡수시켰다** — 검증은 메모가 아니라 "앞으로 할 일"이다. 실드 실측·킬 피드·탑뷰 시신·히트박스 기즈모는 **2인 접속 판 하나로 모았고**(2인을 붙이는 비용이 가장 비싸다), 설정 저장 검증은 감도 확정과 **같은 자리에서 끝나므로**(감도를 맞추려면 어차피 껐다 켜야 한다) 그 항목에 붙였다. 발사선 시각화 검증은 `TEMP:` 회수가 걸린 살아 있는 작업이라 **메모가 아니라 우선순위 항목으로 승격**했다.
  **`server-sync-todo.md`(311줄)를 삭제했다** — 존재 이유가 "proto `[작업사항]` 주석 추적"이었는데 (2026-08-27 #2)에서 **주석 자체를 정리하면서 추적 대상이 사라졌다.** 삭제 전에 **거기에만 있던 둘을 이관했다** — T13 시신 프리팹 규격(새로 만들 것 / `ContainerController` 필수 / **프리팹 전에 `ObjectPaths` 항목을 넣지 말 것**)은 진행 고려사항으로, T16 실측 내용("클라 수정 없고 두 맵에서 동일하게 동작하는지 확인만")은 2인 판 하위로. **T번호는 없앨 수 없었다** — 완료 항목들이 참조하고 있어서, 헤더에 "과거 참조용으로만 남아 있고 신규 작업에는 새로 붙이지 않는다"를 명시했다

- [x] (2026-08-27 #5) **컨테이너 오브젝트 5종 + 킬 피드 UI 배선 — 프리팹 대기가 한꺼번에 풀렸다** — 사용자가 에디터에서 **프리팹 7개(컨테이너 5 + 킬 피드 2)를 만들고 `Define.ObjectType`/`ObjectPaths`에 5종을 직접 등재**하면서, 여태 "프리팹 대기"로 막혀 있던 항목들이 동시에 열렸다. **`PlayerLoot`가 그동안 Corpse로 불리던 것이다** — 사망 시 서버가 그 자리에 스폰하고 사망자의 인벤토리·장착·탄창이 전부 옮겨지는 컨테이너이며, **"지금 새고 있는 결함"(매핑 없어 시신이 보이지도 털리지도 않던 것)이 이걸로 닫혔다.**
  **컨트롤러 5종은 `ContainerController` 상속에 `_objectType` 대입만 하는 형태다**(`PlayerLootController` 외 4종). **`PlayerLoot`도 상호작용 텍스트를 "열어보기"로 통일했다**(사용자 확정) — 박스 형태의 컨테이너이고 조작이 일반 컨테이너와 완전히 같아서, 파생 클래스가 `_interactText`를 건드리지 않고 상속받은 것을 그대로 쓴다. **`_objectType`은 현재 읽는 코드가 0개인데도 유지하기로 확정했다**(사용자 판단) — 컨테이너 종류별 분기가 생길 때의 자리다. **이 계층에서 유일하게 위험한 것은 `base.Init()` 누락**이라 `Controller/CLAUDE.md`에 규칙으로 적었다: `_onInteract += RequestOpenContainer` 구독이 `ContainerController.Init()`에 있어서, 빠뜨리면 **E키를 눌러도 아무 일이 없고 에러도 남지 않는다.**
  **킬 피드는 사용자가 만든 스크립트 둘(`IngameKillLogUI`/`SingleKillLog`)을 추적해 결함 셋을 고치고 배선했다.** ① **프리팹 경로가 실제 폴더와 달랐다** — 코드는 `UI/IngameScene/`을 찾는데 프리팹은 `UI/IngameSceneUI/`에 있어, 로드 실패 후 **다음 줄에서 NRE가 나 킬이 날 때마다 예외**가 터질 상태였다 ② **`_killLogs`에 등록하는 코드가 없어** `RemoveSingleKillLog()`의 `TryGetValue`가 항상 실패했다 — 로그가 화면에 영원히 쌓인다. 인덱스를 지역 변수로 받아 목록과 자식이 같은 값을 갖게 했고, **등록을 `Init()`보다 앞에 뒀다**(그 안에서 수명 타이머가 걸리므로 등록이 늦으면 만료 시점에 못 찾는다) ③ 5초 제거의 `TODO:`를 `Invoke`로 채우되 **스스로 파괴하지 않고 부모의 `RemoveSingleKillLog()`를 거치게 했다** — 목록에서 빼는 것과 파괴가 한 곳에서 일어나야 딕셔너리에 죽은 참조가 안 남는다.
  **표시명은 `KillLogName()` 한 함수로 모았다** — 서버가 킬 피드용 표시명을 보내지 않아 지금은 `가해자 없음`/`나`/`objectId=N`뿐이고, **표시명이 생기면 이 함수만 갈아끼우면 호출부는 안 건드린다.** 로그용 `DescribePlayer()`와 나눈 것은 그쪽이 `weaponId`·미스폰 여부까지 붙여 화면에 올리기엔 길어서다.
  **부수 효과로 `ResourceManager`의 실패 로그 둘을 `Debug.Log` → `Util.LogError`로 올렸다.** 프리팹 미제작(`InstantiateFromObjectDataStruct`)과 경로 오타(`Instantiate`) 양쪽인데, **후자는 방금 ①이 정확히 그 경로로 샜다** — 로드 실패가 콘솔에서 묻히고 원인이 아니라 NRE부터 보게 되는 구조였다. 프리팹을 하나씩 만드는 동안 계속 밟게 되는 자리라 매핑 누락과 같은 급으로 드러나게 했다. **컴파일·런타임 모두 미검증이고, 프리팹 계층 이름이 코드가 찾는 것과 맞는지도 확인되지 않았다**(아래 「진행 고려사항」)

- [x] (2026-08-27 #6) **컴파일·프리팹 계층 검증 완료 + 씬 내장 UI 바인딩의 NRE 구멍 하나 차단** — (2026-08-27 #5)로 들어온 스크립트 7개와 프리팹 7개가 **씬 초기화에서 NRE로 터졌다** — `IngameScene.Init()`의 킬 피드 바인딩 지점이었다. **`GameObject.Find`는 성공했는데 `GetComponent<IngameKillLogUI>()`가 null을 반환한 것**(씬에 이름 맞는 오브젝트는 있는데 스크립트가 안 붙어 있었다)이고, 그 결과가 단순히 킬 피드가 안 뜨는 게 아니라 **`Init()`이 거기서 멈춰 크로스헤어 생성과 키 리스너 등록을 포함한 아래 초기화가 통째로 안 도는 것**이었다.
  **이건 제가 만든 구멍이 아니라 기존 패턴을 그대로 따라 쓰면서 하나 더 만든 것이다** — 씬 내장 UI 6종이 전부 `Find` 결과만 검사하고 `GetComponent` 결과는 보지 않는다. 킬 피드 쪽에 `Util.LogError` 가드를 넣어 **NRE 대신 원인이 문장으로 뜨고 초기화가 계속 돌게** 했고, `KillLogContainer` 누락도 함께 막았다(이쪽은 NRE가 아니라 **킬 로그가 캔버스 루트에 붙는 조용한 증상**이라 로그로만 드러난다). **나머지 5종의 같은 구멍은 남아 있고 `UI/CLAUDE.md`에 규칙으로 적었다** — 맵 씬이 늘어날 때마다 UI 5종을 배치해야 하는 구조라(우선순위 11번) 같은 실수가 맵마다 반복될 자리다.
  **검증은 두 단계로 닫혔다.** ① 씬 초기화가 끝까지 도는 것으로 컴파일과 `IngameKillLogUI` 부착이 확인됐고 ② **상자에 다가가 "열어보기"가 뜨고 상호작용이 되는 것**으로 컨테이너 5종이 한꺼번에 검증됐다 — 컨트롤러 부착(안 붙었으면 상호작용 대상으로 안 잡힌다) · **콜라이더가 트리거가 아님**(조준 레이가 맞아야 `CheckInteractable()`이 잡는다) · **`base.Init()` 체인**(문서에 적어둔 유일한 함정. 빠뜨렸으면 "열어보기"는 떠도 E키가 아무 일도 안 했을 것)이 전부 한 번에 통과했다. **`PlayerLootController`도 같은 계층·같은 형태라 컨트롤러 쪽은 함께 검증된 셈**이고, 남은 것은 사망 시 서버가 실제로 스폰하는지와 그 프리팹 자체뿐이다.
  **남은 미검증 둘은 킬이 나야 밟히므로 2인 판으로 넘겼다** — `SingleKillLog` 하위의 `KillerId`/`VictimId`(`transform.Find` 직후 `.GetComponent<TextMeshProUGUI>()`를 바로 부르므로 **없으면 첫 킬에서 NRE**)와 `PlayerLoot` 스폰이다

- [x] (2026-08-27 #7) **킬 로그 표시명을 서버 이름으로 전환 + 오브젝트 킬의 킬 피드 미적용 확정 + proto `[작업사항]` 잔여 2건 정리** — 서버가 `D2CNotifyPlayerKilled`에 `victim_object_name`/`killer_object_name`, `D2CNotifyObjectKilled`에 `killer_object_name`을 추가하면서 **(2026-08-27 #5)에 "표시명이 생기면 이 함수만 갈아끼운다"로 걸어둔 `TODO:`가 그대로 소진됐다.** 플레이어의 이름은 userId이고, 생성된 `ExternalProtocol.cs`에 필드가 이미 들어와 있어 재생성 요청도 `PktId` 누락 확인도 필요 없었다. **한 함수만 갈면 되도록 좁혀둔 설계가 실제로 값을 했다** — 표시 경로에서 손댄 곳은 `KillLogName()` 하나이고 `IngameKillLogUI`·`SingleKillLog`는 **한 줄도 건드리지 않았다.**
  **가해자 숨김의 판정 기준을 objectId가 아니라 이름으로 잡은 것이 이 작업의 핵심 판단이다.** proto가 *"비면 킬 로그에 가해자를 표시하지 않으면 된다"* 로 못박았고, **빈 이름은 `killer_object_id == 0xFFFFFFFF`(가해자 없음)보다 넓다** — 서버가 가해자를 찾지 못한 경우까지 덮는다. 그래서 신호가 둘로 갈리는데 **표시는 이름으로 / 로직은 objectId로** 나눴다(스폰 요청·킬 기록·사망 판정은 전부 objectId 그대로). 판정은 `HandlePlayerKilled` 한 곳에만 두고 `KillLogName()` 안으로 넣지 않았다 — 넣으면 **피해자 이름이 비었을 때도 로그 줄이 통째로 빈칸이 된다**(피해자 쪽은 `objectId=N` 폴백으로 살렸다). 결과적으로 `"가해자 없음"` 문자열은 킬 피드에서 사라졌다(로그용 `DescribePlayer()`에는 남아 있다).
  **자기 자신을 `"나"`로 덮던 규칙은 사용자 지시로 폐기했다** — 받아온 아이디를 그대로 표기한다. 부수 효과로 `KillLogName()`이 `_spawnCompleted`·`_myObjectId`를 더는 보지 않아 **표시명이 씬 상태에 의존하지 않게 됐다**(스폰 완료 전에 도착한 킬 통보도 같은 결과를 낸다. 전에는 자기 자신이 `objectId=N`으로 찍혔다). `DescribePlayer()`는 여전히 `"나"`·`weaponId`·미스폰을 붙이므로 **두 함수의 표기 규칙이 갈리는 것이 의도**임을 주석에 남겼다.
  **`D2CNotifyObjectKilled`는 킬 피드에 올리지 않기로 확정했다(사용자 판단).** 착수 전에 호출 흐름을 먼저 훑어 **지금도 킬 피드에 손대지 않는다는 것을 확인했다** — `MakeSingleKillLog()` 호출은 `HandlePlayerKilled` 한 곳뿐이고, 오브젝트 킬 경로는 `RecordObjectKill` → 로그 → `DespawnObject`(처치에는 39번이 오지 않아 여기서 직접 치운다)로 끝난다. 즉 **코드 변경이 아니라 결정의 확정**이었고 `killer_object_name`은 **소비처가 없는 필드**가 됐다. 다만 이 경로는 미스폰 킬러를 스폰 요청하지 않아 `DescribePlayer()`가 대개 `(미스폰)`으로 찍히므로, **킬러 식별이 실제로 되는지 눈으로 보려고 이름을 로그에만 `TEMP:`로 실었다**(사용자 지시). 원복 지점이 `Handle_D2CNotifyObjectKilled`의 추출과 `HandleObjectKilled`의 파라미터 **두 곳이라 서로를 가리키게 주석을 달았다.** 문서에는 "빠뜨린 배선이 아니라 의도적 미소비"로 명시했다 — 안 적으면 다음 세션이 배선을 다시 붙인다.
  **함께 `object_type` 3(`PlayerLoot`)을 확인했더니 코드는 이미 전부 갖춰져 있었다** — enum·`ObjectPaths`·`PlayerLootController`·프리팹 넷 다 (2026-08-27 #5)에서 들어와 있었고 proto의 이름만 `Corpse` → `PlayerLoot`로 바뀐 것이었다. **그래서 명칭과 문서만 맞췄다** — `Define.cs`의 `//Corpse의 역할을 하는 컨테이너` 주석과 `Controller/CLAUDE.md`의 `(시신)` 표기를 정리하고, `[작업사항]` 블록에만 있던 **"사망자의 인벤토리·장착·탄창이 전부 이 컨테이너로 옮겨진다"** 를 모듈 문서로 옮겼다. **사망 연출 쪽 "시신"(`DeathCameraController`·`PlayerController`·`IngameScene`)은 자기 캐릭터 모델을 가리키는 것이라 건드리지 않았다** — 같은 단어라도 대상이 다르다.
  **이걸로 proto `[작업사항]`은 `D2CUpdatePlayerStates` 하나만 남았다.** 블록 (3)(4)를 지우면서 **헤더의 "(3)(4) … 세 건" 상호 참조를 함께 고쳤고**((2026-08-27 #2)에서 밟은 끊긴 참조와 같은 유형이다), 필드 인라인 주석 `// 비면 표시하지 않는다`는 처리 과정이 아니라 **필드 계약이라 항구 주석으로 남겼다.** **서버 사본 전달은 사용자가 직접 한다.** **컴파일·런타임 모두 미검증**이고, **가해자가 빈 킬 로그가 화면에서 어떻게 보이는지는 프리팹을 열지 않아 확인되지 않았다**(아래 우선순위 4번)

- [x] (2026-08-28 #0) **접근 범위를 `Assets/Resources/`까지 확장 + `Assets/Resources/CLAUDE.md` 신설 — CLAUDE.md 트리가 6개가 됐다** — 여태 탐색·수정이 `Assets/Scripts/` 이내로 묶여 있었고, **그 제한의 청구서가 progress.md에 "프리팹 대기"·"확인 필요"로 계속 쌓이고 있었다**(`Image`의 Type이 `Filled`인지, 슬라이더 Min이 0.1인지, `SingleKillLog`에 구분자가 있는지 — 직전 작업에서도 이것 때문에 한 항목을 열어둔 채 넘겼다). **제한을 둔 이유(코드와 무관한 파일이 컨텍스트를 먹는 것)는 실재하지만 원인이 폴더가 아니었다** — Unity YAML의 정보 밀도가 극단적으로 낮아(TMP 컴포넌트 하나가 수백 줄) **비싼 것은 '접근'이 아니라 '전체 Read'** 다. 그래서 폴더는 열되 **`Read`를 마지막 수단으로 내리고 `Glob` → 경로 지정 `Grep`을 앞에 두는 규칙**으로 바꿨다. 씬(`.unity`)은 크기 자체가 위험군이라 그대로 밖에 뒀고, **프리팹은 사용자가 에디터로 관리하므로 읽기 전용**(수정이 필요하면 말할 것)이라는 조건을 함께 박았다.
  **새 문서의 실질은 "이름이 곧 계약인 자리"를 처음으로 한곳에 모은 것이다.** 흩어져 있거나 아예 문서화되지 않았던 규약 7종을 표로 만들었다 — 특히 **`UI/Scene`·`UI/Popup`은 파일명이 곧 UI 클래스명**이라(`UIManager`가 `typeof(T).Name`으로 경로를 조립한다) **클래스를 리네임하면 프리팹도 같이 바꿔야 하는데** 어디에도 적혀 있지 않았다. 함께 **경로 접두어가 진입점마다 다르다**는 것도 명시했다(`Instantiate`는 `Prefabs/` 자동 · `Sound.Play`는 `Sounds/` 조건부 · `Resources.Load` 직접은 전체 경로).
  **실측에서 구조적 사실 하나가 드러났다 — 코드가 경로로 로드하는 UI 프리팹은 넷뿐이고**(`UI/Scene/*`·`UI/Popup/*`·`UI/EventSystem`·`UI/IngameSceneUI/SingleKillLog`) **나머지 씬 내장 UI는 전부 `GameObject.Find`로 잡힌다.** 즉 그쪽의 계약은 **프리팹 파일 이름이 아니라 씬 오브젝트 이름**이며, 프리팹 이름을 맞춰도 씬 인스턴스 이름이 다르면 못 찾는다. 이 구분이 없으면 "프리팹 이름은 맞는데 왜 안 되지"가 반복된다.
  **계층 트리를 두 문서에 중복해 두기로 한 것이 이 작업의 핵심 판단이고, 제 처음 제안을 뒤집은 것이다.** 원안은 "트리는 자산 옆(`Resources`)에, 포인터만 `UI/CLAUDE.md`에"였는데 — **포인터는 이미 아쉬움을 느낀 사람에게만 작동한다.** 계층 트리를 본 적 없으면 "찾아봐야 할 게 있다"는 생각조차 안 든다. 여기에 **문서 로딩이 트리거 기반이라 부분적**이라는 점이 겹친다(어떤 세션엔 한쪽만 붙는다). **결정적이었던 것은 이 저장소가 이미 그 방식을 쓰고 있다는 사실이다** — `http-api-spec.yaml` ↔ 서버 사본, `MAX_HEALTH_POINT` ↔ 서버 상수, 감도 상수 ↔ 슬라이더 Min/Max가 전부 **"중복 + 한쪽만 고치지 말 것"** 으로 처리돼 있다. 문서 사이라고 다른 규칙을 쓸 이유가 없었다.
  **다만 중복하는 것은 트리(레퍼런스 데이터)뿐이고 산문은 나눴다** — 짧고 형태가 고정된 데이터는 두 사본이 글자 단위로 같게 유지되고 어긋나면 즉시 드러나지만, **산문은 옮겨 쓸 때마다 문장이 달라지고 어느 쪽이 최신인지 판별할 수단이 없다.** 그래서 강조점을 반대로 뒀다 — `Resources` 쪽은 **만들 때의 규격**, `UI` 쪽은 **찾는 방식과 어겼을 때의 증상**. 양쪽에 동기화 마커를 박았다. **루트에는 이 원칙을 적용하지 않았다** — 항상 로드되므로 복사는 매 세션 고정 비용이고, 루트의 역할은 자기완결이 아니라 **인덱스**다(78줄이 적정선). 함께 루트의 `Managers` 라벨을 "입력 시스템" → "설정 영속화 · 입력 시스템"으로 고쳤다(실제 내용의 절반이 `SettingManager`인데 표에서 보이지 않았다).
  **확장한 권한을 바로 써서 열려 있던 질문 하나를 닫았다** — `SingleKillLog`의 자식은 둘이 아니라 **셋**이었다(`KillIcon`·`KillerId`·`VictimId`). 가해자 이름이 비면 `KillerId`만 비고 아이콘은 남아 `[아이콘] [피해자]` 꼴이 되는데, **어색하지 않아 빈 문자열 처리를 그대로 유지하기로 확정했다**(사용자 판단). 두 문서의 트리에 `KillIcon`을 반영했다. **프리팹 전체를 읽지 않고 `m_Name:` grep 한 번으로 끝났다 — 새 규칙이 의도대로 동작한 첫 사례다.**
  **관찰로 남긴 것 셋** — ⓐ **`Sounds/` 12개 전부 재생 호출부가 0개다**(발사음·빈 탄창음·발소리가 파일로는 준비돼 있다) ⓑ `System/EventSystem`과 `UI/EventSystem`이 **같은 것 두 벌**이고 코드는 후자만 쓴다 ⓒ `Prefabs/Scene/*`·`TestPlayer`·`LobbySettingUIBackup` 등이 코드 참조가 없다. **`Resources/`는 참조 여부와 무관하게 전량 빌드에 포함되므로** 이 목록은 용량 관점에서 의미가 있다(삭제 제안은 아니다). **`.md`는 Unity가 `TextAsset`으로 임포트하는 확장자가 아니라 새 문서가 빌드에 들어가지 않는다.** **트리 로드 실증은 아직 못 했다** — 프리팹에 건 `Grep`에서는 새 문서가 컨텍스트에 붙지 않았고(지금까지 붙은 사례는 전부 `Read`였다), 다음 세션에서 `Assets/Resources/` 하위를 실제로 읽을 때 확인된다

- [x] (2026-08-28 #1) **빈 탄창 효과음 + 탄약 HUD 배선 — 우선순위 10번이 닫혔다** — 두 항목이 한 묶음이었던 이유가 그대로 값을 했다. `EmptyAmmoFire()`는 `_fireBlocked = true` + `TEMP:` 로그 한 줄뿐이었고, 사용자가 프로토타입으로 만들어 둔 `IngameWeaponUI`는 **스크립트가 어디에도 연결돼 있지 않아 완전히 죽은 코드였다**(`Init()`·`DeactiveThis()` 호출부가 프로젝트 전체에 0개라, 씬에 배치돼 있어도 프리팹에 저장된 더미 텍스트가 그대로 떠 있기만 했다).
  **`TODO:`의 절반이 구현이 아니라 폐기로 닫혔다 — 재장전 유도 UI는 만들지 않는다(사용자 확정).** 딸깍 소리와 잔탄 표기의 `0`이 그 역할을 대신한다는 판단이고, 이걸 문서에 남기지 않으면 `TODO:` 문구를 근거로 다음 세션이 그 UI를 만든다. 그래서 `EmptyAmmoFire()`는 `Managers.Sound.Play("empty_gun_shot")` 한 줄이 전부이고 **이걸로 완결이다**. **중복 재생 가드를 새로 만들지 않은 것이 판단의 요점** — 같은 줄의 `_fireBlocked = true`가 해제 조건이 마우스 재클릭뿐이라 **트리거 1회당 딸깍 1번**을 이미 보장한다(연사 중 탄이 떨어지는 경우도 마지막 발 다음 인터벌에 한 번만 운다). 별도 쿨다운을 넣으면 같은 것을 두 곳에서 관리하게 된다.
  **이것이 프로젝트 최초의 `Managers.Sound.Play()` 호출부라, 사운드 경로 전체가 이번에 처음 런타임 검증됐다.** `_sound.Init()`이 `_setting.Init()`보다 먼저 도는 것(볼륨이 AudioSource에 제대로 얹힌다)과 `Resources.Load<AudioClip>("Sounds/...")` 경로는 코드로 확인했고, `AudioListener`·임포트 설정은 실제 재생으로 확인됐다. **남은 사운드 11개는 이제 호출 한 줄씩 붙이는 일만 남았다.** 다만 **실패가 조용하다는 점을 문서에 박았다** — 클립을 못 찾으면 `Play()`가 null 체크로 그냥 return하고 `GetOrAddAudioClip()`이 **그 null을 딕셔너리에 캐시까지 해서** 이후 시도도 전부 무음이며 로그도 없다. 로드 실패가 `LogError`로 드러나는 프리팹 쪽과 갈리는 지점이다.
  **HUD 쪽의 실질은 갱신 지점을 하나로 모은 것이다.** 표시가 바뀌어야 하는 자리가 다섯인데(전체 동기화·재장전 응답·아이템 조작이 모이는 `SyncInventoryUI()` / 장착·해제의 `SyncHeldWeapon()` / 전환 확정의 `ApplyServerWeaponState()` / 최초 장착의 `TryInitWeapon()` / 매 발사의 `Fire()`), 각자 텍스트를 건드리게 두면 **반드시 한 경로가 빠져 표시가 조용히 어긋난다.** `IngameScene.SyncWeaponUI()` 하나를 만들고 다섯이 그것만 부르게 했다. **순서 함정이 하나 있었다** — `ApplyEquipItem()`이 `SyncInventoryUI()`를 먼저 부르고 `SyncHeldWeapon()`을 나중에 부르므로 **앞의 호출 시점에는 손에 든 무기가 아직 낡은 값**이다. 양쪽 다 걸어 **마지막 호출이 이기게** 뒀다(텍스트 대입이라 중복 1회는 비용이 없다). `SyncWeaponUI()` 호출은 **`_ingameInventoryUI` null 가드보다 앞**에 뒀다 — 무기 표시가 인벤토리 UI 존재에 종속되면 안 되고, `SyncHealthBarMax()`가 같은 자리에 있는 것과 같은 이유다.
  **부수 효과로 탄창 선택 규칙이 한 곳으로 모였다.** `IsPrimaryWeaponApplyed`로 주/보조 탄창을 고르는 규칙이 `PlayerController.CurrentMagazine`에 있었는데, 표시부가 같은 판단을 하면 **세 번째 사본**이 생길 자리였다. `IngameInventory.CurrentMagazine`을 `CurrentWeapon` 옆에 만들어 출처로 삼고 `PlayerController`는 그것을 읽기만 하게 했다 — **쏘는 탄창과 보여주는 탄창이 갈리는 것을 구조로 막았다.** 예비탄은 `IngameInventory.CountAmmo(ammoItemId)` 신설(`WeaponSpec.AmmoType`이 곧 탄약 item_id)이며 **인벤토리 25칸만 센다** — 장착된 탄창은 `MagazineAmmoCount` 쪽이라 이중 계상이 되고 컨테이너 슬롯은 내 소지품이 아니다.
  **표시 규격 둘이 확정됐다(사용자 판단)** — ① **탄창 용량(`MaxAmmo`)은 표시하지 않는다**(프리팹에 자리가 없고 `30/30` 꼴로 합치지도 않는다) ② **맨손은 설계상 없는 상태다** → 숨김 경로를 만들지 않았다. **②를 문서에 박은 것이 중요하다** — 코드에는 맨손 경로가 곳곳에 살아 있어서(`EquipWeapon(0)`, `_fireInterval` 가드, `weapon_id = 0`) 그것만 보면 다음 세션이 숨김 UI를 만든다. 무기 슬롯 둘을 모두 비우는 조작 중에 잠깐 지나가므로 값만 비우고 창은 그대로 둔다.
  **프리팹 쪽 지적 둘 중 하나만 실제 문제였다** — `sortingOrder` 99가 `UIManager`의 SceneUI 0~/PopupUI 20~보다 크길래 지적했는데 **인게임 UI들의 관례라 결함이 아니었다**(사용자 확인). 같은 지적이 반복되지 않도록 `UI/CLAUDE.md`에 관례로 적었다. 반면 `raycastTarget`은 실제 문제라 **사용자 요청으로 프리팹의 그래픽 4개를 껐다**(창 배경 3개는 원래 꺼져 있었다) — HUD가 인벤토리를 연 상태에서 겹치는 영역의 드래그를 먹는 자리였다. **프리팹은 원래 손대지 않는 자리지만 스칼라 값 하나만 바꾸는 편집이라 GUID·fileID 참조는 건드리지 않았다.**
  **문서에서 잘못된 뉘앙스 하나를 걷어냈다(사용자 지시)** — `Resources/CLAUDE.md`가 "경로 로드는 **넷뿐이다**", "**유일하게** 경로로 로드된다"로 적고 있어 **개수를 제한하는 규칙처럼 읽혔는데 그런 의도가 없었다.** 현재 상태의 목록임을 명시하고, 갈리는 실제 기준(**런타임에 찍어내야 하는지**)을 대신 적었다. 같은 맥락으로 **`WeaponImage`는 `TODO:`로 남겼다** — 쓸 이미지가 미정이고 **`Images/WeaponSprites/*`는 여기 쓰는 것이 아니다**(사용자 확인). 제 추측("들어갈 자리로 보인다")이 문서에 남아 있었으면 다음 세션이 그대로 배선했을 자리다.
  **검증 완료(사용자 확인)** — 컴파일 / 빈 탄창 발사 시 효과음 / **무기 교체·발사·탄약 보급의 여파가 적절한 시점에 HUD에 반영되는 것**. 우선순위 10번이 걸고 있던 "재장전이 탄창을 몇 발 채웠는지"도 이걸로 눈에 보이게 됐다

---

## 진행 중 / 다음 할 것들

> **T번호는 과거 작업 참조용으로만 남아 있다.** T1~T18은 서버 변경분 반영 작업(`server-sync-todo.md`, 2026-08-27 삭제), T19~T24는 클라 작업이며 **전부 완료됐다**(T19 크로스헤어 / T20·T21 총알 궤적 / T22 빈 탄창 발사 모션 / T23 인게임 옵션 UI / T24 재장전·행동 잠금). 신규 작업에는 T번호를 새로 붙이지 않는다 — 완료 항목의 날짜 표기로 충분하다.

### 진행 우선사항

1. **마우스 감도 기본값 확정 + 설정 영속화 검증 (혼자 가능) — 배선은 확인됐고 값 결정이 남았다** — **슬라이더를 움직이면 시점 속도가 실제로 바뀌는 것까지 확인됐다**(2026-08-27). 슬라이더 → `Managers.Setting` → `PlayerController`가 매 프레임 읽는 경로가 끊기지 않았다는 뜻이다. **이 항목의 목적은 "적용되는가"가 아니라 기본값을 확정하는 것이므로 아직 닫히지 않았다.** (2026-08-26 #5)에서 감도가 **슬라이더 값 × 0.1 도/픽셀**로 바뀌어, `SettingManager` 기본값 1.0 기준 **예전보다 10배 느린 0.1 deg/px로 시작한다**(세로 가동 범위 170도가 마우스 170픽셀에서 1700픽셀이 된다). 볼 것: ① 0.1 deg/px가 과하게 느리지 않은지 ② **연사 중 총구를 끌어내리는 조작이 되는 감도인지** — 반동을 손으로 잡는 설계라 이게 이 값의 진짜 기준이다. 고칠 곳은 `PlayerController.MOUSE_SENSITIVITY_DEG_PER_PIXEL` 하나이고, **체감으로 수렴시켜야 하는 값이라 상수만 바꿔서는 안 맞는다** — 슬라이더로 몇 판 돌려 보고 확정할 것.
    - **저장 검증이 같은 자리에서 끝난다** — 감도를 맞추려면 어차피 껐다 켜야 하고, **설정 창을 이미 열어 본 상태라 셋 다 몇 초면 끝난다.** ① 감도·볼륨이 재실행에 유지되는지 ② **설정 창을 닫고 정상 종료가 아닌 방법으로**(에디터 플레이 모드 강제 정지 등) 끈 뒤에도 남는지 — `OnApplicationQuit` 하나로는 부족해서 '닫힘'에 건 이유를 이게 검증한다 ③ **취소로 닫아도 되돌린 값이 저장되는지**(빠뜨리면 메모리와 디스크가 어긋난 채 남는다)
    - **손상값 방어는 저장소를 직접 편집해 본다** — Windows는 `HKCU\Software\<회사>\<제품>`이다. `Setting.MouseSensitivity`를 999로 바꾸면 3.0으로 clamp되고(로드가 setter를 통과한다는 증거), `Setting.Resolution`에 없는 문자열을 넣으면 경고 로그와 함께 기본값으로 돌아간다
    - **감도 슬라이더 Min이 0.1인지 프리팹에서 확인할 것** — `SettingManager.MIN_MOUSE_SENSITIVITY`와 손으로 맞추는 값이다. 슬라이더 Min이 0이면 끝까지 내렸을 때 값은 0.1로 clamp되는데 핸들은 0에 남아, 다음에 창을 열면 0.1 위치로 튄다(동작은 정상, 표시만 어긋난다)
2. **로비 경로 잔여 검증 (혼자 가능)** — 두 건 다 "로비로 돌아가는 경로가 없어 여태 실행된 적 없는 코드"였고, 재매칭까지는 이미 통과했다.
    - **세션 유지의 실패 분기 둘** — (2026-08-24 #0) 판단의 핵심이었는데 정작 미검증이다. ① 서버 중지 상태로 복귀 → `Unreachable`, 재시도/취소 팝업이 뜨고 취소 시 추가 팝업 없이 TestStart 플로우로 합류하는지 ② Redis 세션 삭제 후 복귀 → `Expired`, **폴백이 먼저 수행되고 안내가 나중에** 뜨는지(`LobbyReconfirmUI.isActive` 때문에 순서가 뒤집히면 안내가 조용히 삼켜진다) ③ 게스트 로그인으로 같은 루프 → Header가 Guest로 복귀하는지 ④ 일반 최초 실행 경로 회귀 확인
    - **로그아웃 → ESC 종료** — (2026-08-24 #2)의 `OnDestroy` null 가드가 아직 한 번도 실행된 적이 없다
3. **발사선 시각화 검증 후 `TEMP:` 회수 (혼자 가능)** — (2026-08-25 #3)의 `DrawFireRayDebug`가 아직 붙어 있다. **에디터 플레이 모드에서 Scene 탭**으로 보이고(Game 탭은 Gizmos 토글 필요, **빌드에서는 안 보인다**) 잔상은 2분이다. 흰 십자=발사 원점(`_shotPoint`) · 청록 십자=조준점(`_aimTarget`) · 파랑=카메라 중심축 · 발사선(초록=유효 대상/노랑=대상 아님/빨강=미명중). 볼 것: ① **흰 십자가 가슴팍에 찍히는지**(카메라 위치면 `_shotPoint`가 null이라 폴백된 것이고 `Setup()`에 에러 로그가 함께 뜬다) ② 흰 십자에서 나간 선이 청록 십자를 지나는지(수렴 성립) ③ 파랑 선과 발사선이 근거리에서 벌어졌다가 조준점에서 만나는지 — **계속 평행하면 `MIN_CONVERGE_DIST`(0.5m) 폴백에 걸린 것**. 유효 대상 판정(초록)은 2인 전투로 이미 실증됐으므로 남은 확인은 ①~③뿐이다. **총알 궤적(T20)이 이걸 대체하지 않는다** — 궤적은 `MuzzlePoint`에서 출발해 원점·수렴을 아예 그리지 않는다
4. **2인 접속 한 판 — 사망 경로 세부 관측 + 이동 동기화 실측** — 골격(데미지 주고받기 · 사망 후 로비 복귀 · 귀환 후 로비 복귀)은 이미 통과했다. **2인을 붙이는 비용이 가장 비싸므로 아래를 한 판에 몰아서 볼 것.**
    - **사망 경로 7항목**: ① 진입 직후 HP·실드 만피 → 방어구 해제 시 0 ② 1/2 키 전환이 유예 뒤 반영 / 전환 대기 중 클릭이 안 나감 / 주무기 해제 시 보조무기가 손에 ③ 상대 화면에서 내 손의 무기가 바뀌는지 ④ 피격 시 게이지가 서버값으로 점프 ⑤ 처치 시 킬러 화면 `[KillFeed]` ⑥ 피해자 화면 탑뷰 연출 + 4초 조작 차단 + **상대는 계속 움직이는지**(관전 유지) ⑦ 킬러 쪽 `킬수 — 플레이어: 1`. **⑤가 안 나오면 ③을 볼 것** — `weapon_dbid`가 어긋나면 사격이 통째로 조용히 무시된다
    - **실드 기대값 대조** — 진입 직후 실드 만피(`[ShieldInit] 스폰 실드 최대치 적용 — N` 로그와 대조. 방어구 없이 스폰하면 `N=0`이 정상) → 방어구 **해제** 시 0 → 재착용 후 초당 100(=1%) 상승 → 피격 시 서버값으로 점프. HP 쪽은 (2026-08-25 #5)에서 닫혔다. 어긋나면 후보는 `SyncHealthBarMax()` 호출 시점과 실드 예측 누적 로직 둘뿐이다
    - **윈체스터 맵 이동 동기화** — 서버가 그 룸에서 `D2CUpdatePlayerStates`를 아예 브로드캐스트하지 않던 버그를 고쳤다. **클라 수정 사항은 없고 확인만 하면 된다** — 두 맵에서 타 플레이어 이동이 동일하게 동작하는지. **proto에 남은 마지막 `[작업사항]`이 이것이다**((2026-08-27 #7)에서 나머지가 정리됐다) — 확인이 끝나면 그 블록만 지우고 서버 사본에 반영할 것
    - **탑뷰 연출에서 시신이 보이는지** — 컬링 마스크를 원본 카메라에서 복사하므로, 1인칭에서 자기 몸을 숨기려고 레이어를 빼뒀다면 탑뷰에서도 안 보인다(마스크에 더하는 한 줄이면 된다). URP 후처리·전용 렌더러도 연출 카메라에는 복사되지 않는다
    - **히트박스 반지름 확인(30초)** — 별도 튜닝 세션은 불필요하다고 판정했다. 여러 판의 피격 실험에서 이상이 없었고 **부위별 차등 데미지 계획이 없어**(2026-08-27 서버 확인) 캡슐 11개는 실루엣 판정용일 뿐이다. 다만 **"너무 두껍다"는 실패 모드는 원리상 플레이로 드러나지 않으므로**(빗나간 탄이 맞는 건 쏘는 쪽이 못 느끼고 맞는 쪽도 원인을 못 짚는다) **상대를 선택해 Scene 뷰 캡슐 기즈모와 모델 실루엣이 겹치는지만 볼 것.** 어긋나면 `HITBOX_DEFS.RadiusFactor` 비율만 만진다. 판정이 다소 관대한 것 자체는 결함이 아니다
    - **시신·킬 피드의 남은 절반이 이번 판에서 처음 밟힌다**((2026-08-27 #6)에서 씬 초기화·컨테이너 상호작용까지는 통과했다) — **`SingleKillLog` 하위의 `KillerId`/`VictimId`가 없으면 첫 킬에서 NRE가 난다**(`transform.Find` 직후 `.GetComponent<TextMeshProUGUI>()`를 바로 부른다) — ⑤는 이제 로그가 아니라 **화면의 킬 로그가 5초 뒤 사라지는지**까지 보고, 죽은 자리에 **`PlayerLoot` 컨테이너가 서고 열리는지**를 함께 볼 것. 정상인데 오해하기 쉬운 것은 `오브젝트: 0` 하나만 남았다(전투 오브젝트가 아직 없다)
    - **킬 로그 표시명이 (2026-08-27 #7)에서 서버 이름(userId)으로 바뀌어 이번 판이 첫 실측이다** — ⓐ 킬러·피해자 자리에 아이디가 뜨는지(`objectId=N`이 보이면 서버가 이름을 비워 보낸 것이다) ⓑ **자기 킬·자기 죽음도 아이디로 뜨는지**(`"나"` 특별 취급을 폐기했다) ⓒ **가해자 없는 죽음**(낙하 등)에서 킬러 칸이 빈 문자열로 들어가 `[KillIcon] [피해자]` 꼴이 되는데 **실제 화면에서 어색하지 않은지**. **프리팹 구조는 (2026-08-28 #0)에서 확인됐고**(자식은 `KillIcon`·`KillerId`·`VictimId` 셋) **빈 문자열 유지가 확정이라 이건 눈으로 보는 것만 남았다** — 어색하면 고칠 곳은 `SingleKillLog.Init()` 한 곳(아이콘도 함께 `SetActive(false)`)이다
    - **명중 로그 `[Fire] 피격 대상 objectId=N`(`TEMP:`) 대조** — (2026-08-28 #1)에서 우선순위 10번을 닫을 때 이것만 남았다. **대상이 있어야 밟히므로 2인 판이 유일한 확인 자리**이고, ⑤가 나오는데 이 로그의 objectId가 상대와 다르면 히트스캔이 엉뚱한 것을 잡고 있는 것이다. 확인되면 `TEMP:` 회수
    - **오브젝트 킬 로그의 `name=`(`TEMP:`)** — 전투 오브젝트가 없어 이번 판에서는 밟히지 않는다. `ICombatTarget`이 보급되면 그때 `[ObjectKill] name=…`으로 킬러가 식별되는지 보고 `TEMP:` 둘을 함께 회수한다(우선순위 8번)
5. **게임 결과 표시 UI** — 흐름(전환·엔터 복귀·세션 유지)은 끝났고 `GameResultScene.LogGameResult()`가 `Util.Log`로만 뿌린다. 데이터는 `Managers.Scene.LastGameResult`(이탈 사유 + 인벤토리 25슬롯 + 무기 2·방어구 + 킬수 2종)이며, **사유별 의미 차이를 표시할 것** — Recalled=반출 확정, Dead·ConnectionLost=잃은 것. 프리팹은 `Assets/Resources/Prefabs/UI/GameResultSceneUI/`에 자리가 잡혀 있고 `UI/CLAUDE.md` 규칙을 따른다. **전환은 반드시 `CompleteMatchExit()`을 거칠 것** — 우회하는 강제 전환은 결과가 저장되지 않는다. 탈출(귀환) 연출은 여전히 `BeginMatchExit()`에 자리만 비어 있다
6. **귀환 스팟 씬 배치** — 맵 씬에 귀환 단말기 오브젝트 배치, `RecallSpotController` 부착 후 인스펙터에서 `_recallSpotIndex`를 서버 테이블 값에 맞춤. 조준 레이가 맞아야 하므로 트리거가 아닌 일반 콜라이더 사용
7. **귀환 진행 중 UI 피드백** — 승인~결과 사이 5초 구간 표시(카운트다운 등). `InteractText`는 매 프레임 재조회되므로 `virtual` 프로퍼티화하면 동적 텍스트 전환 가능
8. **TEMP 회수 3건 — 워치독 2건 제거 검토 + 오브젝트 킬 이름 로그** — 후자는 (2026-08-27 #7)에서 확인용으로만 실은 `killer_object_name`이며, **원복 지점이 `Handle_D2CNotifyObjectKilled`의 추출과 `IngameScene.HandleObjectKilled`의 파라미터 두 곳이라 한 쌍으로 되돌린다**(오브젝트 킬은 킬 피드 미적용이 확정이라 소비처가 생길 일이 없다). 워치독 둘은 귀환(`RECALL_TIMEOUT`/`_recallTimer`)과 행동 잠금(`ACTION_PENDING_TIMEOUT`). 후자는 (2026-08-27 #0)에서 무기 교체 전용 워치독이 흡수되며 재장전까지 함께 커버하게 됐다 — 응답이 유실되면 발사가 그 판 내내 막히므로 결과를 추측하지 않고 로컬 잠금만 푼다(판정 권한은 서버에 남는다). 서버 응답 신뢰성이 실측으로 확인되면 제거. **귀환 쪽은 실처리가 붙어 이제 검토 가능**하다
    - **`TODO:` 귀환·상호작용의 행동 잠금 편입 범위 (미결)** — 귀환(`_recallRequested`)은 구조가 사실상 같아 그대로 흡수되고 **워치독도 하나로 합쳐진다.** 반면 **상호작용은 컨테이너가 열려 있는 동안 지속되는 상태라 `Pending`으로 잡으면 여는 내내 모든 행동이 잠긴다** — "요청~응답 구간만" 잠그고 열린 상태는 기존 `_isContainerOpen`/`_uiOpenCount`에 맡기는 쪽이 유력하다. 코드에는 `IngameScene.RequestRecall()` 위에 같은 `TODO:`가 걸려 있다
9. **발사 이펙트 프리팹 준비 → 이펙트 구현** — 로컬 탄착 이펙트(`ProcessHit`), 수신 측 머즐 플래시/총성/탄착 이펙트(`HandleWeaponFireBroadcast`). 파티클·사운드 에셋이 `Resources/Prefabs/` 아래에 필요. **이펙트 기준점은 `_shotPoint`가 아니라 총구다** — `_shotPoint`(가슴팍)는 피격 판정 전용이고 시각 표현은 별도 기준을 쓰기로 확정했다. 판정선과 이펙트선이 어긋나는 것은 의도된 것이므로 맞추려 들지 말 것. **총구 기준점 추적은 (2026-08-26 #1)에서 이미 끝났다** — `GameObjectController.FindMuzzlePoint()`와 `PlayerController._muzzlePointTr`을 그대로 쓸 것
10. **실제 맵 씬에서 IngameScene 상속 완성** — `IngameScene`을 상속하는 맵별 씬 컴포넌트 구현. **씬 내장 UI 7종(`IngameInventoryUI`·`IngameDragGhost`·`InteractUI`·`IngameHealthBarUI`·`IngameSettingUI`·`IngameKillLogUI`·`IngameWeaponUI`)을 맵 씬마다 배치해야 한다** — `GameObject.Find()` 기반이라 없으면 그 기능만 조용히 죽는다(크로스헤어만 코드가 세우므로 예외). **배치 개수가 늘수록 `GetComponent` 결과를 보지 않는 바인딩 6곳이 그만큼 위험해진다**(킬 피드에만 가드가 있다) — `Find`+`GetComponent`+실패 로그를 헬퍼로 묶으면 한 번에 닫힌다
11. **설정값 실제 적용 — 해상도·창모드·FOV** — 저장은 (2026-08-27 #3)에서 끝났고 **적용만 남았다.** `SettingManager`가 화면에 실제로 반영하는 것은 여전히 `ApplyFrameRate()`와 볼륨뿐이라 **셋은 기억은 되는데 화면은 그대로다**(이전에도 적용되지 않았으므로 회귀는 아니다). `Screen.SetResolution()`·`Camera.fieldOfView`에 연결하면 되며, **화면 모드를 바꾸는 작업이라 에디터·빌드 동작이 갈리고 되돌리기가 번거롭다** — 착수 전에 에디터에서 원복하는 절차를 먼저 정할 것. FOV는 카메라가 `PlayerController` 하위라 인게임 진입 시점에 적용할 자리를 따로 잡아야 한다

### 진행 고려사항

> 코드를 고칠 때 지켜야 할 제약·불변식은 **모듈 `CLAUDE.md`**(`Assets/Scripts/*/CLAUDE.md`)에 있다. 여기에는 **아직 코드가 없어 적을 자리가 없는 것**과 **오해를 막기 위한 관찰**만 남긴다.

- **재장전·행동 잠금의 방어 경로 넷은 미검증으로 둔다** — (2026-08-27 #1)의 2인 검증은 정상 경로를 덮었을 뿐이다. ① `DENY_VERSION_MISMATCH` 1회 재요청(R 직후 2초 안에 인벤토리에서 탄약을 옮기면 재현된다. **재요청이 한 번만 나가는지**가 관건 — 무한 재시도는 버전이 계속 바뀌면 루프가 된다) ② 낡은 스냅샷 폐기(`[Reload] 낡은 스냅샷 폐기` 경고로 드러난다) ③ `fire_sequence` 복구(**되돌아간 시퀀스의 유일한 복구 경로**라 여기가 죽으면 그 무기의 발사가 계속 거부된다) ④ `ACTION_PENDING_TIMEOUT` 워치독. **①②는 마음먹으면 재현되고 ③④는 자연 재현이 어려워 방어 코드로 둔다** — `[Action] … 응답 미수신 (3초)`가 반복되면 워치독이 아니라 **서버가 43번을 안 보내는 것**이므로 서버 쪽부터 볼 것
- **resume 경로의 로비는 재화·인벤토리가 비어 보인다 (버그 아님)** — 서버 `/api/session/resume` 핸들러가 아직 뼈대라 `money: 0, inventory: []`를 반환한다. 서버 구현이 끝나기 전까지 정상이므로 클라 파싱 버그로 오해하지 말 것. **`http-api-spec.yaml`은 서버 사본과 동기화되어 있으므로 한쪽만 수정하지 말 것**
- **사운드 자산 12개 중 재생 호출부가 붙은 것은 `empty_gun_shot` 하나다** — 발사음(`gun_shot_1`)·발소리 3종·M4 재장전 3단계·UI음이 전부 파일로만 있다. **(2026-08-28 #1)에서 사운드 경로 전체가 처음 런타임 검증됐으므로 남은 11개는 호출 한 줄씩 붙이면 된다** — 우선순위 9번(발사 이펙트)이 "에셋 준비"를 선행으로 잡고 있는데 **소리 쪽은 제작도 배선 검증도 이미 끝나 있다는 뜻**이다. `m4_reload_*` 3종은 재장전 2초 유예에 그대로 얹을 수 있다. **소리가 안 나면 호출부가 아니라 파일명부터 볼 것** — 클립을 못 찾으면 무음이고 로그가 남지 않는다(`Assets/Resources/CLAUDE.md`)
- **`IngameWeaponUI`의 `WeaponImage`가 `TODO:`로 비어 있다 — 쓸 이미지가 미정이다(사용자 확인)** — 프리팹에 자리는 있고, **`Images/WeaponSprites/*`는 여기 쓰는 것이 아니다.** 자산이 정해지기 전까지는 배선할 수 없으므로 우선순위 목록에 올리지 않는다
- **`Resources/`는 참조 여부와 무관하게 전량 빌드에 포함된다** — 코드 참조가 없는 자산이 확인된 것만 여럿이다(`Prefabs/Scene/*` 4종, `System/@Managers`, `System/EventSystem`, `TestPlayer`, `TestLobbyScene/`, `LobbySettingUIBackup`, `Images/WeaponSprites/*`). **`System/EventSystem`은 코드가 쓰는 `UI/EventSystem`과 같은 것 두 벌**이다. 알파 단계라 용량이 문제는 아니고 **삭제 판단은 사용자 몫이므로 사실만 남긴다** — 목록은 `Assets/Resources/CLAUDE.md`에 있다
- **씬 이름 컨벤션 잔여 (판단 보류)** — `TestIngame2.unity`는 컨벤션대로면 `TestIngame2Scene.unity`이지만 `Define.Scene` enum에 없어 코드 영향이 없고, `WinchesterAlpha.unity`는 맵 에셋 성격이다. **enum에 올리는 순간 파일 이름을 맞춰야 한다**(`Scenes/CLAUDE.md`의 씬 이름 규칙)
